# Canon candidates

Knowledge this repo paid for that has no mechanical trigger here, staged for the parent.

This is a staging area, not a second index: an entry lives here only until it lands somewhere that binds, and the file is deleted when it empties. Nine entries were staged on 2026-09-02 and eight of them landed in `../fleet/FLEET.md` (fleet `51ed9a6`, `44504c6`), deleted here in the commit that recorded this:

- Reading the outcome curve before optimising the machinery, counting the decisions before tuning the decision maker, counting the options a decision offers before blaming the chooser, and two metrics disagreeing in direction are all instances of "verify the instrument before trusting the measurement" — whose canon text already cites this repo's 99.8%-forced-no-ops population as its anchor.
- Convergence across every variation of a family, and imitation plateauing across unrelated architectures, are the new green-gate rule: a result is bounded by what it searched and proves nothing past that bound, and past it can measure a different phenomenon entirely and report it just as confidently.
- Freezing the tree at a ref before a review is the standing critic rule's "given the diff, the claim, and the measurement" plus one-agent-at-a-time; the half where `npm test` went red because a mutation was live in the working tree is the new exit-status rule's "red deserves the same suspicion as green".
- "Pin every input that reproduces the defect, not just the one you thought of" landed verbatim, which is what the ninth entry was staged as an amendment to obtain.

---

## End a turn on what was done, not on what will be done next: before finishing, any sentence written in the future tense about work in this session is either executed first or explicitly flagged as not done

**From:** python_mini_metro / "A stated intention is not a completed action -- end a turn on what was done, not on what will be done next."

**What it adds that the canon does not say:** the constitution audits the claims that commands and tools make — an exit status is a claim about the command, a tool's "applied" reports a no-op identically to a refusal — and says nothing about the claims the session's own report makes. A future-tense sentence about this session's work is an unexecuted action that reads exactly like a completed one.

**Why it has no gate:** it is a property of a report, not of a repository.

**Anchor:** 2026-08-14; a turn that ended with "I'm going to stop this run and re-launch with checkpointing" while the run kept training and degraded from 40.0 to 35.5. The plan was correct, specific, and entirely unexecuted; the run was stopped only after the user asked whether it had been. What makes it hard to self-catch is that describing the fix produces the same sense of resolution as applying it, so the check that works is mechanical rather than introspective: "I will X" and "X is done" must never be indistinguishable in the same report.
