NOT CLEAN — four contracts remain unresolved.

1. [P1] Fractional progress needs a universal identity rule and an owner. `PLAN.md:36` applies preserve/reset only to composition changes, but spawning and route discovery occur before every service pass (`src/passenger_flow.py:177-181`) and can replace the next candidate. Current `Metro` stores only numeric timer/progress (`src/entity/metro.py:47-49`). Specify where pending `(kind, passenger)` identity lives and require every service update to preserve progress only when that identity remains exact. Cover completion, reset, line/metro removal, replacement rollback, exceptions, and stale-reference cleanup.

2. [P1] Query/reconciliation purity must include seeded RNG and transient plan fields. Boarding eligibility currently shuffles destination stations (`src/mediator.py:625-628`) and constructs candidate plans (`src/mediator.py:651-667`). The carriage transaction snapshot in `PLAN.md:25` omits RNG, so timer-callback failure can return false after advancing RNG, contradicting `PLAN.md:25,27`. Require an RNG-free/pure eligibility query or snapshot-and-restore both random streams and touched plan state; test success/failure and historical replay call counts.

3. [P1] Independent endpoint interpolation collapses a reversing consist. Under `PLAN.md:48`, at a straight terminal the previous first carriage is `head - spacing` and the current first carriage is `head + spacing`; linear interpolation puts it exactly on the locomotive at alpha 0.5. Every carriage collapses there simultaneously. Pin a non-collapsing turnaround policy and assert intermediate-alpha ordering/minimum spacing, not only alpha-zero/alpha-one direction.

4. [P2] The demonstrator cannot make its promised assertion through the specified privileged boundary. `PLAN.md:45` adds only carriage-control coordinates, while `PLAN.md:62` requires proving one attachment and one-unit availability reduction. Current `PrivilegedSnapshot` has no carriage counts (`src/rl/privileged_oracle.py:17-25`). Add UUID-free total/assigned/available carriage fields while preserving every existing field exactly.

The revised approach predicate, insufficient-arclength fallback, bottom control band, stable control topology, old locomotive-coordinate field, and low-level PlayerPixel boundary otherwise address the first-pass findings. No files were edited.
