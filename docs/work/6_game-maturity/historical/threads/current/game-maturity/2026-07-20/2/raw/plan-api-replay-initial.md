HIGH 1 — Reused marker Node is checkpoint-observably stale, while the current oracle cannot detect it.

- `PLAN.md:45` makes the old current target Node the one-alight marker. A Node owns topology-derived `.paths` (`src/graph/node.py:14-16`; populated fresh in `src/graph/graph_algo.py:14-27`). If a rider on another line will alight at a station removed from the edited line, its reused Node still names that edited Path.
- Checkpoint v2 serializes every marker Node’s path set (`src/recursive_checkpoint.py:307-319`), but `reference_errors()` only range-checks those path indices (`src/recursive_oracles.py:140-161`). It therefore reports CLEAN for a semantically stale marker.
- This refutes `PLAN.md:45,63,65,76-77`, `REVIEW.md:7`, and D-018’s no-stale-plan rationale even though runtime unloading only reads the station.
- Correction: create the marker from the candidate/current graph’s fresh Node for the alight station via a late graph-builder resolver; do not reuse an old Node. Add route-edit semantic assertions (or strengthen the oracle) for cursor bounds, `next_station == node_path[cursor].station`, and each Node’s path membership matching the current graph. Keep the existing reference oracle, but do not claim its current cleanliness proves these invariants.

HIGH 2 — The “exact rollback after any BaseException” promise exceeds the declared snapshot.

- `PLAN.md:53` promises exact pre-edit restoration after any post-mutation `BaseException`; `:57` requires button/color ownership, progression, clocks, spawning, deliveries/credits, and visual effects identical on rejection.
- `:55` snapshots neither `host.stations`/`all_stations` collection identity, path/color/button maps and button internals, nor the scalars/maps/effects in `:57`. Yet `:47` deliberately invokes a current public bulk-planning hook and `:64` injects partial-planning/arrival failures. A rebound hook can mutate one uncovered field then raise, defeating the stated guarantee.
- Correction: either (a) snapshot and directly restore every claimed canonical/public boundary, including collection-object rebindings, before the first live callback/effect, or (b) narrow the guarantee to transaction-owned state and explicitly exclude arbitrary hook side effects. Also clarify whether the late candidate factory is required pure; “off-live” alone does not make a patched factory unable to mutate the facade. Tests must match the chosen scope.

MEDIUM 3 — Exact action/facade/late-binding compatibility is underspecified.

- Live dispatch deliberately accepts dict and action-type subclasses (`src/input_coordinator.py:361,364`), and the facade test freezes this (`test/test_mediator_input_contract.py:362-380`). `PLAN.md:13/61` specifies exact payload types but never says top-level `action`/`type` retain `isinstance` semantics. An implementation using `type(action) is dict` or exact `type` string would regress the established surface.
- `PLAN.md:15` correctly requires selector XOR, exact id/index semantics, duplicate-ID failure, and late public `replace_path`; preserve those. Add explicit tests for selector presence XOR (including both keys where one is `None`), `str`/`int` subclasses, duplicate target object membership, and station-index aliasing to the same Station object.
- `PLAN.md:23` names only a late Path factory. Fresh waiting sentinels and fresh marker Nodes require TravelPlan/graph construction too. Existing boundaries pass factories/builders through resolver thunks (`src/mediator.py:421-443,598-605`) and keep PathLifecycle domain-import-free. State exact public signatures/defaults for all three facade methods, require late `Path`/`TravelPlan`/graph/bulk-hook resolution at the actual use point, and test rebound order plus the import-light/stateless invariant.

MEDIUM 4 — File-size and gate language can violate AGENTS and contains an unsupported claim.

- `PLAN.md:66` calls 625 an “established” Mediator ceiling, but this is the only repository occurrence; live Mediator is 605. Call 625 a plan-local cap or cite/record the decision. AGENTS requires under 500 preferred / 1,000 hard, not an established 625.
- New cases must not be appended indiscriminately: current `test_path_lifecycle.py`=495, `test_input_coordinator.py`=487, `test_passenger_flow.py`=498, `test_mediator_path_contract.py`=450, `test_env.py`=548, while `recursive_checkpoint.py`=498. `PLAN.md:66` constrains only new files. Correction: require new focused GM-05a test modules and a final line-count audit of every changed/new handwritten file; do not grow the 495/487/498 suites, and do not touch checkpoint source absent a schema need.
- `PLAN.md:68` should spell the mandated commands exactly: `python -m ruff check <files>`, `python -m ruff format --check <files>`, and `pre-commit run --files <files>` after any hook rewrite, plus full py313 unittest. Because replacement changes rendered topology, add a deterministic surface/geometry comparison or explicitly justify why exact segment geometry tests fully cover the visual effect.

MEDIUM 5 — Documentation plan needs exact truthfulness corrections, not a generic update.

- `GAME_RULES.md:34` currently says duplicate station picks are ignored, but live creation only ignores consecutive repeats, treats the first station as loop closure, and permits other nonconsecutive repeats. GM-05a’s unique replacement contract must be documented separately without falsely claiming create-path behavior changed. If creation is intentionally fixed too, that is extra behavior scope and needs its own RED tests.
- `ARCHITECTURE.md:272` hard-codes 12 PathLifecycle algorithms; three real replacement methods would make 15. InputCoordinator remains 19 if only `apply_action` gains a branch (`ARCHITECTURE.md:274`). Update exact counts/boundary text.
- D-018 says “persisted schema,” but GM-07 persistence does not exist; say canonical checkpoint/replay schema. `REVIEW.md:7` says “accepted draft” while `:3` says review in progress/no approval; say “current draft” until findings converge. Raw reviewer evidence must exist before Commit A because EVIDENCE already claims three audits.

No two-commit cursor defect found. `STATE.md` accurately opens `[GM-05a:A]` from green `8c4ba85`, GM-04c A/B evidence agrees, and `PLAN.md:70` correctly keeps GM-05b closed through B’s exact CI. For precision, Commit A’s state update should use the parent contract’s exact `awaiting-implementation-ci` cursor; Commit B must be evidence-only, record A SHA/run, mark GM-05a complete/advance to GM-05b, then be pushed and independently checked, with B’s exact result reconciled by GM-05b’s opening transaction.

Residual risks after correction: stopped-state enumeration and reversed/padding mappings remain algorithmically delicate; two-station loop ambiguity should stay fail-closed; success-path env checks must use `dt_ms=0`/no default tick so time advancement cannot masquerade as teleportation; canonical equality still cannot prove UUID/object identity, so the planned direct identity assertions remain mandatory.
