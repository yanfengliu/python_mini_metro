GM-05a live-plan adversarial review: NOT CLEAN. Prioritized findings:

[P1] Deferred alight must not invoke the current global bulk hook (`PLAN.md:47`). `PassengerFlow.find_travel_plan_for_passengers` scans every active station (`src/passenger_flow.py:428-438`); fallback/sentinel plans are always considered unplanned because validity is `next_path is not None` (`src/route_planner.py:28-34`); every considered rider calls `Mediator.get_stations_for_shape_type`, which shuffles with session Python RNG (`src/mediator.py:551-554`). Therefore one marker alight can re-evaluate and advance RNG for every unrelated unreachable waiter, and multiple marker alights in one boarding loop can repeat that globally. It can also mutate unrelated arrivals mid-dwell. Correction: introduce a scoped single-rider route/apply hook (or defer that rider to the next normal bulk pass); use global scope only once at replacement commit. Tests: marker + unrelated fallback waiter; two markers alighting in one call; assert unrelated plan/holder unchanged, exact Python shuffle count/order, NumPy unchanged, and destination/full-station branches invoke no replan before a successful transfer.

[P1] The promised synchronous global waiting replan is false if the existing bulk hook applies an arrival. The iterator loops the live `station.passengers` list (`src/route_planner.py:206-208`); the caller removes the yielded rider from that same list while the generator is suspended (`src/passenger_flow.py:439-443`), so the adjacent rider is skipped. This is explicitly a tested current contract: `test_caller_removal_before_resume_preserves_live_adjacent_skip` (`test/test_route_planner_iterators.py:338-373`). `PLAN.md:41/64` explicitly retains arrival/delete and fault-injects after arrival, so it cannot claim all waiters handled. Correction: transaction-scoped stable `(station, rider)` capture/targeted batch, or reject/pre-handle arrived holder states; do not silently change the default live iterator contract. Test `[arrived, routed, fallback]` in one station and prove all three dispositions plus exact rollback after arrival.

[P1] The all-onboard scope is underspecified beyond target-line metros. Movement iterates `path.metros` (`src/passenger_flow.py:151-152`), while passenger transfer processing iterates only `host.metros` (`src/passenger_flow.py:312-313`). Checkpoint deliberately unions both fleets/passenger holders (`src/recursive_checkpoint.py:218-229`), and duplicate locations overwrite each other (`src/recursive_checkpoint.py:239-251`); reference oracles only bounds-check refs. `PLAN.md:29` validates fleet ownership only for target-line metros, but `PLAN.md:43` mutates every onboard plan network-wide. An unrelated path-only/global-only metro, duplicate passenger holder/global entry, or shared TravelPlan alias can be marked and then never safely alight—or one rider’s exhaustion can reset another’s shared plan. Correction: preflight the identity union of every live path fleet and global fleet; require each live metro exactly once globally and on exactly one path with matching path object/ID, every active passenger exactly once globally/in exactly one holder, and unique plan objects for onboard riders. Add malformed-state zero-effect tests for each alias/orphan case.

[P1] Plan validation can violate the zero-effect rejection contract unless it forbids `TravelPlan.get_next_station()` during preflight. The getter writes `next_station` and directly indexes `node_path[next_station_idx]` (`src/travel_plan.py:20-25`). `PLAN.md:43` says validate cursor/current target before mutation, while 53 promises expected rejection before live mutation. Correction: resolve the target from raw fields without the getter; require exact integer cursor in range, `plan.next_station is plan.node_path[cursor].station`, `plan.next_path is owning_path`, target active and identity-present on that line. Tests with stale cached next_station, bool/negative/out-of-range cursor, foreign/equal-ID station must return False with exact plan fields/list identity/checkpoint/RNG unchanged. On exhaustion, reset all four sentinel fields before any code can call the getter again.

[P2] Reusing the old current target Node makes `PLAN.md:45`’s “node adjacency is never consulted” false and can persist stale pre-edit topology. Checkpoint serialization reads every plan node’s `.paths` (`src/recursive_checkpoint.py:307-319`). If the edited line is added to/removed from an unrelated-line rider’s target station, that old Node’s `path_indices` remain pre-edit. `reference_errors` merely range-checks those indices and never validates cursor/next-station/path membership (`src/recursive_oracles.py:140-161`), so “clean oracles” will miss it. Correction: build the one-node marker from the post-commit graph/current path membership, not the historical Node; add semantic assertions that cursor=0, cached station matches node, current path matches holder, and node path membership matches current topology. Checkpoint test both adding and removing the edited line at another line’s planned alight.

RNG/rollback test correction: successful global replan needs an explicit seeded-twin expected Python state (one destination shuffle per captured waiter, in documented holder/rider order) and exact NumPy equality; replay equality alone only proves repeatability, not correct consumption. No-op/reject/preflight/factory failure must leave both states exact; failures after first/partial plan/arrival must restore both. Snapshot NumPy state deeply before first live effect. Full-station test should prove marker/cursor/RNG stay unchanged until `station.has_room()` permits `metro.move_passenger` (`src/passenger_flow.py:368-377`); destination-at-target must take the earlier delivery branch (`src/passenger_flow.py:355-366`), award once, delete plan, and consume no marker-replan RNG.

Residual risk after corrections: the untagged one-node marker is behaviorally distinguishable only by destination-first handling plus exhaustion. Add a direct semantic invariant test because current reference oracles cannot attest that distinction. Read-only review; no files edited/tests run.
