# GM-06b Commit A hosted failure and correction

Implementation Commit A `df04fb6b191f3d8609e316623950b06416d61c19` triggered exact [run 29801037756](https://github.com/yanfengliu/python_mini_metro/actions/runs/29801037756). `rl-smoke` job `88542015196` passed every step. `build` job `88542015227` passed isolated setup, recursive-loop contracts, and the clean recursive pass, then failed one of 820 Python tests with 22 optional-stack skips.

The sole failure was `test_player_protocol_info_actions_and_task_identities_remain_exact`: the new test hardcoded mixed local working-tree raw bytes at `b195946ef62db7058b5ff8c295045d285019cce10b2a12d8b86d28f180670f93`, while GitHub's unchanged LF checkout produced `f6fa3ad50bb992152ea0f24dff35603e8e906714cf58c5fcc359ede4af54f65c`. Adversarial review then proved a fresh `core.autocrlf=true` materialization has another valid raw-byte hash, so platform labels or a finite live-worktree allowlist would be unsound. No production file in the training fingerprint source set changed in GM-06b, and the production raw-byte fingerprint algorithm is intentionally unchanged by the correction.

Local RED reproduced the hosted boundary by copying every declared training source into a temporary canonical-LF checkout: the new regression failed exact `f6fa... != b195...`. The final correction removes the live-worktree raw-byte assertion, requires that deterministic source materialization to equal exact `f6fa...`, and proves that changing one copied source from LF to CRLF changes the production fingerprint; semantic source changes and raw-byte EOL sensitivity remain covered while Git LF/CRLF materializations no longer reject the test itself. Trainer/artifact code remains untouched. The targeted methods, GM-06b discovery, and definitive local suite then passed 2, 73, and 826 tests respectively, with 12 expected skips in the full suite.

Corrective `[GM-06b:A2]` passed review, static/hook gates, and exact staged audits; commit, push, and its own exact-head workflow remain before Commit B can finalize the work unit.
