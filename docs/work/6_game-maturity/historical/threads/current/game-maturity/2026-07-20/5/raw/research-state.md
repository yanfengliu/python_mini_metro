# GM-06a observation and checkpoint research

Live structured observation exposes assigned Metros but no labeled inventory. Canonical checkpoint v2 already records total under `progression.limits.num_metros` and normalized global assigned Metros under `structured.metros`; `metroMotion` can include additional path-owned entities in malformed/detached graphs and is not the selected arithmetic source.

The research lane proposed a validated inventory value, structured and array mirrors, checkpoint v3, and recursive-input v4. The plan accepts the need for labeled structured totals but rejects the duplicate array/checkpoint/version surfaces because availability is derived exactly from existing canonical facts and is not new latent state. Existing session scalars are not array-mirrored, and direct callers intentionally mutate the total cap.

Required evidence instead: exact runtime/structured/checkpoint arithmetic, no new array key, checkpoint-v1/v2 and recursive/agent-input legacy compatibility without input mutation, unchanged action/protocol/task identities, and a changed content fingerprint only.

No files were changed by this research lane.
