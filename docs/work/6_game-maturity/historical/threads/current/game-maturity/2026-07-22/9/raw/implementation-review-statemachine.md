# GM-07a adversarial implementation review — state-machine/UX lane (verbatim)

Method: full code read of `app_controller.py`, `menu_screens.py`, `main.py` (+ diff vs HEAD), the mediator pause store, coordinator, session, clock, renderer/interpolator; 80 probe checks across a controller-level event×state matrix probe (39) and a real `run_game` probe under `SDL_VIDEODRIVER=dummy` with a spy controller, SDL-timer synthetic events, per-frame advance/draw/byte traces, and screenshots (41). Focused suites re-run (36 OK); full py313 suite re-run independently: OK (skipped=12).

## Findings

- F1 MEDIUM (coverage, plan-promised): the loop's new frame-composition branches are unpinned. `main.py:78-94` — TITLE `advance(0)`+title draw, the same-frame `session is not previous_session` rebind `advance(0)`, and pause-menu compositing are covered by no unit test: every `run_game` test either mocks `AppController` entirely or predates the states. Live probes prove today's behavior correct, but deleting the rebind or swapping the TITLE branch would pass the entire suite. Fix: one loop test with the real controller driving a TITLE frame, an Escape→Restart frame, and asserting advance args plus draw selection per frame.
- F2 LOW (UX hazard, net-new): pause-menu buttons fire on a bare, unarmed MOUSE_UP (`app_controller.py:100-116`). Open the menu mid-drag, release the still-held button over Restart — the run is discarded instantly (probed with a real armed drag). Fix option: require a same-control MOUSE_DOWN in PAUSE_MENU before honoring the UP.
- F3 LOW (UX race, inherited binding collision): Escape near the game-over flip exits the app — KEYDOWN while playing (intending the menu), flag flips during that frame's advance, KEYUP routes to GAME_OVER → SystemExit; ~100 ms window. Both bindings documented; note or debounce if desired.

Confirmed-intentional deviations (no action): restart mid-pump no longer breaks — trailing same-pump events reach the new triple (designed per F2-fold; double-click restart probed safe); outside-viewport UP at game over maps to (-1,-1) → None with identical outcomes; Escape on TITLE is a silent no-op; HUD pause button renders active under the menu (correct derived bool); pre-menu hover freezes under the menu (cosmetic, byte-stable).

## Confirmed sound (probed)

Matrix: R never restarts while PLAYING; Space/1/2/3/RETURN/R blocked in PAUSE_MENU with zero mediator/session traffic and no post-resume latch; DOWN/MOTION never actuate menus; letterbox (-1,-1) UP no-op in TITLE/PAUSE_MENU/GAME_OVER and the pinned cancel in PLAYING; None events no-op; KEY_DOWN cannot trigger bindings; double-Escape round-trips; user+menu reasons compose/release independently. Game-over edge: flip only inside advance; menu-held advance early-returns (time/steps frozen across 10×250 ms), so game-over under the menu is impossible; first post-flip event transitions before routing; overlay and routing agree from the first over-frame. Factory/rebinding: 90 TITLE frames leave time_ms=0, steps=0, byte-identical RNG states, renderer never invoked; rebinds route dispatch/advance/draw to the new triple same-frame with advance(0); `prepare_layout` runs inside the factory; old holds released; no old-session advance after switch. Render pipeline: letterbox block byte-identical to HEAD; menu chrome drawn pre-scale on `game_surface`; 39 consecutive menu frames byte-identical under elapsed jitter {16,17}; title renders with a never-advanced triple. Manual run screenshots: title, pause menu over frozen game, resumed, restarted fresh network, game-over overlay; all SystemExit paths propagate exactly as the old loop. Docs accurate; no stray SETTINGS references; line budgets respected.

## Verdict: NOT CLEAN — no behavioral defects (80/80 probes green); F1 substantive coverage gap vs the plan's committed scope; F2/F3 low-severity UX hazards needing explicit disposition.
