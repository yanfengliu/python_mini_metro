CLEAN.

Verified live code paths:

- Fresh recurrent omission resolves to exact ten-frame profiled history; explicit PPO omission remains contiguous-8.
- Explicit selectors override defaults.
- Resume and evaluation always pass the authenticated manifest descriptor through environment construction and persistence.
- Invalid, incomplete, drifted, and ineligible campaigns suppress medians and performance gates with one structural reason.
- All reviewed files remain below 500 LOC; largest are `test_rl_training.py` at 493 and `resource_profile.py` at 488.

Validation:

- Core focused suite: 20/20 passed.
- Exact RL focused suite: 39/39 passed.
- Ruff check and format check passed for all eight changed code/test files.
