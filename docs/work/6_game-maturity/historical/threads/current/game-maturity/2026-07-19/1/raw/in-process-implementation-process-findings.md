Second transaction finding: `docs/threads/current/game-maturity/2026-07-19/1/REVIEW.md:15` hardcodes `gpt-5.6-sol` and `claude-fable-5[1m]`, violating the repo rule that model pins live only in `../loop-ops/docs/skills/multi-cli-review.md` and never elsewhere. The raw logs can remain verbatim, but synthesis should say only “fleet-pinned Codex/Claude lane” (as line 27 already largely does). Runnable `--expected` replay independently passed and regenerated both files byte-for-byte: artifact SHA `d096c039...`, summary SHA `dd595a71...`; focused 33-test direct/facade/effect/consumer slice also passed.

---

Re-review finds one remaining factual documentation defect plus one residual naming cleanup: `EVIDENCE.md:504` says `git diff --check` was clean apart from an inaccessible global-ignore warning. Live `git ... diff --check` exits 0 but emits five LF→CRLF conversion warnings (ARCHITECTURE, PROGRESS, DECISIONS, EVIDENCE, STATE), not the global-ignore warning. Record the actual warning class or simply “exited zero; five line-ending conversion warnings were informational.” Also `EVIDENCE.md:506` still says “Claude Fable”; for the same single-pin-site policy and consistency with corrected REVIEW, use “fleet-pinned Claude invocation.” Everything else in updated REVIEW/STATE/EVIDENCE/diff is internally consistent so far.

---

[P1 reproducibility] The byte-exact `--expected` contract is not durable across a normal Windows checkout. Repo-local `core.autocrlf=true`; both new JSON artifacts have `text/eol` unspecified. A temporary-index clean-checkout simulation (real index/refs untouched) converts the canonical artifact’s terminal LF to CRLF: 110,080 bytes / `d096c039...` becomes 110,081 bytes / `c2a9a796...`. The summary likewise becomes 1,038 bytes / `51f44ce9...`. Because the verifier emits LF and compares raw `expected_bytes`, `--expected` will fail after Git re-materializes these files. Evidence: `git check-attr text eol` => unspecified; simulated `checkout-index -c core.autocrlf=true` ends `0D 0A`. Add an LF policy for these canonical evidence files (e.g. path-scoped `.gitattributes` `text eol=lf`) or make expected replay read/validate canonical Git bytes without weakening byte equality. Re-run the clean-checkout replay before CLEAN. This is the only remaining substantive defect I found.
