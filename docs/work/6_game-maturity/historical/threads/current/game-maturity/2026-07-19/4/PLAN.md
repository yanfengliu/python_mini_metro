# GM-04b safe setup and verification plan

Status: implementation Commit A `8cff6201dd4d0b2b07ae1fbf9068a5b179109457` and corrective Commit A2 `16d786098543f32d3b00e8aef37b56a88f67b9a5` exposed and preserved the npm 10 root-layout and downstream portability/snapshot-order failures; corrective Commit A3 `069973c8a1b0018f6edcc2de98e77ae7c139af78` closed those defects plus both fresh adversarial findings and passed exact workflow run `29757294004`; evidence-only Commit B `41ecfc691ac4d4784acff549f06e3fe2f26e9c3b` passed exact workflow run `29758092140`, so GM-04b is remotely finalized and GM-04c has opened

Transaction marker: `[GM-04b:B]`

## Baseline and boundary

GM-04a Commit A `585dc6067d1c8c2c0a115bd31f1d4e238dc25ddc` passed workflow run `29730625404`, and evidence-only Commit B `28a6c7e6082f03a0590fb27d99996837575d5062` passed workflow run `29731075431`. The checked-in descriptor, exact package/complete lock graph, exact `.npmrc` link-and-silent policy, isolated path, full-history CI path, and physical provenance checks now agree on civ-engine 2.2.0 at commit `e0cb614a516c449159a4562c2ac45bd40bffd3df` and runtime digest `960f4af06a8012298ca7f6fda65e64590a78e059fbe4ca154c0ca5ce33282891`.

GM-04b adds the safe public setup/verifier and fail-fast public-command hooks. GM-04c retains the live repeated-setup proof, complete final Node-suite and clean recursive-pass evidence, hostile live-sibling mismatch proof, final credential scan, and overall GM-04 reconciliation.

The tracked `package.json` and `.npmrc`, selected top-level npm and Node executables, and their pre-start environment/configuration form an explicit trusted bootstrap boundary for the canonical commands. Node evaluates `NODE_OPTIONS`, CLI startup options, and npm-provided Node options before loading repository modules; the setup/guard mains detect non-empty `NODE_OPTIONS` or `process.execArgv` after startup and refuse their own later effects, but cannot undo pre-main code, so caller-selected bootstrap overrides are outside attestation. After clean startup, caller script arguments, ambient configuration passed toward setup children, credentials, filesystem links and reparse points, linked worktrees, existing checkout state, and concurrent invocations are untrusted; every Git/npm/build child receives the scrubbed allowlisted environment, and the descriptor remains the only authority for repository URL, install path, package, version, commit, and runtime digest.

## Frozen command contract

- Add one dependency-light setup entry point, `scripts/civ-engine-setup.mjs`, exposed publicly as `npm run setup:civ-engine`, plus one dependency-light guard/dispatcher for each canonical Node test/playtest body and one shared startup assertion used by both actual mains before their own effects.
- Accept exactly three argv shapes: setup with no argument, strict read-only verification with `--verify-only`, and recursive-canary verification with `--verify-only --allow-dirty`. Reject every other argument and ordering, including caller-selected URL, path, ref, credentials, force, repair, or dirty setup.
- Resolve the repository root from `import.meta.url`, never ambient `cwd`; importing the module must not import or execute civ-engine.
- Setup obtains or reuses only the descriptor-owned checkout, authenticates the complete physical non-generated working tree to exact detached-`HEAD` blob identity modulo LF/CRLF normalization allowed only by the authenticated repository-wide `* text=auto` policy before installation or build, validates the complete root lock semantics against the descriptor-pinned canonical parsed-lock digest, exclusively creates only a missing exact root local-dependency link, performs a fresh default-resolution provenance capture, and prints an attributable credential-free summary.
- Registered source and engine captures accept data properties only and recursively freeze their complete object graphs before exposure; policy checks, artifact serialization, and start/end comparison therefore consume immutable captured values rather than caller-mutable or accessor-backed state.
- Verification performs no network, lock acquisition, Git mutation, npm invocation, filesystem creation, or engine import. It refuses to race an active setup lock, uses no-optional-lock sanitized Git reads, and preserves the Git index and repository filesystem byte-for-byte. Strict mode requires exact clean content; recursive-canary mode relaxes only the already attributed commit/version/digest/status policy while continuing to reject unavailable, nonphysical, aliased, wrong-root, or wrong-runtime identity.

## Existing-path fail-closed policy

- A missing pin may be created transactionally. An existing exact standalone clean detached pin may be verified, may rebuild only ignored generated dependencies/output after complete `HEAD` blob-identity authentication with only the authenticated `* text=auto` LF/CRLF normalization and all containment checks, and may create only an absent expected root `node_modules/civ-engine` link below a validated physical root container and descriptor-digested install graph. A stale link, physical slot, nested `scripts/node_modules` shadow, or any other external resolution shadow fails before root mutation. A completely valid installation returns without clone, fetch, checkout, build, dependency installation, or root-link work.
- Before any repository-aware Git command, perform a filesystem-only metadata audit: require a physical contained `.git` directory, config, detached hexadecimal `HEAD`, objects and refs; recursively reject reparse points; reject config includes, worktree/fsmonitor/hooks/filter/credential/command redirection, alternates, grafts, replace refs, shallow/external common/object storage, and unexpected remote configuration. Only then use sanitized read commands to confirm top-level/common directory, commit, detached state, remotes, and cleanliness.
- Reject a destination file, symlink, junction, non-Git directory, linked worktree, `.git` file/link, external Git directory/common directory, attached branch, wrong commit, dirty tracked or untracked state, wrong/credential-bearing/multiple remote, nonphysical package/lock file, or linked/escaping generated boundary before any network, checkout, build, install, repair, or deletion.
- Never reset, clean, force-checkout, fetch into, replace, or recursively delete a suspicious pre-existing pin. Never inspect or mutate `../civ-engine` as part of setup.
- Before any npm phase that can replace generated content, recursively `lstat` the pin `node_modules` and `dist` trees, reject directory reparse points and every escaping link, then revalidate immediately before execution. Independently audit the physical root `node_modules` container and exact allowed entries before classification, linking, and strict verification. Fixture sentinels must prove pre-existing linked targets remain byte-identical on refusal.

## Transactional setup state machine

1. Validate descriptor/root package/descriptor-pinned canonical parsed-lock digest/`.npmrc` parity and acquire an exclusive tokenized setup lock before classifying either missing or existing state; hold it through fast-path verification, every rebuild/install, final provenance, and cleanup. An existing lock fails clearly and is never removed by a non-owner.
2. Atomically create one uniquely named ignored transaction parent on the repository filesystem, record a random ownership marker plus its physical identity there, and reserve an initially absent `checkout/` child as the clone destination. This parent also supplies controlled empty home/config/temp surfaces to child processes.
3. For a missing pin, clone only the fixed public HTTPS descriptor URL with `--no-checkout --no-tags` into the empty child, with prompts, credentials, hooks, templates, unsafe protocols, and ambient Git config injection disabled; filesystem-audit the resulting metadata and prove the descriptor object before an unforced detached checkout, then run the full audit.
4. For both staged and existing pins, perform the filesystem-first metadata/generated-tree audit, then prove top-level, common directory, remotes, detached `HEAD`, commit, package metadata, lock metadata, generated-directory boundaries, and every physical non-generated file's exact `HEAD` blob identity modulo LF/CRLF normalization allowed only by the authenticated repository-wide `* text=auto` policy before any npm installation or build executes.
5. If the explicit pin runtime is not already exact, run the pin lock install with `npm ci --ignore-scripts` through the validated Node-distribution CLI, then invoke the exact physical pin-local TypeScript CLI through `process.execPath` with no npm lifecycle or caller `PATH` lookup, then require an exact fresh explicit-root provenance capture.
6. For a missing pin, atomically claim the still-absent final pin directory and record its physical identity plus transaction token in a transaction-side claim file. Recursively publish authenticated directories through exclusive `mkdir`, regular files through `COPYFILE_EXCL`, and contained links through exclusive remapped symlinks; every primitive fails rather than replacing an intervening winner, and a complete source/destination byte, mode, type, and link-target comparison precedes success. Publication performs no rename, rollback, deletion, or shared-inode hard linking at the final path. Reauthenticate the complete detached Git/content/generated-tree contract at that final path before root linking; any failure after the directory claim retains both the partial pin and its matching source transaction for exact inspection, while successful publication leaves an exact marker-free pin and the authenticated transaction source intact until ordinary token-owned cleanup.
7. If the default resolution is not already exact solely because the expected root dependency slot is absent, require a safe physical root `node_modules` boundary plus the exact root package, descriptor-pinned canonical parsed-lock digest, and `.npmrc` contract; capture the container's exact bigint device/inode identity; exclusively create `node_modules/civ-engine` as the exact pin symlink/junction without invoking root npm; recheck the container identity; revalidate the complete root contract after the exact-link audit; and require exact fresh default-resolution provenance at `/.civ-engine-pin/`. A stale or physical expected slot, any other root entry, any nested/external shadow, or lock semantic drift fails without replacement or deletion. An `EEXIST` race winner is preserved. A path-level container swap after the last pre-create identity check is detected after atomic link creation; both containers are retained, and the exact task-created link remains wherever that atomic operation landed because path-based Node APIs cannot safely delete either state. Concurrent package/lock/`.npmrc` drift during link creation is rejected by final validation with the created link and drifted contract retained for inspection.
8. Release only the token-owned lock. On pre-claim failure, remove only the exact token-owned physical contained transaction parent after comparing marker and physical identity. On post-claim publication failure, retain that transaction with the identity-recorded partial pin while still releasing an owned setup lock; after successful publication, retain the verified marker-free pin for an attributable retry if root linking fails and remove the now-redundant transaction normally. Never sweep another or crash-left staging parent or publication claim.
9. Freeze exact ignore rules for the pin, lock, and tokenized transaction parents. A process crash can leave ignored artifacts, an attributable lock, and a transaction-side identity record for a partial final-path claim; a crash between directory creation and record creation remains deliberately unattributed. Documentation gives exact inspection/manual-removal boundaries because automatic dead-owner deletion risks PID reuse, deleting a live owner, or touching a destination winner.

## Subprocess and credential boundary

- Centralize subprocess execution with argv arrays, `shell: false`, explicit `cwd`, ignored stdin, captured bounded output, `windowsHide: true`, checked spawn/error/signal/status handling, and phase-labelled redacted failures.
- After the top-level setup process passes the trusted-bootstrap assertion, give every Git/npm/build child a minimal allowlisted environment with controlled empty `HOME`/`USERPROFILE`, npm user/global configs, and temp paths. Exclude inherited cloud credentials, auth sockets, credentialed proxy values, `NODE_OPTIONS`, CI tokens/secrets, all behavior-changing `npm_config_*`/lifecycle variables, and all ambient Git config/object/ref/transport redirection variables; sentinel-secret tests prove none reach those child processes or their diagnostics. This child scrubbing does not retroactively sanitize setup's own already-started Node process.
- Disable Git prompts, credential helpers, hooks, templates, filesystem monitoring, optional locks, replace objects, unsafe protocols, and system/user configuration. Compare filesystem-audited raw local remote configuration without printing credential-bearing values; both provenance modules share the hardened no-write Git runner.
- Root linking invokes no npm or lifecycle script. Pin installation suppresses dependency scripts, and the reviewed pin build bypasses npm lifecycle resolution entirely by launching only the physical pin-local TypeScript CLI through exact `process.execPath`.
- On both POSIX and Windows, validate the npm launcher and `npm-cli.js` inside the active Node distribution, then launch that exact JavaScript CLI through physical `process.execPath` with `shell: false`. Windows live evidence shows direct `npm.cmd` plus `shell: false` returns `EINVAL`, while `cmd.exe` reparses metacharacters even when the outer spawn is shell-free; POSIX validation likewise prevents a substituted launcher from selecting another CLI. Reject reparse/substituted launcher or CLI paths and never consult caller `PATH`, `npm_execpath`, or `ComSpec` for npm identity.

## Public hooks and CI parity

- Add `pretest` and `preplaytest:verify` scripts that invoke strict `--verify-only`, plus `preplaytest:recursive` using canary verification, as defense in depth.
- After clean module startup, make the actual `test`, `playtest:verify`, and `playtest:recursive` package-script bodies invoke the dependency-light guard/dispatcher before spawning their fixed Node body. The test body rejects every forwarded package-script argument and always invokes the complete `node --test` suite; focused development runs use direct `node --test <files>`. Suppressing npm pre-hooks alone still reaches this guard under the trusted bootstrap, and the recursive guard derives strict versus canary policy from the forwarded script arguments. The exact root `.npmrc` pins `loglevel=silent` so the standard canonical npm invocation does not echo expanded arguments before categorical guard diagnostics. npm/Node bootstrap overrides such as npm `--node-options`, ambient `NODE_OPTIONS`, or direct Node CLI options are outside this post-start guarantee and are detected only after any pre-main behavior.
- Freeze the recursive sentinel matrix: strict runs reject dirty status and wrong commit/version/digest before dynamic import/body effects; an explicit `--allow-dirty` run permits only those attributed content mismatches; both modes reject unavailable, nonphysical, aliased, wrong-root, wrong-runtime, or package-identity mismatch.
- Replace CI's duplicated nested engine checkout/build/root-install/inline-provenance sequence with `npm run setup:civ-engine` from a missing pin. Keep `contents: read`, root checkout `persist-credentials: false`, `fetch-depth: 0` for complete Git history, and the same setup-before-tests-before-recursive order. Add Node setup plus missing-pin setup/strict-verification smoke to the Windows job so the physical npm CLI path runs on a real Windows runner.
- Update README to use the one-line public setup, document strict verification/refusal behavior and automatic hooks, and remove the PowerShell-only manual clone sequence. Update `AGENTS.md`, `ARCHITECTURE.md`, `PROGRESS.md`, and the parent cursor/evidence truthfully.

## TDD and validation sequence

1. Add baseline-runnable contract tests that initially fail only because the setup/guard modules and commands are missing. Freeze shared categorical startup-taint detection before setup/guard effects, parser modes, cwd independence, dependency-light import, fixed descriptor authority, command planning, Windows/POSIX Node-distribution npm identity, direct pin-local TypeScript identity, phase ordering, short-circuit behavior, exact root install-graph/npmrc validation, missing-only exact root linking, and package/workflow wiring.
2. Add filesystem/Git fixture tests for every rejected existing state, filesystem-first malicious config/alternates/replace/shallow metadata, linked Git metadata, remotes, detached commit, complete non-generated `HEAD` blob-identity mismatches outside the authenticated `* text=auto` LF/CRLF normalization, recursively nested generated-tree reparse points, exclusive all-setup locking, two-level ownership cleanup, promotion races, exact ignore parity, optional-lock/index no-write behavior, and preservation of unrelated/sibling state.
3. Add guarded-body sentinel tests proving that, after clean startup, suppressing lifecycle hooks alone does not bypass identity, `npm test` forwards no package-script argv into child Node, strict versus explicit recursive canary behavior is exact, every body remains unexecuted after identity failure, and no dirty or caller-selected setup override is accepted. Add live/unit probes showing startup taint is categorically detected without reflection before setup/guard effects while recording that pre-main code cannot be undone. Exercise missing-pin setup and strict verify on real Ubuntu and Windows CI.
4. Implement the smallest focused setup, checkout-safety, and subprocess boundaries needed to pass those tests, keeping each handwritten file below 500 lines.
5. Run the focused setup suites, all 44 frozen pre-GM04 test names plus the exact larger current Node suite, syntax/JSON/YAML checks, `npm ls --depth=0`, root/pin dependency audits, full py313, exact RL, changed-path pre-commit, cached diff/credential scans, and three independent adversarial implementation reviews.
6. Preserve the real corrective history: Commit A failed during setup, Commit A2 proved setup on both hosted platforms but failed later in Ubuntu's canonical Node suite, and Commit A3 corrected those downstream defects plus both fresh review findings. A3 exact workflow run `29757294004` passed, then evidence-only Commit B `41ecfc691ac4d4784acff549f06e3fe2f26e9c3b` passed exact workflow run `29758092140`; GM-04c opened from that remotely finalized baseline.

## Review boundary

The multi-CLI runbook applies because this unit changes supply-chain and workflow behavior. The fleet-pinned external workflow remains unavailable at the established repository-export authorization boundary and will not be bypassed; no external review or approval will be claimed. Independent in-process reviewers must read the live plan/code, attempt to refute the safety contract, and have every substantive finding adjudicated before delivery.

## Acceptance

- Missing-pin setup is descriptor-driven, transactional, cross-platform, credential-minimized, CI-exercised, and never mutates the sibling.
- Exact reruns are stable and unnecessary phases are skipped; every suspicious pre-existing state fails before mutation with attributable expected-versus-actual diagnostics. Exclusive-create races preserve an intervening slot winner, while a path-level parent-container swap is detected after creation and retains all resulting state for inspection rather than deleting through an untrusted path.
- Under the explicit trusted bootstrap, every canonical Node test/playtest surface fails before its guarded body on physical pin identity mismatch, `npm test` has a fixed zero-argument full-suite child body, the standard npm invocation suppresses its expanded command prelude, and recursive dirty canaries retain their documented attributed path; startup-taint detection is fail-closed for later effects but is not represented as prevention or attestation of pre-main execution.
- Focused/full local gates and independent implementation review are clean, Commit A and evidence-only Commit B are pushed to `main`, and both exact workflows succeed before GM-04c.

## Remote finalization

Evidence-only Commit B `41ecfc691ac4d4784acff549f06e3fe2f26e9c3b` passed exact workflow run `29758092140`; `build` job `88405558876` and `rl-smoke` job `88405560427` both succeeded. This satisfies the GM-04b acceptance and durability boundary without making any claim about GM-04c Commit A.
