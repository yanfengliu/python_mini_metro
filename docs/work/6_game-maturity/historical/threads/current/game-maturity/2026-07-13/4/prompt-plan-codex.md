# GM-03c Codex plan-review prompt

You are a senior code reviewer. Flag bugs, security issues, and performance concerns. Do NOT modify files or propose patches. Only return findings, explanations, and suggestions in plain text. Be concise but effective: keep the reasoning, impact, and file/line evidence needed to act without preserving transcripts, command chatter, or repetitive detail. Only point out an issue if it is real and important. If there is no issue, say so instead of nit-picking.

Review `docs/threads/current/game-maturity/2026-07-13/4/PLAN.md` as a high-risk behavior-neutral architecture plan from baseline `00ea38c`; the verified routing/coupling slice passes 80/80. Verify every claim against the live codebase: grep the symbols, signatures, direct writes, RNG calls, graph/BFS ordering, object identities, checkpoint consumers, route invalidation, boarding lookahead, and test paths; do not approve from prompt text alone. Prior live-code findings: search-only extraction misses the size gate; live-list arrival mutation, unreachable retry/read-only lookahead, and empty-sentinel reuse have surprising timing/RNG/identity; direct callback capture breaks rebinding; constrained plans are wired while unowned but bulk plans must be installed before the public hook. Intent: extract stateless route selection plus lazy boarding and bulk proposals while keeping RNG, fresh graphs, live collection mutation, passenger effects, and freshly resolved public dispatch on `Mediator`; the 178-line source regions have a 95-line target/96-line hard replacement budget, reducing Mediator from 1,193 to a 1,110 target and 1,111 ceiling. Flag design, correctness, TDD, typing, efficiency, file-size, process, stale-doc, or validation defects, with severity/evidence/correction; return `APPROVED` only if no substantive issue remains.

Begin your review with the literal token "===BEGIN-REVIEW===" on its own line and end with "===END-REVIEW===" on its own line. Do not emit those markers anywhere else in your output.
