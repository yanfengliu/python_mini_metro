===BEGIN-REVIEW===
VERDICT: BLOCK

1. [BLOCKER] Offer-stream persistence cannot be deferred. Saving is blocked only while a boundary is pending (`src/save_game.py:65-84`); OFFER quit resolves and immediately autosaves (`src/main.py:331-336`). Saves contain only Python/NumPy RNG state (`src/save_game.py:289-292`), while load constructs `Mediator(seed=0)` and restores only those streams (`src/save_load.py:57-65,346-348`). Continue then re-enables the calendar (`src/main.py:239-244`), so future offers reset/diverge from uninterrupted play. This violates the exact-Continue contract (`README.md:66,230`) and the live GM-10b scope (`docs/threads/current/game-maturity/2026-07-11/1/STATE.md:25`). Persist `rng.offer` now through a new save-schema version, with deterministic legacy initialization; `current_offers` and true mid-offer persistence may remain GM-10h. Alternatively, redesign generation as stateless from already-persisted inputs. Add resolve→save/load→next-offer equivalence coverage.

2. [MAJOR] The rollback/purity claim is incomplete. Exact transaction snapshots capture, compare, and restore only Python/NumPy RNGs (`src/carriage_transaction_snapshot.py:53-54,281-282,314-315`; `src/path_replacement_snapshot.py:108-109,196-197`; `src/path_removal_snapshot.py:150-172,200-203`; `src/passenger_capacity.py:87-108,134-137`). An effectful factory/replanner that consumes `offer_random` and then fails would return with future offers shifted. Extend every full-state rollback/query snapshot to the third stream and mutation-test a failing callback that consumes it. Also remove the inaccurate rationale that offers never occur mid-gesture: OFFER promotion explicitly cancels a still-active gesture after the boundary (`src/app_controller.py:184-206`).

3. [MAJOR] The proposed calendar-ON versus calendar-OFF “separate-stream inertness” test does not isolate offer RNG. A week hold resets `FixedStepClock` during/after application (`src/game_session.py:52-60,73-91`; `src/game_clock.py:43-47`), and D-041 already records that pause trajectory-invariance was disproved (`DECISIONS.md:259` in the existing thread file). Compare two runs with identical calendar/hold/resolve behavior, varying only offer generation, and directly assert unchanged Python/NumPy states plus gameplay projection. Also add offer-RNG continuity across save/load: current checkpoint comparisons omit that stream and will false-green. The v1 fixture is intentionally upgraded to v2 rather than reserializing identically (`test/test_gm07b_save_determinism.py:27-31`).

4. [MAJOR] The existing real-loop test will break and currently cannot verify cached offers. `_LoopMediator` lacks `current_offers` (`test/test_gm10a_calendar.py:383-400`), and the draw spy accepts only `(surface, week_index)` (`test/test_gm10a_calendar.py:439-442`). Update both and assert the exact ordered tuple remains identical across repeated OFFER frames. The proposed generic “non-blank” render assertion is insufficient; it can pass when both labels are omitted. Compare offer-bearing rendering against a no-offer baseline and verify each label region plus repeated-draw byte stability.

5. [MAJOR] The plan silently conflicts with current project canon and omits required documentation reconciliation. `STATE.md:25` and `EVIDENCE.md:1075` assign persisted RNG/checkpoint/save/observation work to GM-10b, while `ARCHITECTURE.md:381` also points offer/observation work at GM-10b; the new plan moves most of it without amending those records. Technically, gated-off recursive checkpoint v4 and RL observation can remain unchanged, but that decision must update the canon rather than drift it. Update `README.md`, `GAME_RULES.md`, `ARCHITECTURE.md`, `PROGRESS.md`, the existing nested `DECISIONS.md`, and `SimulationContext`’s now-stale two-stream description (`src/simulation_context.py:10-11`).

Confirmed claims:

- `spawn(3)` preserves children 0 and 1. Live construction uses child 0 for `python_random` and child 1 for `numpy_random` (`src/simulation_context.py:13-20`); a live probe reproduced the existing GM-09a construction and trajectory fingerprints (`test/test_gm09a_maps.py:42-44,66-115`).
- Save/checkpoint serializers are explicit, so no new attribute is accidentally serialized (`src/save_game.py:289-292`; `src/recursive_checkpoint.py:485-488`). `_normalize_observation` projects fixed existing fields (`src/recursive_checkpoint.py:47-157`).
- Gating and ordering are sound if generation occurs only after all calendar/game-over/crossing guards: `_maybe_hold_week_boundary` runs last after both settlement passes (`src/mediator.py:748-777`). RL, pixel RL, tutorial, and frame-limited runs leave the calendar off.
- The proposed data model is reasonable with an explicitly ordered pool: `random.sample` gives distinct deterministic choices for the same Python 3.13 RNG state. Excluding `TUNNEL` exactly when `num_tunnels is None` matches live semantics; `None` means unbounded, while integer zero remains bounded (`src/mediator.py:261-269,296-302`).
- Applying effects and exposing offers to RL are not required for GM-10b. Persisting the consumed offer RNG is required.
===END-REVIEW===
