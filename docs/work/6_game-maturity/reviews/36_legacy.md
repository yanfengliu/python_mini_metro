# GM-09e implementation — dual adversarial review synthesis

GM-09e is a PURE additive `MapDefinition` (LAKE) with no new machinery, so the review surface is: the PARTIAL-band crossing geometry (the new dimension — a bounded rect, not a full-height channel), the 4-strip spawn frame's coverage, the optional-crossing/budget semantics, determinism, and byte-identity.

- Harness (`raw/impl-harness.md`, **SHIP**): verified the full partial-band crossing matrix (enter+exit=1, corner=0, tangent=0, endpoint-on-edge=0, both-inside=1, in-out-in=2), a 6000-station spawn sweep (0 in the lake, min clearance exactly `station_size`), byte-identity (pure-insertion diff, CLASSIC fingerprint exact), save/checkpoint/render, and both test-traps avoided. No BLOCKER/MAJOR; 1 MINOR (budget test under-covers) + 2 NITs.
- Codex ultra (`raw/impl-codex.md`, **FIX-FIRST**): swept seeds, confirmed the crossing matrix + byte-identity + save/checkpoint/render, and caught 1 MAJOR + 2 MINOR + 2 test-quality MINORs the harness missed (again — the recurring pattern).

## Findings + dispositions (harness H, Codex C)
- MAJOR (C) — the code's `never gates connectivity` claim is FALSE: a line bends only at STATIONS, so at an exhausted budget a station whose only routes cross the lake IS gated until a tunnel is freed (`seed 74`: 3 through-lines, then an unlock at (1261,687) whose every edge crosses the lake). The CODE is correct (the gate enforces the budget uniformly, exactly as on the rivers) — the DEFECT was my mischaracterization. **RE-DISPOSITIONED + CORRECTED** across the `maps.py` comment, `GAME_RULES.md`, `D-037`, `ARCHITECTURE.md`, and the test: the lake makes crossing MORE OFTEN avoidable than a full-screen channel, not always; the budget still limits total crossings and can gate connectivity. A regression test (`test_budget_limits_crossings_and_a_non_crossing_line_stays_allowed`) pins the true behavior (4th crossing rejected; a non-crossing line still allowed at zero).
- MINOR (C, also H NIT) — a line collinear with a lake EDGE was charged a crossing though it lies on dry land; REACHABLE on LAKE (integer vertical edges, no x-erosion of the top/bottom banks), unlike RIVER/DELTA. **FOLDED**: `segment_crosses_band` promoted to STRICT-interior semantics (the in-band overlap's MIDPOINT must be strictly inside), superseding GM-09c's "unreachable" deferral. Verified: LAKE edge lines now count 0, genuine crossings unchanged, RIVER/DELTA counts unmoved (they never place a centerline on an edge), full suite green. Regression: `test_a_line_running_along_a_lake_edge_is_not_a_crossing`.
- MINOR (C) — a Rect glyph at `(777,337)` overwrites ~40 water pixels on one row (its edge reaches the water). This is the SAME pre-existing glyph-edge-touches-water property SHARED with the delivered RIVER/DELTA (erosion clears the CENTER by `station_size`; a 60px glyph reaches the water edge). **RE-DISPOSITIONED** as pre-existing/shared: the "never in or touching the water" claim was corrected to "centres inset from the water by at least a station's size" (`GAME_RULES.md`, test comment); a raster-margin fix can't change RIVER/DELTA's frozen erosion, so it stays with the pre-existing `task_384488d0`-class follow-ups (documented, not folded here).
- MINOR (H, C test-quality) — budget test built only one line. **FOLDED** (above).
- MINOR (C test-quality) — spawn test combined the side strips as `S` and only required top/bottom, so a duplicate strip would pass. **FOLDED**: `test_stations_spawn_in_all_four_frame_strips_and_never_in_the_lake` asserts all four strip-specific regions independently.
- NIT (H) — the crossings docstring's "unreachable" claim. **SUPERSEDED** by the strict-interior rewrite.
- NIT (C) — determinism comment overstated "catches a spurious extra draw". **FOLDED**: comment corrected to what it actually guards (reproducible + seed-sensitive across the trajectory; the frozen CLASSIC fingerprints guard absolute values).

## Result
No BLOCKER; Codex's MAJOR was a framing over-claim (code correct) corrected everywhere. The one substantive code fold — strict-interior geometry — was requested by both lanes, verified empirically (edge=0, genuine crossings + RIVER/DELTA counts unchanged) and by the full suite (1428 OK), and is safe because a genuine crossing's overlap midpoint is always strictly interior. All test-hardening folded (14 tests). CLASSIC/RIVER/DELTA byte-identical.
