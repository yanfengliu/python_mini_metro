# RL framework implementation plan

1. Make Gymnasium a fail-closed core contract: import it unconditionally in the pixel-environment tests, then add red coverage for spaces/dtypes/profiles, deterministic reset, exact coordinate/event routing and pointer state, three-step line creation through player input, exact fixed-tick cadence, delivery reward, true game-over termination and terminal-over-horizon precedence, canonical HWC render, exact cursor/pressed-marker pixels, protocol fingerprints, interleaved RNG isolation, fidelity rendering, and future visible content.
2. Inject a per-mediator `SimulationContext`, record it in recursive checkpoints, and add an immutable protocol descriptor plus `PlayerPixelEnv` over `GameSession` and `GameRenderer`; preserve the structured `MiniMetroEnv` API and source protocol defaults from one module.
3. Add core Gymnasium and optional Stable-Baselines3 dependency declarations, regenerate universal hash-locked core and RL files, install the RL lock into `py313` when writable or an ignored environment built from that interpreter when sandbox-managed, validate through the direct `py313` interpreter against those exact locked packages, run `pip check`, and audit both locks with zero unresolved vulnerabilities as required by the dependency protocol.
4. Add a deterministic low-level demonstrator/curriculum fixture, adaptive-pooling CNN, reusable vector-environment/model-manifest helpers, and PPO training/evaluation CLIs. Default to spawned subprocess vectorization and four-frame stacking; retain dummy/synchronous modes for debugging.
5. Close artifact and provenance gaps before accepting saved models: read, hash, parse, and validate one exact manifest and authenticated-index snapshot; load the exact model bytes that passed that index verification; reject evaluation outputs that would overwrite any authenticated run artifact; include every gameplay-affecting direct dependency in runtime compatibility; make requested train/evaluation seeds authoritative after SB3 load; derive the default standalone evaluation seed from the manifest; recapture content/trainer/runtime state after evaluation; and test those semantics.
6. Run Gymnasium/SB3 `check_env`, interleaved environment determinism, a scripted positive-delivery trajectory, DummyVecEnv and spawned SubprocVecEnv smoke checks, throughput/memory probes, then a real short PPO learn/checkpoint/save/load/evaluate cycle followed by an authenticated resumed run and evaluation. Replay the recorded evaluation seed suite at every training checkpoint, and prove parent digests/tags, requested versus terminal seeds, same-byte manifest/index/model verification, protected output paths, and tampered-model rejection.
7. Update README, architecture, game rules, progress, CI, and recursive provenance scope; keep root-level import instructions executable, include every RL test in focused CI, and make the Windows smoke exercise fresh training, resume, and authenticated evaluation.
8. Run the full Python suite with no dependency-gated RL skips, changed-file and full-repo Ruff/format, pre-commit, `git diff --check`, both dependency audits, `npm audit`, and the full Node suite in a temporary sibling layout at the repository's pinned `civ-engine` commit. After the prospective commit, rerun a clean default `npm run playtest:recursive` in that pinned layout.
9. Run in-process adversarial finder/verifier review plus the required Codex/Claude diff review when available. Fix every verified finding, rerun affected and full gates, write `diff.md` and `REVIEW.md`, move the finished thread under `docs/threads/done/`, commit the coherent rendering/RL increment directly to `main` with `pip-audit: 0 CVEs` recorded in the commit message, push, and verify both pushed CI jobs.

External plan review was attempted against the live tree. Codex CLI 0.144.1 reached the pinned model but lacked API authentication, Claude Fable was quota-limited, and the required `opus[1m]` fallback timed out after ten minutes without output. The raw failure evidence is retained under `raw/`. Three independent in-process reviewers instead inspected the player contract, artifact/training stack, and release gates; this revision incorporates their substantive findings before implementation continues.
