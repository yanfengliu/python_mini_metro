You are the final independent Codex reviewer for a high-risk reinforcement-learning architecture and dependency change in python_mini_metro. Do not modify any file. Read AGENTS.md, ARCHITECTURE.md, README.md, GAME_RULES.md, docs/rl-model-selection.md, docs/threads/current/rl-framework/2026-07-11/1/diff.md, the complete live git diff, every changed test/CI/dependency file, and the new untracked files src/rl/dependencies.py and src/rl/policy.py.

Verify each claim in the plan/diff against the live codebase — grep for the symbols, function signatures, column names, and file paths it references; do not approve based on prompt text alone.

The target is to maximize the undiscounted total passengers delivered before true game over. Fresh training should use eight-frame MiniMetroCNN plus separate 256-unit actor/critic LSTMs through SB3-Contrib RecurrentPPO, with episode memory preserved within but never across games; external horizons must bootstrap and evaluation must label partial delivery totals as right-censored. Legacy feed-forward PPO artifacts and helper APIs must remain fail-closed but usable with explicit drift overrides. The new recurrent batch 64 must not silently alter legacy PPO's established configuration.

Adversarially inspect correctness, recurrent state/reset semantics, truncation bootstrapping, evaluation aggregation, algorithm dispatch, resume inheritance, exact-byte artifact authentication, runtime/training fingerprints, compatibility re-exports, dependency lock/provenance, Windows spawn/CI behavior, test fidelity, file boundaries, numerical/resource risks, research accuracy, statistical protocol, stale docs, process regressions, and missing validation. Try to refute the already-fixed findings listed in diff.md. Treat a substantive defect as more important than an approval vote.

Known validation context: the exact locked RL environment passed 316 tests and real recurrent/legacy fresh-resume-evaluate lifecycles. Both hashed locks audited at zero known CVEs. Live Node tests have an unrelated sibling-engine version drift; the pinned acceptance layout passed 41/41 and no Node source changed.

Return concise findings ordered by severity with exact file/line evidence and a concrete fix. If there are no substantive findings, say APPROVED and list any residual validation limitations. Wrap only the review in ===BEGIN-REVIEW=== and ===END-REVIEW=== markers.
