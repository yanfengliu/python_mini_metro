You are a senior code reviewer checking the implementation plan for adding longer visual history and persistent recurrent memory to python_mini_metro's player-equivalent RL stack. Do not modify files or propose patches. Read AGENTS.md, ARCHITECTURE.md, README.md, GAME_RULES.md, docs/threads/current/rl-framework/2026-07-11/1/PLAN.md, the live code under src/rl/, scripts/train_rl.py, scripts/evaluate_rl.py, the RL tests, requirements files, and CI.

The requested outcome is: maximize total passengers delivered before a game ends; increase the default frame history; persist learned policy memory throughout an episode; research CNN, recurrent, Transformer/ViT, and world-model alternatives; preserve authenticated artifact/provenance behavior and old PPO evaluation; keep the implementation practical on the current fast pixel profile and eight-worker training setup.

Focus on whether eight stacked frames plus a 256-unit CNN-LSTM RecurrentPPO is the safest useful integration, whether recurrent state is propagated/reset correctly in training and evaluation, whether `gamma=1.0` and `gae_lambda=0.99` actually match the reward/horizon/truncation semantics, whether old PPO/four-frame artifacts and resume defaults remain fail-closed, whether the `sb3-contrib` dependency/lock/provenance protocol is complete, and whether the tests and validation can refute hidden-state leakage, memoryless evaluation, artifact mismatch, Windows-spawn failures, and misleading model-selection claims. Also flag process regressions, stale documentation, and missing validation.

Verify each claim in the plan against the live codebase - grep for the symbols, function signatures, manifest fields, dependency declarations, and file paths it references; do not approve based on prompt text alone.

Only point out real and important issues. Return APPROVED if the plan is sufficient; otherwise return concise findings with severity and file/line evidence. Begin your review with the literal token "===BEGIN-REVIEW===" on its own line and end with "===END-REVIEW===" on its own line. Do not emit those markers anywhere else in your output.
