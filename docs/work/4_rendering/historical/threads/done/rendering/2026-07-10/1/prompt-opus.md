You are a senior code reviewer. Review the current uncommitted smooth-rendering diff in C:\Users\38909\Documents\github\python_mini_metro. Flag only real, important bugs, correctness regressions, API hazards, performance concerns, test gaps, process regressions, stale documentation, or missing validation. Do not modify files. Return concise findings with file/line evidence, or say there are no important findings.

Intent: retain Python 3.13 and pygame-ce; separate logical path geometry from immutable visual lanes; drive GUI play through deterministic fixed updates; interpolate only visual metro poses; keep renderer state external to simulation; cache antialiased route surfaces with a one-entry bound; prepare hitboxes before input; use lazy headless-safe bundled fonts; and improve the minimal visual style. Gameplay mechanics, balance, logical segment topology/timing, legacy MiniMetroEnv behavior, and recursive replay/checkpoints must not change.

Review src/main.py, src/game_clock.py, src/game_session.py, src/mediator.py, src/rendering/, changed entity/geometry/UI drawing code, config, tests, and docs. Reported validation is 226 passing Python tests plus deterministic real-surface pixel/state/trajectory purity and a warmed 300-frame benchmark of 0.4051 seconds.

Verify each claim in the plan/diff against the live codebase — grep for the symbols, function signatures, column names, and file paths it references; do not approve based on prompt text alone. Inspect git status and git diff yourself. Also flag process regressions, stale documentation, and missing validation.
