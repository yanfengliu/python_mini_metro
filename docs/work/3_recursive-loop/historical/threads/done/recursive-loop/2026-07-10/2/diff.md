# Reviewed change surface

Review base: `main` at `5cb229d` before this implementation.

The reviewed surface includes the deterministic Python drive/checkpoint/oracle modules, Node verifier/orchestrator/provenance/manifest/ledger/lock modules, strict Python and Node tests, the pinned `civ-engine` package/CI contract, hashed Python dependency lock, route-determinism regressions, and the related README, architecture, game-rule, and progress documentation.

Generated `output/` evidence and the unrelated untracked `.agents/` directory are excluded from the commit and review diff.
