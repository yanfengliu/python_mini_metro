===BEGIN-REVIEW===

P2 — Assignment does not preserve an unrelated stale sibling across its late factory seam. `src/fleet_management.py:236-256` calls the injected factory but checks only the two owner collections; `:279-291` then accepts any structurally valid stale cache and rolls back only those collections. Through the live seam at `src/mediator.py:534-535`, a factory that changes the sibling cache from `('board', rider)` to `('transfer', same_live_rider)` makes assignment succeed with the sibling modified. If the factory raises, assignment fails but the modified cache still remains. This violates both commit-around and rollback-verbatim guarantees. Validate and restore the full pre-factory transaction state, with regressions for successful and failing effectful factories.

P2 — The observation/badge claims and validation are inaccurate. `PROGRESS.md:161` says observation bytes do not change and describes `queued_locomotives_for_path` as the UI badge count. In reality, relaxed guards change enabled control colors through `src/ui/fleet_button.py:63-75,98-113`, which `src/rl/player_env.py:257-275` renders directly into pixel observations; the seed-9 fast-profile frame changed bytes before any action. Successful actions also change raw metro/queue state serialized by `src/env.py:184-230` and `src/save_game.py:123-152`. Meanwhile, the visible badge independently counts raw flags at `src/ui/fleet_button.py:78-88,132-150` and never calls the changed facade. The new tests contain no stale-state public-count or pixel assertion. Narrow the claim to unchanged schemas/current frozen fixtures and add coverage for the actual public count and player-visible change.

===END-REVIEW===
