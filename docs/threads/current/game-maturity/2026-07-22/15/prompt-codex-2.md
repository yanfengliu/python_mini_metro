You re-reviewed GM-08a (typed settings + SETTINGS screen + reduced-motion gating + fullscreen) in python_mini_metro; your first pass (docs/threads/current/game-maturity/2026-07-22/13/raw/codex.md) was NOT CLEAN with 2 majors + 5 minors. They have been addressed. Adversarially VERIFY each fix against the LIVE code and check for regressions. Read the live files and `git diff`; do not trust this summary.

Fixes to verify:
1. MAJOR (validate_settings TypeError on non-string keys): src/settings.py now rejects any non-string key with ValueError BEFORE _exact_keys. Verify a directly-built dict with mixed int/None extra keys raises ValueError (not TypeError), and normal JSON docs still validate.
2. MAJOR (carriage riders blink under reduced motion): src/entity/carriage.py `draw` now declares `reduced_motion: bool = False` (keyword-only) and forwards it to `passenger.draw`, so `_call_flexibly` delivers it. Verify a carriage with a warning-age rider at a blink-OFF phase holds the rider VISIBLE under reduced_motion=True and is byte-identical under the default False; confirm the metro-vs-carriage consist path is now uniform.
3. MINOR (settings.py fd-guard): src/settings.py save_settings now tracks handle_opened and closes the raw fd in finally when os.fdopen never ran (mirrors GM-07d:C). Verify an injected os.fdopen failure closes the descriptor, preserves the prior file, leaves no .tmp litter, and propagates unmasked.
4. MINOR (volume rows not labeled stored-only): src/ui/menu_screens.py draw_settings_menu now labels the three volumes "... % (stored)". Verify.
5. MINOR (test guards): a carriage-rider propagation test and a GM-08a-owned isolation-membership test were added (test/test_gm08a_settings_render.py); a non-string-key ValueError test was added (test/test_gm08a_settings.py). Verify they are meaningful.
6. MINOR (snap-blip wording): DECISIONS.md D-029 and PLAN.md now say "warning/unlock blinks held steady; snap-blip rings suppressed". Verify.
7. MINOR (ARCHITECTURE gaps): the file tree now lists src/settings.py and src/rendering/flexible_draw.py; the state-machine bullet lists the SETTINGS state and the appended settings menu entry; the line-count note says GameRenderer is 478 (not 494); the settings module bullet says "no direct gameplay imports". Verify.

Regressions to hunt: does the settings.py non-string-key guard reject any valid document? Does carriage.draw's new param break the _call_flexibly filtering for other consist bodies (speed/carriage buttons still filtered)? Is the reduced_motion=False path still byte-identical for carriage (default)? Any file now over 500 lines (settings.py, carriage.py, menu_screens.py, game_renderer.py)? Did the volume-label change break the byte-stable draw_settings_menu test (it compares same-value stability + different-value inequality, not golden bytes — confirm)?

Do NOT modify files. Output: for each fix, CONFIRMED-RESOLVED or STILL-BROKEN with file:line + a concrete scenario; any new regression with severity; then a final VERDICT line: CLEAN or NOT CLEAN.
