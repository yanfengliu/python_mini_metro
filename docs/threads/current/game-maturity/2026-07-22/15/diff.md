# GM-08a typed settings store and SETTINGS screen diff ledger

Status: delivered as Commit A `fe525ef` (rebased onto `origin/main` at `131e0da`, merging GM-07e's `reconcile_game_over` into the SETTINGS wiring with an additive ARCHITECTURE/PROGRESS merge, full suite re-verified 1277/0 on the integrated tree), exact [run 29975144529](https://github.com/yanfengliu/python_mini_metro/actions/runs/29975144529) green; evidence-only Commit B active. Thread iteration renumbered 13->15 at delivery for the origin collision.

## Implemented production surface

- `src/settings.py` (new, 187): the typed frozen `Settings` (`fullscreen`, integer-percent `master`/`music`/`sfx` volumes, `reduced_motion`) with `DEFAULT_SETTINGS`; strict `validate_settings` (non-string keys rejected first, then exact keys before field access, forward versions rejected, strict `_bool`, `_percent_int` volumes, duplicate-key rejection); `settings_to_document`/`settings_from_document`; `load_settings` FAIL-SAFE-to-defaults (catches `Exception`, never raises); `save_settings` (validate-before-write, save-local atomic writer with the GM-07d:C fd-ownership guard, RAISES on failure). Reuses `save_schema.canonical_save_bytes` + `save_schema_records` validators, joining the persistence isolation set.
- `src/rendering/flexible_draw.py` (new, 42): the kwarg-filtering `_call_flexibly`/`_supported_kwargs`/`_supported_keyword_names` dispatch extracted verbatim from `game_renderer`, keeping it at 478 lines.
- `src/app_controller.py`: `AppScreen.SETTINGS`; an optional inert `settings` seam (`load`/`save`); public `current_settings` (loaded at construction or `DEFAULT_SETTINGS`); `_settings_origin`; `_open_settings`/`_handle_settings`/`_edited_settings`; and the title/pause dispatch branches. Entering settings from pause keeps the `menu` hold; Back returns to the origin; each control edits `current_settings` and persists once (seam-less edits update memory only). The controller never touches the `Mediator`.
- `src/main.py`: `SETTINGS_PATH`, `read_settings`/`write_settings`, the settings seam, the fullscreen consumer (`applied_fullscreen` gate + `set_mode(FULLSCREEN|SCALED)` + `window_surface` reassignment), `reduced_motion` threaded into `renderer.draw`, the `SETTINGS` render branch, and `SETTINGS` frozen via `advance(0)`.
- `src/rendering/game_renderer.py`: `draw(..., reduced_motion=False)` stashed as `self._reduced_motion` and delivered to the entity draws through the `_call_flexibly` kwarg boundary; `src/entity/{passenger,holder,station,metro,carriage}.py` and `src/ui/{menu_screens,path_button}.py` each gate their blink/blip on `reduced_motion` (default `False` byte-identical): warning/unlock blinks held visible, snap blips suppressed.
- `src/ui/menu_screens.py`: `draw_settings_menu` (byte-stable, volume rows labeled `(stored)`), a `settings` entry appended after the existing title and pause layout entries (prior rects unchanged), and the wider settings layout.
- No schema, observation, action, checkpoint, protocol, or frozen-artifact change; settings touch no `Mediator`/`config` balance.

## Implemented evidence surface

- Reconcile GM-07d's remote finalization (Commit B `60f59c0` and the run recorded in EVIDENCE) and the concurrently-landed follow-ups on `origin/main`: GM-07d:C/D (`2873acb`/`28d86e5`, the atomic-writer fd-guard whose pattern `settings.py` shares), GM-07e:A/B (`a720cf7`/`35d7ee8`, deterministic per-frame `reconcile_game_over`, merged into the SETTINGS wiring), and GM-07b:E (`3757a2c`/`131e0da`). Record D-029 (the full three-category settings store).
- Research/plan reused the highscores template; combined adversarial plan review (2 majors + minors) folded; a 4-module red baseline plus four product-driven existing-test edits; a harness implementation-review lane (CLEAN, gold-standard byte-identity + windowed run) that missed the carriage path; an escalated external Codex persistence lane (NOT CLEAN, 2 majors + 5 minors) that caught it; and a Codex re-review confirming the fixes — all preserved under `raw/`.
- Update `README.md`, `GAME_RULES.md`, `ARCHITECTURE.md` (tree, state machine, line count, direct-imports), and `PROGRESS.md`.

Local gates: the four GM-08a modules plus the product-driven edits green; full py313 suite 1260/0 with 12 expected skips; guarded `npm test` 249/0 (one transient flake, clean on re-run); Ruff and per-file pre-commit clean; budgets held. Commit A rebases onto `origin/main`, re-verifies, then pushes; evidence-only Commit B follows the exact remote workflow.
