You are an adversarial code reviewer for a Python 3.13 pygame-ce Mini Metro implementation. Review the UNCOMMITTED working-tree change GM-08a (a typed settings store + a SETTINGS screen + reduced-motion render gating + fullscreen) against the LIVE code. REFUTE its correctness; green tests are not proof. Read the live files and `git diff`.

Binding contract: docs/threads/current/game-maturity/2026-07-22/13/PLAN.md and D-029 in docs/threads/current/game-maturity/2026-07-11/1/DECISIONS.md. The plan review it folded is docs/threads/current/game-maturity/2026-07-22/13/raw/plan-review.md.

The change (read the live files):
- src/settings.py (NEW): typed frozen Settings, strict validate_settings, settings_to_document/settings_from_document, load_settings (FAIL-SAFE to DEFAULT_SETTINGS, never raises), save_settings (validate-before-write, save-local atomic writer, RAISES). Reuses save_schema.canonical_save_bytes + save_schema_records validators.
- src/rendering/flexible_draw.py (NEW): the _call_flexibly kwarg-filtering dispatch EXTRACTED from game_renderer to keep it under 500 lines.
- src/app_controller.py: AppScreen.SETTINGS, optional `settings` seam, current_settings, _settings_origin, _open_settings/_handle_settings/_edited_settings, and the title/pause dispatch branches. Imports DEFAULT_SETTINGS from settings.
- src/main.py: SETTINGS_PATH, read_settings/write_settings, the settings seam, the fullscreen consumer (applied_fullscreen gate + set_mode(FULLSCREEN|SCALED) + window_surface reassignment), reduced_motion threaded into renderer.draw, the SETTINGS render branch, and SETTINGS frozen via advance(0).
- src/rendering/game_renderer.py: draw() gained reduced_motion=False, stashed as self._reduced_motion and passed to entity draws via _call_flexibly.
- src/entity/{passenger,holder,station,metro}.py and src/ui/path_button.py: each draw gained reduced_motion=False and gates its blink/blip.
- Docs: README.md, GAME_RULES.md, ARCHITECTURE.md, PROGRESS.md.
- Tests: test/test_gm08a_*.py (4 new), plus edits to test/test_station.py, test/test_main.py, test/test_gm07a_run_game_loop.py, test/test_gm07b_save_determinism.py.

Attack these, each with file:line and a concrete failing scenario:
1. PERSISTENCE correctness (settings.py): does validate_settings reject every malformed input BEFORE field access (no KeyError/TypeError escaping as non-ValueError)? Bool-as-int volume? float/negative/over-100 volume? int-as-bool fullscreen/reducedMotion? forward schemaVersion? duplicate keys? Does load_settings REALLY never raise (missing/truncated/non-JSON/BOM/non-ASCII/dup-key/forward-version/200k-deep RecursionError) and always return the exact DEFAULT_SETTINGS? Does save_settings validate before writing so a hand-built out-of-range Settings can never overwrite a valid file? Atomic writer: interrupted os.replace preserves the prior file, no .tmp litter, canonical ASCII + single trailing LF? Is the mkstemp fd-guard nit present (same as the highscores/save_game follow-up)?
2. REDUCED-MOTION BYTE-IDENTITY (the highest risk): is EVERY default `reduced_motion=False` path byte-for-byte identical to pre-GM-08a output? Check each gate in passenger.py/station.py/path_button.py/holder.py/metro.py — a False default must not change the historical skip/draw logic at all. Does the game_renderer stash `self._reduced_motion` get set before every _draw_metro/_draw_buttons call (any path where they run without draw() setting it first)? Is the snap-blip SUPPRESSED (not held) and the unlock/warning blinks HELD VISIBLE (not suppressed) — verify the semantics match D-029 and are not inverted. Does _call_flexibly correctly filter reduced_motion to only the draws that declare it (carriage/speed-button draws must NOT get it)?
3. flexible_draw EXTRACTION: is _call_flexibly behavior identical after extraction? Any caching (lru_cache) or import-cycle concern? Does game_renderer still import and use it correctly? Any other module that imported these helpers from game_renderer and now breaks?
4. FULLSCREEN (main.py): is the startup set_mode still exactly ((sw,sh), RESIZABLE)? Does the fullscreen consumer reassign window_surface, gate on change (not every frame), and use FULLSCREEN|SCALED? Any interaction with get_window_size/viewport/the letterbox path? Does a persisted fullscreen=True apply on frame 1 without breaking the loop?
5. SETTINGS SCREEN + SEAM (app_controller.py): does entering settings from the pause menu keep the `menu` pause hold (not release it)? Does Back return to the correct origin? Does each control edit current_settings AND call seam.save exactly once? Does a seam-less controller update current_settings in memory but never persist (and never crash)? Does app_controller importing `settings` (which imports save_schema) create any isolation problem — is app_controller ever imported by a headless/RL surface? Does SETTINGS freeze the sim (advance(0)) so a title→settings→back doesn't tick a fresh game?
6. ISOLATION: env.py/agent_play.py/recursive_playtest.py/recursive_checkpoint.py/rl/* import no settings module; the AST scan (test_gm07b_save_determinism.py SAVE_MODULE_NAMES) includes "settings". No Mediator/config/checkpoint/observation/save-schema/frozen-artifact change. Every touched renderer/entity file < 500 lines and game_renderer < 500.
7. DOC honesty: README/GAME_RULES/ARCHITECTURE/PROGRESS match the implemented behavior (integer-percent volumes stored-not-yet-audible, reduced-motion holds blinks steady + suppresses snap blips, fullscreen live, fail-safe-to-defaults, isolation).

Do NOT modify files. Output structured findings: SEVERITY (blocker/major/minor), file:line, concrete scenario, suggested fix; list what's sound; final line VERDICT: CLEAN or NOT CLEAN. Terse and concrete.
