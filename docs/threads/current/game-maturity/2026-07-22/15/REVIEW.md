# GM-08a review synthesis

Status: delivered as Commit A `fe525ef` (rebased onto `origin/main` at `131e0da`, merging GM-07e's `reconcile_game_over` into the SETTINGS wiring in `app_controller`/`main` with an additive ARCHITECTURE/PROGRESS merge and GM-07e's `_LoopRenderer` fake updated for the new `reduced_motion` kwarg, full suite re-verified 1277/0 on the integrated tree), exact [run 29975144529](https://github.com/yanfengliu/python_mini_metro/actions/runs/29975144529) green; evidence-only Commit B active. The thread iteration was renumbered 13->15 at delivery because a concurrent worker claimed 13/14 on `origin/main`.

Scope was decided by the owner (D-029): the full three-category settings store (display/audio/accessibility) plus the `SETTINGS` screen, over a real-consumers-only subset. The plan (`PLAN.md`) reused the highscores persistence template and the optional-seam pattern; the combined adversarial plan review (`raw/plan-review.md`, NOT CLEAN, 2 majors + minors) corrected the reduced-motion architecture (the blink phase lives in the entity leaves, not `game_renderer` — so a `motion.py` extraction would extract nothing; carry `reduced_motion` to the leaves through the `_call_flexibly` kwarg boundary with default-`False` byte-identity, holding blinks steady and suppressing snap blips), pinned the fullscreen `window_surface` reassignment + `applied_fullscreen` gate + `FULLSCREEN|SCALED` with the required `test_main.py` set_mode-once insulation, dropped a false "menu byte tests break" premise (the menu tests are property-based, so appending a `settings` entry is safe), and resolved the seam-less `current_settings` semantics. All folded before red tests.

Implementation added `src/settings.py` (typed `Settings`, strict validation, fail-safe-to-defaults load, validate-before-write atomic save), extracted the `_call_flexibly` dispatch to `src/rendering/flexible_draw.py` (keeping `game_renderer` at 478 lines), threaded `reduced_motion` through the entity leaves, wired the controller `SETTINGS` screen + seam, and wired `main`'s fullscreen and reduced-motion consumers. Red evidence (`red-evidence.md`): four new `test_gm08a_*` modules plus the four product-driven existing-test edits, zero collateral.

Two independent implementation-review lanes ran. The harness lane (`raw/implementation-review.md`, CLEAN) built a clean pre-GM-08a worktree and proved byte-identity of every default-`False` path against the real old code, drove a real windowed `run_game`, and verified fullscreen/isolation/persistence — but its fixture had no carriage rider and its fuzz used only string keys, so it did not reach two real defects. The escalated external Codex persistence lane (`raw/codex.md`, NOT CLEAN, 2 majors + 5 minors), verified against the live code, caught them: (MAJOR) `carriage.draw` did not declare `reduced_motion`, so `_call_flexibly` filtered it and carriage riders still blinked under reduced motion — fixed by declaring and forwarding it, exactly as `metro.draw` does, with a regression test; (MAJOR) `validate_settings` could leak a `TypeError` from the shared `_exact_keys` `sorted()` on a directly-built dict with mixed non-string keys — fixed by rejecting non-string keys with `ValueError` first, with a test. Five minors were folded: the `save_settings` fd-ownership guard (mirroring the concurrently-landed GM-07d:C twin fix), stored-only volume labels, a GM-08a-owned isolation-membership test, the snap-blip "held steady"->"suppressed" wording in D-029/PLAN, and the ARCHITECTURE tree/state-machine/line-count/direct-imports gaps. This is the second unit where the harness lane returned CLEAN while the persistence lane found real bugs its scenarios did not reach (`[[gm07d-review-coverage-lesson]]`).

A focused Codex re-review (`raw/codex-2.md`) confirmed all production fixes resolved (non-string keys, carriage, fd-guard, labels, wording, ARCHITECTURE) with no production regression and byte-identical default carriage pixels, holding NOT CLEAN only on test-coverage quality: the carriage test exercises the `_call_flexibly` dispatch boundary rather than a full `GameRenderer.draw` consist, an on-phase default comparison, a missing PathButton case, and two hygiene nits (an unclosed `open` and no `os.fdopen`-failure regression). Folded: an off-phase default byte-identity comparison, a PathButton reduced-motion test, a `with`-guarded read, and an `os.fdopen`-failure fd-guard fault-injection test. The one residual — a committed full-consist `GameRenderer.draw` carriage-integration test — is deferred as a follow-up: the carriage fix is confirmed correct, its mechanism is pinned at the exact `_call_flexibly` renderer dispatch, and the harness gold-standard drove a real full `GameRenderer.draw`; the `game_renderer._draw_metro` propagation is a single stable kwarg.

Final local evidence: the four GM-08a modules plus the product-driven edits are green; full py313 suite 1260 tests, 0 failures, 12 expected skips; guarded `npm test` 249/0 (one transient setup-contract flake on a first run, clean on re-run); Ruff, format, and per-file pre-commit clean; budgets held (`settings.py` 187, `flexible_draw.py` 42, `app_controller.py` 309, `main.py` 326, `menu_screens.py` 252, `carriage.py` 123, and every touched renderer/entity file under 500, `game_renderer.py` 478). No schema, observation, checkpoint, or frozen-artifact change; settings touch no `Mediator`/`config` balance. Both concurrently-landed GM-07d follow-up chips are reconciled: GM-07d:C/D (the fd-guard, whose pattern `settings.py` now shares) and GM-07e (deterministic per-frame `reconcile_game_over`, merged into the SETTINGS wiring at rebase); GM-07b:E is preserved.
