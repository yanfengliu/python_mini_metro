# GM-08a Implementation Review (harness lane) — VERDICT: CLEAN

Reviewed at thread iteration 13 (renumbered to 15 at delivery for the origin collision), before the Codex fold. Preserved as the record of what this lane did and did not cover: it returned CLEAN with a gold-standard byte-identity comparison, but its fixture did not include a carriage rider, so it did not reach the carriage-passenger path the external Codex lane (`raw/codex.md`) surfaced as a MAJOR.

## Method
Refused to trust "green". Built a **clean git worktree at HEAD (60f59c0, the pre-GM-08a baseline)** and rendered identical scenarios in both the old and new code, comparing `pygame.image.tobytes`. Drove a **real `main.run_game` under SDL dummy with real `set_mode`**, captured the rendered settings chrome, and verified persistence to a temp HOME. Full suite from the repo root: **1255 tests, 0 fail, 12 skip** (the first "54 errors" run was a wrong-cwd artifact shadowing the stdlib `test` package).

## Findings by attack point — all CONFIRMED SOUND at this lane's coverage
1. settings.py CORE fuzz (`p1_settings_fuzz.py` 98/98, `p1b_deep_and_interrupt.py`): every structural break raises `ValueError`, no `KeyError`/`TypeError` escaping; `load_settings` returns EXACTLY `DEFAULT_SETTINGS` and never raises for missing/truncated/non-JSON/empty/BOM/non-ASCII/dup-key/forward-version and a 200k-deep RecursionError (subprocess); save validates before writing, canonical ASCII + single trailing LF, mocked `os.replace` failure re-raises and preserves the prior file, no `.tmp` litter; save->load survives restart. [Did not exercise a directly-built dict with non-string keys — the Codex-flagged TypeError path.]
2. Reduced-motion byte-identity (gold standard, `render_scenario.py`+`compare_render.py`+`p2_fullframe_plumbing.py`): pre-GM-08a worktree code and current code produce byte-identical output for passenger-warning-off, station-unlock-off, active-snap-blip, locked-path-button-off, and a full `GameRenderer.draw` of the real fixture Mediator; `nokwarg == reduced_motion=False`. Under True the three blinks held VISIBLE and the snap blip SUPPRESSED (3492->2788 px). Instrumentation confirmed True reaches the station/passenger/path_button leaves. [The fixture had no carriage rider, so the carriage consist body was not exercised.]
3. flexible_draw extraction: helpers moved verbatim; `game_renderer.py` 478 lines (<500); Station/Passenger/Metro/PathButton declare `reduced_motion`, Carriage/SpeedButton/Button/CarriageButton do NOT and `_call_flexibly` strips it (no crash). [Correctly observed Carriage.draw does not declare reduced_motion — but read this as intended filtering, not the defect Codex identified.]
4. Fullscreen (`p4_fullscreen.py`): default False -> exactly 1 set_mode(RESIZABLE); persisted True -> exactly 2 (startup RESIZABLE + one FULLSCREEN|SCALED) with no repeat over 5 frames and window_surface reassigned; reduced_motion threaded into renderer.draw. test_main set-mode-once insulated via patch("main.read_settings").
5. SETTINGS screen + seam (`p5_controller.py`, `p5b_freeze_and_geometry.py`): title->settings->Back=TITLE; pause->settings keeps the menu hold (zero release across the round trip), Back=PAUSE_MENU; each control edits current_settings and persists exactly once; volumes cycle 25% wrapping 100->0; seam-less edits update memory but never call a seam and never crash; SETTINGS freezes the sim (advance(0)); appended entries leave prior title/pause rects byte-identical.
6. Real windowed run (`p6_windowed_run.py`, screenshot `p6_settings_screen.png`): title->settings->toggle flow wrote settings.json to a temp path (not ./saves) with the exact 7-key canonical shape and correct values; a fresh load and controller reload them; ./saves never created.
7. Isolation + suite FS safety (`p7_isolation.py`): 25 isolation targets import no persistence module; SAVE_MODULE_NAMES includes "settings"; no headless surface imports app_controller/main; canonical_checkpoint byte-identical across building a settings controller; the full 1255-test suite created NO ./saves/settings.json. No checkpoint/observation/save-schema/fixture files changed.
8. Doc honesty: README/GAME_RULES/ARCHITECTURE/PROGRESS accurate at review time.

## Verdict
Found no blockers/majors/minors at this lane's coverage. **CLEAN** — but superseded by the external Codex persistence lane (`raw/codex.md`), which reached the carriage-rider path and the non-string-key validate path this lane's fixtures/fuzz did not.
