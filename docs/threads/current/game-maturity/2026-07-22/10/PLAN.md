# GM-07b versioned save-snapshot plan

Status: revised after two independent adversarial plan reviews (`raw/plan-review-loader.md`: NOT CLEAN, one blocker + four majors; `raw/plan-review-schema.md`: NOT CLEAN, one major + three mediums — the missing-scalar gap found independently by both lanes); all findings folded below; narrow recheck complete (`raw/plan-recheck.md`: all 21 findings verified closed; its two fold-introduced gaps and one hardening note are dispositioned in this text) — red tests may begin

Transaction marker: `[GM-07b:A]`

## Baseline and reconciliation

GM-07b opens from `main == origin/main == 272ba2d`, GM-07a's evidence-only Commit B, whose exact [run 29907985159](https://github.com/yanfengliu/python_mini_metro/actions/runs/29907985159) (#150) succeeded; GM-07a is remotely finalized (Commit A `baa5bf8` exact [run 29907589648](https://github.com/yanfengliu/python_mini_metro/actions/runs/29907589648), #149). This opening transaction reconciles that result. Both research lanes are preserved under `raw/` and every design choice below cites them.

Scope per the roadmap: strict versioned JSON snapshot roundtrips at clock-reset-safe boundaries, including pause reasons. GM-07b delivers the schema, saver, loader, and roundtrip/determinism proofs as a programmatic API only; autosave, Continue, and menu integration are GM-07c; high scores are GM-07d. `recursive_checkpoint.py` remains a one-way verifier and becomes the state-equality oracle for load verification. No recursive/agent/PlayerPixel contract, observation shape, low-level protocol, or gameplay-behavior change; content identity advances intentionally (new `src/` modules).

## Save schema v1

- New `src/save_schema.py` (< 500): `SAVE_SCHEMA_VERSION_V1 = 1`, current-version constant, supported set, and a strict validator family following the exact repo idioms — `schemaVersion` int top-level, `type(value) is not ...` checks rejecting bool-as-int, `_exact_keys`-style unknown-and-missing rejection per section, pinned contract string `"stateContract": "mini-metro-save-v1"`, forward versions rejected outright. The schema is a NEW validator family: it deliberately retains real entity ID strings, so checkpoint validators (whose `_reject_entity_ids` forbids IDs) are never reused on save documents.
- Serialized content is the canonical state from the research classification (`raw/research-serialization-surface.md`) plus the five review-surfaced scalars both lanes flagged (loader F5/schema F1): time/steps/speed/game-over; `passenger_spawning_step`, `passenger_spawning_interval_step`, `passenger_max_wait_time_ms`, and `overdue_passenger_threshold` (runtime-writable and production-mutated, so never config-assumed); the sorted `_pause_reasons` list with the exact v1 vocabulary `{"menu", "user"}` (unknown reasons rejected); the int `{1, 2, 4}` speed multiplier with bool rejection; progression's eleven fields; spawn-timer values with exactly one entry per pool station, missing or extra entries rejected (loader F11); stations (ID, position, shape type, active flag, capacity, waiting-passenger IDs in order, unlock-blink start, snap blips); passengers (ID, destination shape type, `is_at_destination`, `wait_ms`); paths (ID, color, ordered station IDs, ordered metro IDs, loop flag, `path_order`); metros (ID, `path_id`, position, `current_segment_idx`, `current_station` ID or null, direction, speed, kinematic constants, stop/boarding timers including `boarding_time_per_passenger_ms`, `just_arrived_and_stopped`, queue flag, `_base_capacity`, ordered carriage records with ID and capacity, onboard passenger IDs in order); travel plans keyed by passenger ID with next-path/next-station IDs, `next_station_idx`, and node paths encoded as per-node records carrying the station ID and that node's path-ID set filtered to document-live paths — exactly mirroring the checkpoint's ref-filter (`recursive_checkpoint.py:293-297`), which makes already-removed members oracle-invisible and behaviorally inert — so plan-creation-time topology views survive the roundtrip verbatim up to already-removed paths while the universal dangling-reference rejection stays satisfiable for the reachable onboard-survivor-after-removal state (loader F2; recheck Gap 1); path-color allocation as an order-preserving array of pairs (iteration order is load-bearing for first-free-color picks; `sort_keys` objects would destroy it — loader F6), with `path_to_color` and the spawn dicts likewise array-encoded; fleet totals (`num_metros`, `num_carriages`); per-slot path-button lock and unlock-blink values; a pinned `"rulesVersion": "rules-v1"` literal, with D-026 adopting the policy that any trajectory-affecting balance-config change must bump the save schema version in the same commit (schema F3 — map identity is deliberately absent and a future version synthesizes classic); and both RNG streams. IDs validate against per-class prefix grammars with global uniqueness (schema F5). The entire document passes the checkpoint-safe value coercion before strict validation (`np.float64` color components fail bare `type is float` checks — schema F2), and the loader's inverse transforms are pinned: the Python stream restores via deep tuple reconstruction `(state[0], tuple(state[1]), state[2])` (the outer-tuple-only form also raises — probe-verified), the NumPy PCG64 state dict round-trips as-is, and JSON color arrays tuple-ify before keying.
- Excluded as derived or transient, rebuilt on load: path segments/padding and every Segment/Node identity (rebuilt by `update_segments()` and the lazily built station graph), button geometry and `path_to_button` (layout-owned), `Metro.current_segment` object binding, `_station_service_action`, passenger display positions, interpolation caches, wall-clock accumulators, and every input gesture (`is_mouse_down`, creation/redraw/edit drafts). Saving is permitted only when no creation gesture is active (`is_creating_path` false, no redraw/edit draft); the saver rejects otherwise — the clock-reset-safe boundary from the research, which GM-07c's menu-paused save point satisfies by construction.
- Canonical bytes follow the frozen-fixture recipe exactly: `json.dumps(..., allow_nan=False, ensure_ascii=True, separators=(",", ":"), sort_keys=True).encode("ascii") + b"\n"`, no CR. One deterministic v1 fixture is frozen under `scripts/fixtures/save-v1.json` with SHA-256 and byte-length pins in its test, no `.gitattributes` entry.

## Saver and loader

- New `src/save_game.py` (< 500): `serialize_game(mediator) -> dict`, `save_game(mediator, path)` writing canonical bytes through a NEW save-local byte-level atomic writer following the mkstemp→fsync→`os.replace` idiom with the ASCII recipe and no `rl.` import (schema F6 — `write_json_atomic` itself re-dumps UTF-8 and would diverge fail-open on the first non-ASCII value), `deserialize_game(document) -> Mediator`, and `load_game(path) -> Mediator` (read, strict-validate, deserialize). The save directory is config-owned (`save_dir_name = "saves"`, repo-root-relative, created on demand, added to `.gitignore` as anchored `/saves/` — the source-provenance guard audits `.gitattributes` only, verified); every function also accepts explicit paths so tests use temp dirs and GM-07c chooses the user-facing location.
- The loader is the repo's first JSON-to-Mediator reconstruction (research: no precedent exists — the in-memory restore helpers hold live references). Order: construct `Mediator(seed=0)` headlessly; overwrite both RNG streams from the document (all construction-time draws precede the overwrite — verified sound); rebuild the station pool by constructing stations directly (no RNG draw) then assigning persisted IDs (constructors mint fresh `shortuuid`s independent of the game seed, so IDs are always assigned post-construction — every entity follows this rule); restore the active-station prefix, progression fields, fleet totals, and every scalar including the five review-surfaced ones; rebuild paths (assign IDs/colors/order/loop, attach station objects by ID, `update_segments()` while `path.metros` is still empty so the rebind loop cannot clobber persisted state — verified); rebuild metros and carriages with IDs, kinematics, timers, queue flags, and base capacity (writing `Carriage._capacity` directly — the public property is read-only, loader F8), bind `path_id` and dual list membership manually (never `add_metro`, which clobbers position), re-derive `current_segment` as `segments[current_segment_idx]` with bounds validation, and re-derive `metro.shape.color` from the owning path (the visual side effect only `add_metro` performs; `shape.degrees` self-corrects on the first tick — loader F7); rebuild passengers with IDs and fields, placing station-queue riders by DIRECT list append (D-024 over-capacity queues are canonical and `add_passenger` hard-asserts room — loader F4) while strictly rejecting any metro whose onboard count exceeds its derived capacity; rebuild `travel_plans` keyed by the restored passenger objects, rehydrating each node from its persisted station ID and exact path-ID set; restore the spawn-timer dicts and `path_colors`/`path_to_color` from their ordered-array encodings; synchronously call `assign_paths_to_buttons()` (which recomputes lock state), then restore per-slot `unlock_blink_start_time_ms` directly and validate persisted `is_locked` equals the derived value — the draft's pending-layout deferral is dropped because `prepare_layout` neither assigns buttons nor re-fires reliably (loader F3); hold the persisted pause reasons verbatim (a held `menu` reason restores as-is; GM-07c owns screen reconciliation and `release_pause_reason("menu")` is the API lever — loader F9); run the established station-service reconcile for every at-station metro (RNG-neutral — verified); and finally RE-APPLY the persisted `stop_time_remaining_ms`/`boarding_progress_ms` over the reconcile's reset and strictly validate the invariant — `stop_time == boarding_time_per_passenger_ms - progress` with `0 <= progress < interval` when an action is bound, exactly `(0, 0)` when none (mirroring `recursive_checkpoint_carriages.py:102-112` in full, recheck note) — because the reconcile always sees a `None` cache post-load and would otherwise wipe mid-boarding progress (loader F1, the blocker).
- Validate-equal semantics for every derived-redundant persisted field (loader F10): per-slot lock vs `unlocked_num_paths`, station active flags vs the prefix, `path_to_color` vs `path.color` and the taken flags; disagreement rejects. Per-slot array lengths tie to the restored `num_paths`, and a save whose `num_paths` or fleet-total shape disagrees with the running config is strictly rejected in v1 (config-divergence relaxation is a future version's explicit business).
- Saver purity pins (loader F12): read `plan.next_station` as an attribute (the getter mutates its cache); the mid-gesture rejection set checks `is_creating_path`, `path_being_created is not None`, `path_redraw`, and `path_edit_selection`; a bare `is_mouse_down` with no gesture does not block saving (it is in no checkpoint field and load-resets harmlessly).
- Strict failure behavior: any validation or reference-resolution failure (unknown ID, duplicate ID, dangling reference, out-of-range index, wrong type, unknown key, unsupported version) raises before any file is written or any partially constructed Mediator escapes; `load_game` never returns a partially restored object. The saver never mutates the Mediator it serializes (byte-identical checkpoint before/after save is a red test).
- Isolation: `env.py`, `agent_play.py`, `recursive_playtest.py`, and `src/rl/` gain no imports and no save behavior; the global-RNG isolation oracle stays green; `AppController`/menus are untouched this unit (GM-07c wires Continue).

## Acceptance oracles (all red tests first)

- Roundtrip state equality: the loaded Mediator wraps in a harness env whose accounting fields (`dt_ms_default`, `reward_mode`, `last_score`, `last_deliveries`, `last_line_credits`) are grafted from the control env before checkpointing, so `canonical_checkpoint` equality is genuinely byte-for-byte including both RNG states (schema F4/loader F13); scenarios cover riders aboard and waiting, queued and occupied locomotives, carriages, over-capacity stations, active travel plans, a plan created BEFORE a newer path exists (the stale-`node.paths` conservation case, loader F2), an onboard survivor whose plan outlived a removed path (the ref-filter case, recheck Gap 1), mid-stop service timers at fractional boarding progress (the loader-F1 blocker case), non-default values for every review-surfaced scalar (the default-config blind spot both lanes flagged), unlock blinks, snap blips, held pause reasons (user, menu, both), and game-over state.
- Seeded-trajectory equality: load into env B while control env A never saved; N lockstep ticks with per-tick checkpoint equality (the `test_simulation_context.py:48-57` pattern), plus a fresh-process `PYTHONHASHSEED=0` byte-identity run following the gm06c cross-process precedent.
- Public-ID preservation: every pre-save observation ID class (path, station, metro, carriage, passenger) survives; the acceptance action shape — pre-save `path_id` → save → load → `apply_action({"type": "remove_path", "path_id": ...})` returns True — plus fleet/carriage selectors by ID.
- Strictness: forward-version, unknown-key, missing-key, wrong-type, bool-as-int, duplicate-ID, dangling-reference, malformed-RNG, and mid-gesture-save rejections, each without partial effects; the frozen v1 fixture byte/digest pins; saver purity (no Mediator mutation, byte-identical checkpoints around save); atomic-write crash surface (temp file cleaned up on failure, destination untouched).
- Docs: README programmatic save/load API; GAME_RULES unchanged (no player-facing surface this unit); ARCHITECTURE new modules and the one-way-verifier boundary note; PROGRESS bullet; D-026 recording the save-directory, ASCII-canonical-bytes, and rules-identity decisions — the pinned `rulesVersion` literal plus the policy that any trajectory-affecting balance-config change bumps the save schema version in the same commit, with map identity deliberately deferred to a future version that synthesizes classic (schema F3).

## TDD, validation, review, and delivery

1. Adversarial plan review in two lanes (loader-correctness/state-conservation; schema/compatibility-strictness) against live code, findings folded, narrow recheck CLEAN — before any test.
2. Red tests in focused modules (schema validation; serialize/saver purity and bytes; loader reconstruction and reference resolution; roundtrip equality; trajectory determinism incl. fresh-process; ID preservation; atomic-write behavior; isolation guards), lane and combined red evidence recorded at the finalized baseline.
3. Implement `save_schema.py` and `save_game.py` plus the minimal Mediator seam for synchronous button-state restoration (`assign_paths_to_buttons` plus direct blink restore) and pause-reason restoration; no near-limit file grows past its budget; the Mediator facade stays under 1,000.
4. Full local gates: definitive py313 suite, guarded `npm test`, Ruff/format, per-file pre-commit, line counts.
5. High-risk review per the constitution (persistence): attempt the multi-cli lane per the fleet runbook (Codex; the Claude CLI reviewer is expected to fail on OAuth per project memory) alongside two independent harness lanes; record honestly which external lanes ran; substantive findings fixed with regressions and a clean re-review required.
6. Stage the reviewed payload, commit/push `[GM-07b:A]`, wait for exact-head `build` and `rl-smoke`, then evidence-only `[GM-07b:B]`. GM-07c remains closed until B is remotely reconciled.

## Acceptance

- A game saved at a quiescent boundary and loaded back is checkpoint-identical (RNG included), replays the identical seeded trajectory as a never-saved control in-process and cross-process, and honors every pre-save public entity ID in structured actions.
- The schema is strictly versioned and fail-closed; the frozen v1 fixture is byte-pinned; saves are atomic; a failed load leaves nothing partially constructed; the saver never mutates game state.
- Headless/programmatic surfaces and all frozen v1-v5 replay behavior are byte-identical; the checkpoint verifier remains one-way; no player-facing UI changes this unit.
