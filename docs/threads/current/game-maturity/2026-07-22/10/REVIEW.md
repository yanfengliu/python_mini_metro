# GM-07b review synthesis

Status: review-clean; local gates green at 1147/0 Python and 249/0 Node; Commit A staging active

Two research lanes (`raw/research-serialization-surface.md`, `raw/research-determinism-versioning.md`) mapped the full canonical/derived/transient state surface, the shortuuid-off-stream ID fact, the RNG round-trip idioms, the versioning and frozen-fixture recipes, and the reusable oracles. The plan gate ran two adversarial lanes (`raw/plan-review-loader.md`: one blocker — the load-time reconcile wiping persisted mid-boarding timers — plus four majors and eight minors; `raw/plan-review-schema.md`: one major and three mediums, with the missing-scalar gap found independently by both lanes and the deep-tuple `setstate` trap probe-verified) and a narrow recheck that closed all twenty-one findings while catching two fold-introduced contradictions (ref-filtered node path-ID sets; a stale pending-layout sentence), both dispositioned.

Red evidence (`red-evidence.md`): 48 tests across four modules, 49 red records, zero collateral at the 1081-test baseline, every scenario probe-verified pre-authoring. The implementation (four modules: `save_schema.py`, `save_schema_records.py`, `save_game.py`, `save_load.py`, plus the frozen `save-v1.json`) went green; completing its interrupted final gate pass surfaced a design contradiction in two red oracles — byte-exact fixture regeneration and fresh-build cross-process identity are impossible for an ID-bearing schema — corrected to strictly stronger oracles (cross-process load→re-save byte idempotence; checkpoint-equivalence regeneration), recorded as a contract amendment.

The high-risk battery ran per the constitution: the Codex CLI external lane (pinned `gpt-5.6-sol`, ultra effort, verbatim at `raw/codex.md`) and an independent harness probe lane (`raw/harness-review.md`); the Claude CLI lane was skipped per the recorded OAuth failure mode and no external approval is claimed for it. Both lanes converged on one blocker from different directions: the schema treated the metro service cache as purely derivable, but a later-indexed metro's same-tick action legitimately invalidates an earlier metro's bound cache, so reachable multi-locomotive saves either refused to load or loaded and silently diverged (boarding progress re-attributed across passengers; ~0.3% of boundaries in the harness's 1,264-boundary sweep). Codex added six majors and two minors (RNG numeric domains, locked-station topology passing validation, silent speed normalization, duplicate-JSON-key bypass, seam-missing atomicity/reconstruction tests, a paused same-hash-seed worker, isolation-scan gaps, doc overstatements); the harness added the OverflowError contract breach, fuzz-surfaced value gaps, and the discovery that `canonical_checkpoint` itself crashes on the stale states — a pre-existing GM-06 defect filed as its own background task alongside the dead GM-03f differential verifier.

The fix round closed everything: schema v1 now persists each metro's bound service action verbatim (nullable kind/passenger record validated for at-station, zero-speed, and exact timer invariants — tampered speed now rejects rather than normalizes), the loader restores it verbatim and the reconcile-then-re-apply machinery is deleted; RNG domains are range-pinned with ValueError normalization; references validate against the active prefix; duplicate JSON keys reject at every level; the atomicity and reconstruction tests exercise their real seams; the cross-process worker replays actively under distinct hash seeds; the isolation scan covers all four modules plus the checkpoint verifier; the fixture was refrozen (15,442 bytes, pins updated); and the docs were corrected. Red-first evidence exists for every blocker reproduction, and the post-fix five-scenario sweep reports zero failures across 1,265 boundaries. The final narrow re-review (`raw/` fix report, verdict in the session record) returned CLEAN after an independent 402-boundary sweep covering board, destination, transfer, null, and genuinely stale caches through a game-over flip, with kind coverage verified against the live oracle constants and the deliberate absence of an aboard/waiting rule shown to degrade only to the live stale-reset semantics.

Final local evidence: six GM-07b modules 66/66; full py313 suite 1147 tests, 0 failures, 12 expected skips; guarded `npm test` 249/0 (one isolated-verified recurrence of the known Windows EPERM ledger flake during the round, 9/9 in isolation); Ruff, format, and per-file hooks clean; all line budgets held (`save_schema_records.py` 471 the largest). The PLAN's derived-cache design is superseded by the verbatim-persistence amendment recorded in `red-evidence.md`.
