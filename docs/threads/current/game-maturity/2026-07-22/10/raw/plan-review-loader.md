# GM-07b plan review — loader correctness / state conservation — verdict: NOT CLEAN (verbatim)

## Findings

- F1 BLOCKER: the final load-step reconcile destroys persisted mid-stop boarding timers. `reconcile_service_action` (passenger_capacity.py:234-261) sees the post-load `None` cache, `same_service_action(None, action)` is False, so it zeroes `boarding_progress_ms` and resets `stop_time_remaining_ms` to the full interval — the plan's own mid-stop oracle fails and trajectories diverge. The GM-06d precedent does not transfer (live cache non-None there). Fix: after the reconcile binds the cache, re-apply the persisted timers and strictly validate `stop_time == interval - progress` when bound, `(0, 0)` when the derived action is None. The reconcile's `metro.speed = 0` write is safe (bound-action metros persisted speed 0).
- F2 MAJOR: station-ID node paths cannot conserve stale `node.paths` sets. Existing plans keep plan-creation-time node objects whose `paths` sets go stale when later paths are created; `invalidate_travel_plans_for_path` (path_lifecycle.py:243-249) keys decisions off them, and the checkpoint records `node_path[].path_indices`. Fresh-graph resolution silently modernizes them → keep/delete divergence after load and checkpoint byte-inequality. Fix: serialize per-node path-ID sets and rehydrate exactly (or explicitly accept normalization and adjust oracles); add the plan-older-than-a-newer-path scenario either way.
- F3 MAJOR: the pending-layout button-state deferral contradicts the primary oracle and may never fire — `prepare_layout` neither assigns paths to buttons nor touches lock/blink (input_coordinator.py:19-61) and re-fires only on surface-size change; post-load `path_to_button` would be empty and every checkpoint with paths fails. Fix: loader synchronously calls `assign_paths_to_buttons()` after paths (which recomputes locks), restores per-slot `unlock_blink_start_time_ms` directly, and validates persisted `is_locked` equals the derived value.
- F4 MAJOR: over-capacity station restore cannot go through `Holder.add_passenger` (asserts `has_room`, holder.py:69-71); D-024 overflow state is canonical and restored queues must use direct list append, with metro overflow strictly rejected (metros can never exceed capacity live).
- F5 MAJOR: five checkpoint-canonical writable scalars missing from the schema inventory: `passenger_spawning_step`, `passenger_spawning_interval_step` (mediator.py:101-102; checkpoint spawning.start_step/base_interval_steps), `passenger_max_wait_time_ms`, `overdue_passenger_threshold` (mediator.py:158-159; checkpoint limits), and `metro.boarding_time_per_passenger_ms` (checkpoint :360). Persist all five (or declare config-assumed explicitly — persisting keeps the byte-equality claim honest). The research classification itself missed the first two.
- F6 MINOR: `path_colors` iteration order is load-bearing (first-free-color pick, path_lifecycle.py:276-282) and `sort_keys` JSON objects destroy it — encode as an ordered array of pairs (checkpoint-style), same discipline for `path_to_color` and spawn dicts.
- F7 MINOR: manual metro binding must re-derive `metro.shape.color = path.color` (only `add_metro` does it, path.py:162, and `add_metro` must be avoided because it clobbers position); note transient `shape.degrees`.
- F8 MINOR: `Carriage.capacity` is a read-only property — write `_capacity` directly or validate equals config.
- F9 MINOR: state explicitly that pause reasons restore verbatim (menu included), GM-07c owns screen reconciliation, and `release_pause_reason("menu")` is the API lever.
- F10 MINOR: validate-equal semantics for derived-redundant fields (per-slot lock, station active flags, path_to_color agreement), and tie per-slot array lengths to the restored `num_paths`, strictly rejecting config-divergent saves.
- F11 MINOR: spawn-timer coverage must be exactly one entry per pool station (missing/extra rejected) or the post-unlock draw desyncs the stream (initialize path passenger_flow.py:102,156).
- F12 MINOR: saver purity — read `plan.next_station` as attribute (the getter mutates the cache, travel_plan.py:20-25); pair `path_being_created is not None` with `is_creating_path` in the rejection set; state the bare-`is_mouse_down`-allowed stance.
- F13 MINOR: `canonical_checkpoint` is env-scoped (dt/reward-mode/last_* fields) — the oracle wraps the loaded Mediator in a matching harness env; test footnote.

## Confirmed sound

RNG-overwrite-after-construction ordering correct (all construction draws precede the overwrite; direct Station construction draws nothing; shortuuid off-stream); load-time reconcile is RNG-neutral (pure_service_action restores both streams in finally); `update_segments` on empty `path.metros` cannot clobber persisted bindings and manual `segments[idx]` matches the sim's own binding; node resolution mechanics work through restored stations (equality by station), resolution-only validation is the correct bar; passenger `position` exclusion byte-exact; pause-reason storage sound; quiescent-boundary transients provably absent; `path_order` verbatim restore reproduces geometry; the progression eleven-field claim exact.

## Verdict: NOT CLEAN — fold F1-F5 before red tests; F6-F13 are cheap now and expensive after the fixture freeze.
