# GM-07b harness-lane adversarial review — verdict: NOT CLEAN (synthesis; probes in session scratchpad)

FINDING 1 — BLOCKER (independently converging with Codex finding 1): reachable multi-locomotive states save fine but cannot load, and a sibling mode loads then silently diverges. Root cause: `move_passengers` iterates `host.metros` in order, so a later metro's executed action invalidates an earlier metro's already-bound `_station_service_action` within the same tick; at the boundary `cache != pure_service_action(state)`. The loader reconciles from a None cache and re-applies persisted timers under an invariant false for these states (`save_load.py:285-306`; `save_game.py:101-129`; `passenger_flow.py:322-361`). Reproductions (pure public API, default knobs): seed 4501 two-locomotive rejection at tick 26; seed 9001 two-lines rejection at tick 20; seed 9001 ticks 55/58 SILENT DIVERGENCE — 250 ms of boarding progress re-attributed from the stale passenger to the newly derived one, boarding it one tick early vs the never-saved control. Incidence ~0.3% of boundaries (4/1264 across 5 rich scenarios, 200-320 ticks each, UUID-normalized save-document oracle). Why 48/48 green missed it: every committed scenario has at most one metro per station, and `canonical_checkpoint` itself raises on exactly these states (`fleet_validation.py:55-106` requires cache==pure), so checkpoint-based oracles structurally cannot express them — a pre-existing GM-06 defect (the recursive verifier also crashes there) worth its own follow-up. Fix (chosen): persist the bound service action per metro (kind + passenger ID, nullable) in schema v1 and restore verbatim, reconciling only when absent — the only option preserving trajectories exactly, because the stale-cache-reset dynamics are real game semantics; the rejection-gate alternative makes save availability flicker and needs GM-07c retry semantics.

FINDING 2 — MEDIUM: `OverflowError` (contract says `ValueError`) escapes on schema-valid RNG fields — `rng.python[1][i] = 2**64` or `-5`, numpy `state/inc = -1` or `2**200`, `uinteger = -1`; `_validate_rng` checks types not ranges. Fix: range-pin words [0,2^32), index bounds, state/inc [0,2^128), uinteger [0,2^32), has_uint32 ∈ {0,1}; normalize setter failures to ValueError.

FINDING 3 — LOW: validator accepts never-serialized shapes: `has_uint32=7` (truthy semantics alter draws), huge `rng.python[2]` gauss (unused — benign), consecutive duplicate station IDs within a path (zero-length segment), unconstrained `pathOrder`. From a 1038-mutant parametric fuzz; all structure-breaking mutants correctly rejected.

FINDING 4 — INFO: BOM/CRLF save files load (json tolerance; canonical bytes pinned on save only — arguably a feature); missing file raises `FileNotFoundError` (OSError family); `config.save_dir_name` consumed by no production code yet (assigned to GM-07c by the plan).

Confirmed sound: 320-tick lockstep across 4 rich scenarios (two lines sharing stations, staggered locomotives, carriages, queued occupied locomotive, cancel, drain over-capacity, remove_path alights, speed 4, loop, pause/resume, game-over flip) byte-equal at every checkpoint-canonical boundary; save-at-every-tick sweep including across the game-over tick clean; full action-after-load parity matrix by ID and index clean; second-order roundtrip clean; adversarial-document battery (NaN/Infinity, negative/huge indices, dup IDs, dual-located passengers, dup pause reasons, unknown spawn stations, unicode/8000-char IDs, forward versions) all ValueError; filesystem fault matrix zero-litter with intact originals; `/saves/` anchored correctly; civ-engine `--verify-only` green with the new files; import isolation including subprocess side-effect checks clean.
