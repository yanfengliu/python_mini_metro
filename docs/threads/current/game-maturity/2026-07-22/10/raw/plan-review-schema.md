# GM-07b plan review — schema/strictness/compatibility — verdict: NOT CLEAN (verbatim)

## Findings

- F1 MAJOR: schema omits four runtime-mutable Mediator scalars the repo's own oracle treats as canonical (`passenger_spawning_step`, `passenger_spawning_interval_step`, `passenger_max_wait_time_ms`, `overdue_passenger_threshold` — mediator.py:101-102,158-159; production surfaces mutate the threshold: agent_play.py:225, recursive_playtest.py:284; checkpoint records all four at recursive_checkpoint.py:380-381,416-434,469-470). Default-config oracles pass anyway, so the omission ships undetected. Fix: persist all four plus a non-default-knob roundtrip scenario. (Independently found by the loader lane, which adds `metro.boarding_time_per_passenger_ms`.)
- F2 MEDIUM (probe-verified): `random.Random.setstate` needs DEEP tuple reconstruction — `setstate(tuple(outer))` also raises; required form `(doc[0], tuple(doc[1]), doc[2])`, byte-exact after that. NumPy PCG64 state round-trips through JSON with zero handling. `path_colors` keys are tuples of `np.float64` — JSON lists must be tuple-ified and the whole document must pass `safe_checkpoint_value` coercion before strict `type is` validation (np.float64 fails `type is float`). Add a malformed-RNG red including the outer-tuple-only mistake.
- F3 MEDIUM: v1 is map- and rules-anonymous while the roadmap commits to both identities. Map identity is a safe omission (v2 upgrade synthesizes classic). Rules identity is not: trajectory-affecting balance config can change under GM-11 with no schema bump, making differently-ruled v1 saves indistinguishable. Fix: a D-026 decision — embed a rules identifier in v1 or adopt config-change⇒schema-bump policy.
- F4 MEDIUM: `canonical_checkpoint` is env-scoped and embeds env-only accounting (`dt_ms_default`, `last_score`, `reward_mode`, `last_deliveries`, `last_line_credits` — recursive_checkpoint.py:160-178,371-374,421-427); byte-for-byte equality on a bare loaded Mediator is unsatisfiable. Fix: graft the control env's accounting onto the wrapper env before checkpointing (or scope equality and say so).
- F5 MINOR: duplicate-ID scope and ID grammar unspecified — pin per-class prefix grammar (e.g. `Station-<base57>-ShapeType.RECT`) and global uniqueness.
- F6 MINOR: `write_json_atomic` re-dumps with `ensure_ascii=False`/UTF-8 and imports the RL stack; silent reuse passes for ASCII payloads then diverges fail-open. Fix wording: a new save-local byte-level writer following the idiom, no `rl.` import.
- F7 MINOR: `.gitignore` audit-safe (source-provenance guard audits `.gitattributes` only); use the anchored `/saves/` form.
- F8 LOW: pin the exact v1 pause vocabulary `{"menu","user"}` (unknown rejected), int-typed `{1,2,4}` speed multiplier with bool rejection; cross-lane note on `metro.shape.color` re-derivation.

## Confirmed sound

NumPy state round-trip exact with zero handling (probe); no `np.float64` in positions (`round(np.float64)` → int; movement math pure-Python float); float-bearing pinned fixtures already proven byte-stable across Windows-local and Ubuntu/Windows CI daily; fixture-freeze recipe precedent exact; versioning machinery matches the live idiom and exact-key strictness does not block evolution; `stateContract` literal matches both precedent families; travel-plan station-ID encoding lossless at the node level; passenger shape color is config-constant; progression eleven-field count exact; no suite pins module inventories; D-026 is the correct next decision id; full-pool spawn coverage is exactly what the checkpoint enforces.

## Verdict: NOT CLEAN — fold F1-F4 (and the minors) before red tests; the shape (new validator family, ASCII canonical bytes, frozen fixture, atomic writer) is sound and precedent-backed.
