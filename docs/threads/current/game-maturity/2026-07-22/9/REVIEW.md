# GM-07a review synthesis

Status: delivered as Commit A `baa5bf8`, exact [run 29907589648](https://github.com/yanfengliu/python_mini_metro/actions/runs/29907589648) green; evidence-only Commit B active

The runtime research (`raw/research-runtime-pause.md`) mapped the loop, the boolean pause surface, game-over navigation, headless entry points, the dual checkpoint pause projections, and ten concrete risks; its one open question — whether new `src/` modules affect RL identity — was answered directly in-session: `compute_content_fingerprint` hashes all non-`rl` `src/` files, so content identity advances intentionally while protocol/task fingerprints stay exact. The research subagent also reported, refused, and escalated an injected cross-repository directive demanding a token-embedded external push; the incident is recorded in session memory and none of it touched this unit.

The combined adversarial plan review (`raw/plan-review-combined.md`, NOT CLEAN, no blockers) confirmed the architecture — controller-gated dispatch, a user/menu reason algebra behind the retained facade, checkpoint-neutral internals — and produced three majors and five minors: the property conversion would break bare `Mediator.__new__` hosts and the GM-03f differential scripts; moving construction into the controller would break every `test_main.py` patch seam and leave stale loop locals; Escape mid-gesture would leave an armed pointer gesture behind the menu; plus start-state mechanics, TITLE-state existence, a roadmap-scope amendment for the deferred settings screen, exact-`bool` projection, and a baseline SHA misattribution. All eight were folded (per-instance lazy store with a class-default prohibition; a main-owned construction closure with per-frame triple reads; the letterbox cancel dispatched before the menu hold; `max_frames`-based auto-selection; eager TITLE construction; D-025; type pins; corrected SHAs) and the narrow recheck (`raw/plan-recheck.md`) returned CLEAN with every disposition re-verified against live code and the exact run/job identities confirmed through the GitHub API.

Red evidence preceded every production change: 31 tests across three modules (30 red records, two marked baseline guards), zero collateral on the 1075-test tree, with the review-mandated reds — bare-host behavior, swap-frame rebinding, mid-drag cancel ordering, exact-`bool` typing — encoded verbatim. The red author disclosed a `git stash -u` incident that briefly swept untracked docs and CRLF-converted nine files; remediation was verified independently (no tracked file touched, LF restored byte-exact, fixtures untouched).

Implementation landed `app_controller.py`, `ui/menu_screens.py`, a rewritten `main.py`, a forty-line mediator addition, and a line-neutral Space reroute, going green on the full contract with no red-test contradiction. Two independent adversarial implementation lanes then attacked it. The compatibility/replay lane (`raw/implementation-review-compatibility.md`) returned one minor doc-honesty finding and otherwise confirmed every surface sound with dynamic byte-level evidence: cross-tree HEAD-vs-live determinism over structured pause/resume sequences, the pause-relevant GM-03f differential cases byte-identical, frozen v1-v5 and Node contract suites green, reason-neutral checkpoints, D-010 semantics proven on a real Mediator, and strict `main.py`-only isolation of the new modules. It also proved the full GM-03f differential verifier has been dead since GM-06c's control-band layout (a pre-existing failure reproduced on an extracted HEAD tree), flagged out of scope. The state-machine/UX lane (`raw/implementation-review-statemachine.md`) ran an eighty-probe event-by-state matrix plus a real windowed run with screenshots, found zero behavioral defects, and raised one medium coverage gap — the new loop branches were pinned by no test — and two low UX hazards: bare-`MOUSE_UP` menu firing that let a released drag hit Restart, and a one-keypress Escape race near the game-over flip.

The fix round (`raw/` fix report reflected in `red-evidence.md`) closed the gap and the first hazard: a mutation-tested loop-coverage module now pins TITLE `advance(0)` and chrome selection, the same-frame session rebind, and menu-frame compositing order — three deliberate `main.py` mutations were each caught and reverted byte-identically — and the pause menu now arms on `MOUSE_DOWN` and fires only on a same-control release, landed red-first with a mid-drag no-op regression and surgical DOWN+UP amendments to the menu-click tests (title and game-over bare-UP semantics untouched). The doc sentence claiming keyboard speed keys unpause was corrected to match the probed behavior, and the Escape race was explicitly accepted as pre-existing game-over binding semantics with any confirmation affordance deferred to GM-08. Final local evidence: 1081 tests, 0 failures, 12 expected skips; guarded `npm test` 249 pass, 0 fail; Ruff, format, and per-file hooks clean; `app_controller.py` 170, `menu_screens.py` 130, `main.py` 125, `mediator.py` 831 under its ceiling, `input_coordinator.py` unchanged at 498. The external multi-CLI workflow remains unavailable at the established repository-export boundary, so no external approval is claimed.
