# GM-07c review synthesis

Status: implementation review-clean; local gates green at 1173/0 Python and 249/0 Node; Commit A staging active

Research (`raw/research-autosave-continue.md`) mapped the controller/menu/loop integration surface and established the load-bearing legality invariant: a PAUSE_MENU Mediator always passes the saver's quiescence preflight because the letterbox cancel dispatched on menu entry clears every armed gesture before the menu reason is held. The combined adversarial plan review (`raw/plan-review-combined.md`, NOT CLEAN) found one blocker and six findings and confirmed the quiescence trace, `prepare_layout` safety, the menu_screens lockstep, and isolation. The blocker was a frame-accurate save-scumming race: `is_game_over` flips during a simulation tick, but the promotion (and its autosave delete) runs only on the next event, so a window close at game over would leave — or the ungated QUIT save would create — a resurrectable finished-run save. The folded D-027 policy gates every exit surface on `is_game_over` (delete when over, save only from the pause menu or un-over play, controller `SystemExit` sites write nothing) and was recorded in the parent decision log. The independent narrow recheck was interrupted by a model-quota limit; the parent self-verified the seven folds against live code (confirming the promotion-precedes-routing ordering that makes the interactive game-over exits delete-safe) and fixed one acceptance staleness — recorded honestly at `raw/plan-recheck.md`, with the substantive plan review remaining the primary independent gate.

Red evidence (`red-evidence.md`): 25 tests across four modules (22 red + 3 baseline guards), zero collateral at the 1148-test baseline, every failure a clean `AssertionError` via the require_attribute idiom; the minted product surface (optional `build_from`/`autosave` seams, `notice` attribute, the state-gated main QUIT branch, the three-button title layout, `draw_notice`) was formalized from the plan with no STOP-worthy gap.

Implementation wired the seams into `app_controller.py`, `main.py`, and `menu_screens.py` with four justified deviations (a title-draw split forced by a frozen single-arg mock; a QUIT-branch nesting that additionally prevents the controller-spy suite from deleting a developer's real save; a broadened best-effort save catch; and a pre-existing font-cache staleness fix surfaced when menus first render). The single combined implementation-review lane (`raw/implementation-review.md`, CLEAN) ran real probes and a windowed session: it verified the live title composite is byte-identical to the tested path with the Continue rect aligned for hit-testing, walked the full QUIT truth table including the F1 race, fuzzed 120 corrupt saves with zero escaping exceptions, confirmed the persisted-user-pause Continue resumes paused, proved `prepare_layout` on a loaded Mediator leaves the checkpoint unchanged, and confirmed the suite creates no real `saves/` directory and headless surfaces gain no save import. Three minor non-blocking observations were accepted as documented (an unreachable `_build_from` None-guard gap covered by the both-absent regression guard; a negligible per-frame title `.exists()`; the deviation-3 policy-vs-code wording gap).

Final local evidence: four GM-07c modules 25/25; full py313 suite 1173 tests, 0 failures, 12 expected skips; guarded `npm test` 249/0; Ruff, format, and per-file pre-commit clean; budgets held (`app_controller.py` 222, `main.py` 225, `menu_screens.py` 158, all under 500). No schema, protocol, observation, or frozen-artifact change; the GM-07b save API and isolation guard are untouched. The two out-of-scope discoveries filed earlier (the checkpoint stale-cache crash and the dead GM-03f differential verifier) were resolved on `origin/main` by separate sessions (`9a33aaf` GM-07b:C and `5522da2`); GM-07c reconciles that history at Commit A via a fast-forward rebase. The external multi-CLI workflow remains unavailable at the established repository-export boundary, so no external approval is claimed for a Codex/Claude persistence-grade lane on this contained UI-wiring unit; the single independent implementation-review lane plus a real windowed run is the review of record.
