# GM-07c implementation review — verdict: CLEAN (verbatim synthesis)

Adversarial pass with real probes and a real windowed session (SDL dummy driver, real `set_mode`) exercising the live paths the tests skip. Artifacts (scratchpad): probe_headless (19/19), probe_windowed + 3 screenshots, probe_corrupt (120 corruptions, 0 escapes), probe_userpause. All gates green (ruff/format clean; 25/25 GM-07c; 1173/0 full; no `saves/` dir created).

The four implementer deviations — all sound:
1. Title draw split (`main._draw_title_continue_button` painting over single-arg `draw_title_screen`): full-surface bytes of the live path are IDENTICAL to `draw_title_screen(surface, continue_available=True)` and the Continue-rect region matches; the button is drawn at `title_layout(...)["continue"]` where the controller hit-tests, so clicks land; drawn only when a save exists; byte-stable. Forced by the frozen single-arg `draw_title_screen` mock in `test_gm07a_run_game_loop.py:163-164`.
2. QUIT branch nesting: the full truth table matches D-027/F1 — PLAYING/un-over → write; PAUSE_MENU → write; PLAYING+is_game_over (the race) → delete, no resurrectable write; PAUSE_MENU+over → delete; GAME_OVER → neither (delete already ran at promotion); TITLE → neither. The F1 window-X race deletes and writes nothing.
3. `write_autosave` broad `except Exception`: with a real quiescent mediator `serialize_game` raises only ValueError/OSError, so the broadening only ever catches test-fake AttributeError in reality; a real disk OSError leaves the prior save byte-intact with no `.tmp` leftover; SystemExit/KeyboardInterrupt not caught.
4. Font-cache removal in `menu_screens._font`: byte-stability preserved; the only remaining `_fonts` reference is the unrelated `game_renderer` instance cache.

Broader attacks confirmed sound: end-to-end roundtrip checkpoint-identity + 8-tick lockstep; game-over deletes → Continue inert; persisted user-pause → Continue resumes PLAYING-but-PAUSED (is_paused True, menu released, user kept); 120-case corrupt-save fuzz with zero escaping exceptions (validate_references catches referential breakage before deserialize dereferences); `prepare_layout` on a loaded mediator does not alter the checkpoint (position updaters touch only `.position`, never lock/blink, and positions are not serialized); full suite creates no real `saves/` dir; README/GAME_RULES honest (game-over deletes, single `saves/autosave.json` slot, mid-run window-close keeps, held-SPACE pause honored); isolation intact (env/agent/recursive/rl gain no save import); the optional-seams regression guard is real.

Minor observations (non-blocking, no fix required): `_continue_game` guards `autosave is None` but not `_build_from is None` (unreachable in shipped wiring — main always passes both — and covered by the both-absent regression guard); `peek_autosave` runs a per-frame `.exists()` on the title screen (negligible, title-only); deviation 3's `except Exception` is a policy-vs-code wording gap (behaviorally identical for real mediators); the reserved empty Continue slot on a no-save title is by explicit design for deterministic hit-testing.

No blockers, no majors. Verdict: CLEAN.
