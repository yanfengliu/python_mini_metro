# GM-07c research — autosave/Continue integration surface (synthesis of the verbatim lane report)

- Sizes: app_controller.py 170, menu_screens.py 130, main.py 125, save_game.py 269 — ample headroom everywhere.
- Controller seams: `_start_new_game` (app_controller.py:89-91) is the reconstruction chokepoint; Continue needs a sibling installing a LOADED Mediator via a second injected factory (`build_from(mediator)`), keeping the controller display/FS-free per its docstring. Autosave-on-menu-open attaches after `hold_pause_reason("menu")` (:98); exits are SPLIT — controller SystemExit sites (:152/:161/:169) and `exit_to_title` (:139) versus the window-close QUIT handled directly in main.py:58-59, which bypasses the controller entirely; both surfaces must autosave.
- menu_screens: `_stacked_buttons` re-centers when a key joins `title_layout` (:58-63); adding Continue means lockstep edits to the keys tuple, the draw iterator (:115), and the hardcoded heading anchor (:114); no public text primitive exists for failure messages (only private `_draw_heading`/`_draw_button`).
- KEY LEGALITY FINDING: a PAUSE_MENU Mediator ALWAYS passes `save_game._require_quiescent` (save_game.py:30-40 — rejects only the four gesture fields; `is_mouse_down` explicitly allowed) because menu entry dispatches the letterbox cancel BEFORE holding the reason, and that cancel clears creation (`abort_path_creation`, input_coordinator.py:256-264 → path_lifecycle.py:407-412), redraw (`_clear_redraw`), and edit selection (`path_handle_input.py:31-33`). The redraw case is test-asserted at the boundary; creation/edit-selection are structurally cleared but unasserted — red-test opportunity. GAME_OVER is NOT guaranteed quiescent (the PLAYING→GAME_OVER promotion at :78-79 dispatches no cancel), so autosave there is a legality risk. TITLE trivially passes but is meaningless to save.
- Loop: fully synchronous, no deferred-work idiom; a ~15KB save (fixture 15,442 bytes) is sub-millisecond — event-triggered synchronous saves are fine off the hot path.
- Continue semantics: `load_game` returns a bare Mediator; the factory wraps it (GameRenderer + GameSession + prepare_layout per main.py:38-42). UNVERIFIED interaction: `session.prepare_layout` on a loaded Mediator versus the loader's already-restored button/blink state (save_load.py:292-302 comment) — must be probed. Pause reasons restore verbatim (a menu-boundary save carries "menu", maybe "user"); the GM-07b worker precedent releases "menu" before active replay (test_gm07b_save_determinism.py:82-83); `release_pause_reason("menu")` is the documented lever (save_load.py:128-129).
- Failure surface: Continue catches `(ValueError, OSError)`; interrupted-write safety already proven at the writer level (TestGM07bAtomicWriteSeams — every seam injected, prior destination intact); discovery policy undecided by D-026 (deliberately).
- Test idioms to extend: `_fake_factory`/`_RecordingMediator` (test_gm07a_app_controller.py), the real-run_game loop module, test_main display mocks with QUIT→SystemExit, `_saved_then_loaded` roundtrip helpers, and the AST isolation scan (Continue/autosave stay inside the main+controller shell that env/agent/recursive never enter).
- Risks: game-over non-quiescence; window-close bypassing the controller; prepare_layout-vs-loader interaction; unasserted creation/edit clearing at menu entry; the three-way lockstep menu_screens edit.

Open decisions resolved by the parent session as D-027 defaults (all reversible): trigger = pause-menu open + exit_to_title + all three controller exits + main's QUIT (never GAME_OVER); file = canonical `saves/autosave.json` under `config.save_dir_name`; game over DELETES the autosave (a finished run cannot be Continued — Mini Metro fidelity; forgiving mode is a one-constant future flip); Continue releases "menu", honors a persisted "user" pause, enters PLAYING; failure UX = Continue absent/disabled when no loadable save, short title notice on load failure; seams = injected `build_from` factory + injected autosave/delete callbacks so the controller stays FS-free.
