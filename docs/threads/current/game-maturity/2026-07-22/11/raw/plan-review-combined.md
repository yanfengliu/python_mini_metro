# GM-07c plan adversarial review — verdict: NOT CLEAN (verbatim synthesis)

- F1 BLOCKER: game-over delete race + QUIT resurrection. `is_game_over` flips inside `session.advance` after the event pump; the PLAYING->GAME_OVER promotion (and the plan's delete) runs only on the next controller-routed event, and `pygame.QUIT` never reaches the controller (main.py:58-59). Window-X can be the first event after game over -> promotion/delete never fire; the plan's QUIT branch saves a mediator with `is_game_over=True` (probe: game-over mediators serialize and restore fine — the saver checks no game-over flag), so Continue resurrects a finished run; even skipping the QUIT-save leaves the earlier menu-boundary autosave undeleted. Fix pinned: every exit surface branches on `mediator.is_game_over` — delete (swallow OSError) when true, never save; save only when state is PAUSE_MENU or (PLAYING and not game-over); the interactive game-over exits (:161/:169) are delete-safe because promotion runs earlier in the same `handle_event` call (:78 then :86-87).
- F2 MAJOR: the D-027 trigger list is self-contradictory — the only controller SystemExit sites are TITLE (:152) and GAME_OVER (:161/:169), so "all three controller exits" intersected with "never GAME_OVER/TITLE" is empty; a literal TITLE-exit save overwrites the user's real autosave with a pristine fresh-game save (probe: fresh mediators save successfully). Fix: triggers are menu entry, `exit_to_title` (save-before-release), and main's QUIT gated per F1; controller SystemExit sites save nothing.
- F3 MAJOR: menu-entry save-failure policy unspecified — an OSError from `save_game` (disk full, permissions) would crash `run_game` on Escape. Pin: swallow `(ValueError, OSError)` on every autosave trigger, enter the menu regardless, previous autosave intact by atomicity; QUIT catch includes ValueError (mid-drag QUIT reachable).
- F4 MINOR: Continue-visibility wording — render when the file exists; loadability proven at click; failure shows the notice.
- F5 MINOR: `exit_to_title` must save BEFORE `_close_pause_menu` releases the menu reason (byte-identical to the menu-entry save since nothing dispatches or advances while the menu holds — pure redundancy, deliberately).
- F6 MINOR: new seams/parameters must be optional with inert defaults (no autosave, Continue unavailable, no notice) to preserve the pinned `AppController(factory, start_state=...)` and one-argument `draw_title_screen(surface)` call sites.
- F7 MINOR: loop-level tests must patch a path seam so suites can never touch a developer's real `saves/autosave.json`; fake mediators lack gesture attributes, so un-patched QUIT-saves would raise AttributeError — tests patch the seam instead.

Confirmed sound: the complete quiescence trace at the menu boundary for all four gesture fields across every reachable armed state (creation, redraw, handle-drag, edit-selection — the only theoretical residue is unreachable); no reachable PAUSE_MENU violation of the canonical-fleet/prefix/spawn preflights; `prepare_layout` calls only position updaters and cannot clobber loader-restored button/blink state (the plan's contingency is unnecessary); the menu_screens three-site lockstep is real and all existing tests survive a third title button; the isolation scan needs no extension; no save-module edit is needed (the schema already carries "menu", `release_pause_reason` is a discard no-op, and save_load.py:128-129 explicitly assigns the reconciliation to GM-07c); atomicity, user-pause preservation, and budgets verified.
