# GM-07d review synthesis

Status: delivered as Commit A `02c7f7f` (rebased onto `origin/main` at `abe36fe`, GM-07b:D, with one additive PROGRESS.md merge and the full suite re-verified 1221/0 on the integrated tree), exact [run 29966701930](https://github.com/yanfengliu/python_mini_metro/actions/runs/29966701930) green; evidence-only Commit B active

Research (`raw/research-highscores.md`) mapped the leaderboard onto the GM-07b save infrastructure and the GM-07a/GM-07c controller and main game-over surfaces, resolving the persisted shape, the record-once policy, and the isolation obligation as D-028. The combined adversarial plan review (`raw/plan-review-combined.md`, NOT CLEAN — 0 blockers, 4 majors, 5 minors) folded every finding: the window-close record breaks an existing GM-07c test whose fake mediator lacks `deliveries` (required test edit, MAJOR-1), the load catch must be `Exception` not `(ValueError, OSError)` because a deeply nested document raises `RecursionError` (MAJOR-2), the `deliveries` read must sit inside the seam guard (MAJOR-3), and the GM-07b atomic writer is not importable so it is re-inlined (MAJOR-4). A self-verified narrow recheck (`raw/plan-recheck.md`) reconciled the MINOR-5/MINOR-8 tie-order tension as stable-over-append.

Red evidence (`red-evidence.md`): 34 tests across four new modules plus the required GM-07c contract edit and the GM-07b isolation-scan membership edit, zero collateral beyond the intended GM-07c window-close case at the 1177-test baseline, every failure a clean `require_attribute` assertion; the minted product surface was formalized from the plan with no STOP-worthy gap.

Implementation wired `src/highscores.py` (215), the controller `highscores` seam, `main`'s recorder and best indicator, and the `menu_screens.draw_best_indicator` primitive. The first review lane was one combined harness lane (`raw/implementation-review.md`, CLEAN) that ran nine standalone probes plus a real windowed run — validate/record fuzz, load START-EMPTY across corrupt/BOM/non-ASCII/forward-version and a 200k-deep `RecursionError`, atomic-writer canonical ASCII with an interrupted-write preservation, exactly-once across the promotion and window-close surfaces, a windowed "NEW BEST" over the byte-unchanged `game_renderer` overlay, and a checkpoint-unchanged recorder. It returned zero findings — but it only ever exercised recorder-generated (already-canonical) boards, which is exactly why it missed the cross-key and misuse paths the escalated lane then caught (`[[gm07d-review-coverage-lesson]]`).

Because GM-07d introduces a new persisted file format and its own atomic writer, it escalated to the persistence trigger: an external Codex read-only lane (`raw/codex.md`, NOT CLEAN, nine findings), every finding verified against the live code before acting. Four were fixed red-first: `record_score` and `save_highscores` now validate inputs and the whole board before any write, so a misuse such as `record_score(deliveries=-1)` can never persist a file that reloads empty and wipes a valid board (MAJOR-2); `record_score` caps only the recorded `(map, rulesVersion)` group, so recording one key can never drop another key's entries (MAJOR-3, a real cross-key data-loss path the recorder-only harness probes could not reach); `map`/`rulesVersion` are required keyword args, never defaulted (MINOR-3); and `main` now funnels both game-over surfaces through a single patchable `record_highscore` (MINOR-4), with a committed mediator-read-only regression test (MINOR-5a) and corrected save-writer wording across README/DECISIONS/ARCHITECTURE (MINOR-6). Three were accepted with note: the mixed-key `_exact_keys` `TypeError` is unreachable from JSON (object keys are always strings) and load-safe; the `mkstemp` fd-guard nit faithfully copies the reviewed GM-07b `save_game` writer the plan mandated parity with (a follow-up chip hardens both twins together); and the isolation AST scan matches the bare-import convention this codebase actually uses, giving `highscores` exact parity with the four save modules.

The one MAJOR held open across both Codex passes — an eventless PLAYING->GAME_OVER promotion records nothing until the next event or the window-close QUIT — is accepted as consistent with GM-07c's reviewed and deliberate D-027 resolution of exactly this frame-accurate race: the record fires at the first post-game-over event or, mirroring the D-027 autosave delete, at the state-gated window-close QUIT (both committed-tested and mutually exclusive), so a real player always triggers one of the two surfaces; only an eventless game-over terminated by a non-`pygame.QUIT` signal (an OS kill, or a truncated `max_frames` smoke run) records nothing, a cosmetic loss for a cosmetic leaderboard. Re-architecting the reviewed frame-loop promotion timing under this contained unit would add regression risk to GM-07c's mutation-pinned loop for a case D-027 already reasoned about, so a deterministic per-frame reconciliation is filed as a follow-up chip rather than folded here. A focused Codex re-review (`raw/codex-2.md`) confirmed all six fixes resolved with no regression (full suite 1216/12, `game_renderer` byte-identical, every changed file under 500 lines), holding NOT CLEAN only on the accepted MAJOR and the doc drift the fixes introduced (stale `record_score` default-args and line counts in EVIDENCE, `SAVE_RULES_VERSION` attribution in ARCHITECTURE/PLAN), all corrected in this commit.

Final local evidence: the four GM-07d modules plus the required GM-07b isolation-scan and GM-07c window-close edits are green; full py313 suite 1216 tests, 0 failures, 12 expected skips; guarded `npm test` 249/0; Ruff, format, and per-file pre-commit clean; budgets held (`highscores.py` 215, `app_controller.py` 244, `main.py` 278, `menu_screens.py` 185, `game_renderer.py` untouched at 494). No schema, protocol, observation, or frozen-artifact change; the GM-07b save API and its isolation guard are untouched. The external Claude CLI reviewer remains skipped per the recorded OAuth failure mode, with no approval claimed for it; the harness lane, the escalated Codex persistence lane with its confirming re-review, and the windowed run are the review of record. Two follow-up chips (atomic-writer fd-guard hardening, deterministic per-frame game-over reconciliation) are filed for later units.
