# GM-07d plan adversarial review — verdict: NOT CLEAN (verbatim synthesis)

0 blockers, 4 majors, 5 minors; architecture (separate highscores.py, injected seam, draw-in-main, start-empty load, per-key ranking) sound and reuse targets mostly exist.

- MAJOR-1: the new window-close record breaks `test_gm07c_run_game_loop.py::test_quit_when_game_over_deletes_and_never_saves` (:153-164) — its `_LoopMediator` (:37-49) has no `deliveries` and the test patches only write/delete autosave, not the highscores seam or `HIGHSCORES_PATH`, so the record reads a missing attr and hits the real `saves/highscores.json`. Fix: extend `_drive_window_close` and that existing test to patch `main.record_highscore` and redirect `main.HIGHSCORES_PATH` to a temp dir, and cover the is_game_over record + mutual exclusion there (a required existing-test edit).
- MAJOR-2: `(ValueError, OSError)` is narrower than the `write_autosave` `except Exception` it claims to mirror — a deep-nested leaderboard raises `RecursionError` (not a subclass of either) and escapes at the game-over critical path, crashing the game. Fix: the record wrapper (and the load path) catch `Exception` at the game-over surfaces; correct the "like the autosave writer" wording.
- MAJOR-3: the promotion must read `mediator.deliveries` ONLY inside the `self._highscores is not None` guard, or the seam-less regression tests (`test_seams_optional_...` :202-213, `_game_over_ready` :301) whose `_RecordingMediator` lacks `deliveries` error with AttributeError; adding `deliveries` to the stubs would mask the guard. Fix: pin the read inside the seam guard.
- MAJOR-4: "the exact save-local atomic writer" is not importable — it is inlined in `save_game.save_game` (:251-269) coupled to `serialize_game`; the plan's import list has no writer. Fix: pin the approach — inline mkstemp->fsync->os.replace in highscores.py (add os/tempfile, drop the "exact writer" reuse framing) rather than extracting from the reviewed save module.
- MINOR-5: pin tie/rank — append-then-stable-sort-then-truncate gives a new entry equal to the current best rank 2 / is_best False, and an empty board's first entry (even deliveries 0) rank 1 / is_best True; add a tie test.
- MINOR-6: the controller's public `game_over_result` collides in name with `mediator.game_over_result` (the unrelated handle_game_over_click return in stubs); rename to `last_highscore_result`.
- MINOR-7: pin that the promotion UNCONDITIONALLY (re)assigns the result (None when seam absent / no record) so a restart on the reused controller cannot show a stale best.
- MINOR-8: `canonical_save_bytes` does not sort array elements, so `entries` order depends on insertion history; pin a canonical array order (sort by map, rulesVersion, deliveries desc, stable) for GM-09 determinism.
- MINOR-9: start-empty on a forward-version file then record->save overwrites a newer leaderboard — an accepted consequence of the cosmetic stance; surface it.

Confirmed sound: reusable symbols all exist and import (`_object`/`_array`/`_string`/`_int`/`_nonnegative_int`/`_exact_keys`, `canonical_save_bytes`, `SAVE_RULES_VERSION="rules-v1"`, the dup-key `object_pairs_hook`); `mediator.deliveries` is a side-effect-free int property; adding `"highscores"` to `SAVE_MODULE_NAMES` correctly forbids it in the four headless surfaces + rl (main/controller unscanned, so main importing highscores and highscores importing save modules are both fine); `game_renderer._draw_game_over` stays untouched so the exact-overlay assertions hold and the main post-frame indicator composites over the overlay; exactly-once across the two surfaces is robust (promotion sets GAME_OVER so the window-close gate then fails; QUIT raises SystemExit before any later handle_event); deliveries already in the checkpoint so the save-v1 fixture and determinism suite are untouched; load tolerance covers missing/truncated/non-JSON/BOM/non-ASCII/dup-key/forward-version, the only uncovered mode being RecursionError (MAJOR-2). Interaction-gated caveat (not a defect): a run abandoned at the game-over overlay with no further event and no window close records nothing, exactly like the D-027 autosave delete.
