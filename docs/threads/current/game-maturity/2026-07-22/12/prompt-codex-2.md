You are re-reviewing the UNCOMMITTED working-tree change GM-07d (a map/rules high-score leaderboard) in a Python 3.13 pygame-ce repo. An earlier Codex pass returned NOT CLEAN; its findings are at docs/threads/current/game-maturity/2026-07-22/12/raw/codex.md. The findings have now been addressed. Your job is to adversarially VERIFY each fix against the LIVE code and check that the fixes introduced no regression. Do not trust this summary — read the live files and `git diff`.

Fixes applied (verify each against src/highscores.py, src/main.py, and the docs):
1. MAJOR-2 (invalid board could be persisted): `record_score` (src/highscores.py) now guards its inputs at entry — `_nonnegative_int(deliveries,...)`, `_ascii_string(map,...)`, `_ascii_string(rules_version,...)` — and `save_highscores` now calls `validate_highscores(document)` before writing. Verify: `record_score(empty, deliveries=-1, map="classic", rules_version="rules-v1")` raises ValueError; `save_highscores(<doc with a negative deliveries>, path)` raises and leaves a pre-existing valid file byte-intact with no .tmp litter; a bool/float/str deliveries also raises.
2. MAJOR-3 (recording one key dropped/reordered another key): `record_score` now caps ONLY the recorded `(map, rules_version)` group; entries under other keys are never dropped. Verify: an externally-valid board with 11 `beta` entries, then recording a `classic` score, keeps all 11 beta entries; cross-key isolation holds; the recorded key is still capped at 10; rank/is_best still correct. Confirm the residual (a hand-edited non-canonical other key gets re-sorted into canonical descending order, never dropped) is acceptable and not data loss.
3. MINOR-3 (silent default keys): `record_score`'s `map`/`rules_version` are now required keyword args (no defaults). Verify calling without them raises TypeError and no production caller relies on defaults.
4. MINOR-4 (two record paths, only one patchable): `main.py` now has a single `record_highscore(mediator)` that BOTH the promotion seam (via `_record_promotion` -> `record_highscore`) and the window-close QUIT path call; `_record_deliveries` is removed. Verify patching `main.record_highscore` (or `main.HIGHSCORES_PATH`) intercepts BOTH surfaces, the seam still passes the deliveries scalar, and mutual exclusion / exactly-once across the two surfaces still holds.
5. MINOR-5a (missing test): a committed test now asserts `record_highscore` reads deliveries and mutates no mediator state. Verify it is present and meaningful (test/test_gm07d_run_game_loop.py).
6. MINOR-6 (doc contradiction): README.md and DECISIONS.md D-028 now say "a save-local copy of the reviewed GM-07b atomic writer" instead of "the exact writer / avoids a parallel writer". Verify the docs now match the re-inlined writer in src/highscores.py.

Also re-open the whole change briefly for REGRESSIONS the fixes might have introduced: does `save_highscores` validating first ever wrongly reject a board `record_score` legitimately produces? Does the input guard reject any valid production input (deliveries is a nonnegative int off mediator.deliveries)? Does removing the `SAVE_RULES_VERSION` re-export from highscores and importing it into main from save_schema break any import or the isolation scan? Does the `_record_promotion` -> `record_highscore(SimpleNamespace(deliveries=...))` indirection behave identically to a direct call under monkeypatching? Any file over 500 LOC? Is `game_renderer.py` still byte-unchanged?

Accepted-with-note (do NOT re-flag unless you find they are actually reachable/harmful in THIS codebase): MINOR-1 (`_exact_keys` TypeError on mixed str/int dict keys is unreachable from JSON, which only yields string keys, and load catches Exception); MINOR-2 (the mkstemp fd-guard nit is a pre-existing property of the reviewed GM-07b `save_game` writer that highscores faithfully copies — a follow-up will harden both together); MINOR-5b (the isolation AST scan matches the bare-import convention this codebase actually uses, giving `highscores` parity with the four save modules).

Do NOT modify any file. Output: for each of the 6 fixes, CONFIRMED-RESOLVED or STILL-BROKEN with file:line + a concrete scenario; any new regression with severity; then a final VERDICT line: CLEAN or NOT CLEAN.
