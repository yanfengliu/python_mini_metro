# GM-07d map-and-rules high-score leaderboard diff ledger

Status: staged as `[GM-07d:A]`, rebased onto `origin/main` at `abe36fe` (GM-07b:D) with one additive PROGRESS.md merge and the full suite re-verified on the integrated tree; exact-head CI and evidence-only Commit B to follow

## Implemented production surface

- `src/highscores.py` (new, 215): the leaderboard document type and its constants (`HIGHSCORES_SCHEMA_VERSION = 1`, `HIGHSCORES_STATE_CONTRACT = "mini-metro-highscores-v1"`, `HIGHSCORES_PER_KEY_CAP = 10`, `HIGHSCORES_MAP_CLASSIC = "classic"`); strict `validate_highscores` (exact top-level and entry keys checked before field access, `schemaVersion` in the supported set with forward versions rejected, string map/rulesVersion, nonnegative-int non-bool `deliveries`, non-ASCII content rejected, duplicate JSON keys rejected via the shared `object_pairs_hook`); a pure `record_score(document, *, deliveries, map, rules_version) -> RecordResult` (map/rulesVersion required, never defaulted) that validates its inputs, returns a new document ranked map-asc/rulesVersion-asc/deliveries-desc with stable-over-append ties, and caps only the recorded `(map, rulesVersion)` group so other keys are never dropped, carrying the new entry's 1-based `rank` (`None` beyond the cap) and `is_best`; a START-EMPTY-tolerant `load_highscores` catching `Exception` (including `RecursionError`) so a missing, unreadable, malformed, forward-version, or pathologically nested file yields the empty document and never raises; and `save_highscores` that validates the board before writing `canonical_save_bytes` through a save-local copy of the reviewed GM-07b mkstemp->fsync->os.replace->finally-unlink atomic writer (raising on failure). Reuses the save-schema scalar validators and canonical bytes, so it joins the persistence isolation set.
- `src/app_controller.py`: one optional inert `highscores` recorder seam beside `autosave`; at the PLAYING->GAME_OVER promotion the controller reads `mediator.deliveries` only inside the `self._highscores is not None` guard and (re)assigns the result to a public `last_highscore_result`, clearing it to `None` when the seam is absent or returns `None`; the controller mutates no mediator state and a seam-less controller is behaviorally identical to pre-GM-07d.
- `src/main.py`: a patchable module-level `HIGHSCORES_PATH` and a single `record_highscore(mediator)` — wrapping load->`record_score`->save with a best-effort `Exception` swallow — that BOTH game-over surfaces funnel through (the promotion seam via `_record_promotion`, resolved at call time so patching the one symbol intercepts both; and the window-close race), the `highscores` seam bound to `AppController`, the window-close record gated identically to the autosave delete (`state in (PLAYING, PAUSE_MENU)` and `is_game_over`) and mutually exclusive with the promotion, `SAVE_RULES_VERSION` imported from `save_schema`, and a best-indicator draw from `controller.last_highscore_result` after the renderer's game-over frame.
- `src/ui/menu_screens.py`: a public byte-stable `draw_best_indicator(surface, result)` painting a "new best" banner only when `result.is_best`.
- No schema, observation, action, checkpoint, protocol, or frozen-artifact change; `game_renderer.py` is untouched at 494 lines; the GM-07b save API and its isolation guard are unchanged (the isolation AST scan is extended to name `highscores`).

## Implemented evidence surface

- Reconcile GM-07c Commit B run `29956050082` (#160, `build` `89045272141`, `rl-smoke` `89045272126`) and the out-of-scope follow-up `abe36fe` (GM-07b:D) now on `origin/main`; record D-028 (map/rules high-score leaderboard, start-empty-on-corrupt, record-once-at-game-over) in the parent decision log.
- Research lane; combined adversarial plan review (0 blockers, 4 majors, 5 minors) folded; a self-verified narrow recheck reconciling the tie-order semantics; a 34-record red baseline with zero collateral beyond the intended GM-07c edit; a harness implementation-review lane (CLEAN, nine probes plus a windowed run — but recorder-only boards); an escalated external Codex persistence lane (`raw/codex.md`, NOT CLEAN, nine findings — six fixed red-first with five added tests, one MAJOR accepted per D-027 with a follow-up, three minors accepted with note); and a Codex re-review (`raw/codex-2.md`) confirming every fix resolved with no regression - all preserved under `raw/`.
- Update `README.md` (high-score leaderboard + save location + writer wording), `GAME_RULES.md` (record-once-at-game-over line), `ARCHITECTURE.md` (`highscores` module + seam + `SAVE_RULES_VERSION` ownership), and `PROGRESS.md`.

Local gates: the four GM-07d modules (including the five fold tests) plus the required GM-07b isolation-scan and GM-07c window-close edits green; full py313 suite 1216/0 with 12 expected skips; guarded `npm test` 249/0; Ruff and per-file pre-commit clean; budgets held. Commit A rebases onto `origin/main`, re-verifies, then pushes; evidence-only Commit B follows the exact remote workflow.
