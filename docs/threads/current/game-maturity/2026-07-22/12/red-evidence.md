# GM-07d red evidence

Captured at the GM-07c-finalized baseline (`db9d4fb`, integrated with GM-07b:C), before any production change.

- `test/test_gm07d_highscores.py` (377 lines): 20 tests, all red — constant pins; `validate_highscores` accept/reject matrix (forward version, wrong contract, unknown/missing top-level and entry keys, non-string map/rules, negative/non-int/bool deliveries, duplicate keys); `record_score` purity, canonical (map, rules, deliveries-desc, stable-over-append) order, per-key cap 10, cross-key isolation, and the pinned tie/empty-board rank/is_best semantics; `load_highscores` START-EMPTY on missing/truncated/BOM/non-ASCII/duplicate-key/forward-version/deep-nested (RecursionError) and strict-accept on valid; `save_highscores` canonical ASCII bytes + atomic interrupted-write preservation.
- `test/test_gm07d_recorder_controller.py` (241 lines): 4 tests, 2 red + 2 guards — the controller `highscores` seam records exactly once at the promotion into `last_highscore_result`; the seam-less guard (mediator lacking `deliveries`) reads no `deliveries` and stays None (MAJOR-3); the both-seams-absent baseline guard.
- `test/test_gm07d_run_game_loop.py` (210 lines): 6 tests, 3 red + 3 guards — main's QUIT records exactly once when un-promoted game-over, is mutually exclusive with the promotion, records nothing at TITLE/GAME_OVER, and swallows a RecursionError from an inner dep (the real `record_highscore` with `main.load_highscores` patched to raise).
- `test/test_gm07d_menu_screens.py` (164 lines): 4 tests, 3 red + 1 guard — a new byte-stable `draw_best_indicator` primitive drawn only on an is_best result, and the isolation-scan membership of `highscores` in `SAVE_MODULE_NAMES`.
- Required existing-test edit (plan MAJOR-1): `test/test_gm07c_run_game_loop.py::test_quit_when_game_over_deletes_and_never_saves` now patches `main.record_highscore` and redirects `main.HIGHSCORES_PATH` to temp so the new window-close record neither errors on the fake mediator's missing `deliveries` nor touches a real `saves/highscores.json`; red until the product exists, the other four cases stay green.

Combined: full suite `Ran 1211 tests, failures=36, errors=0, skipped=12` — baseline 1177 + 34 new; every FAIL is `test_gm07d_*` or the one intended `test_gm07c_run_game_loop` edit; zero other collateral (independently verified by the parent session).

Six plan-underspecifications formalized during authoring: a private `highscores._reject_duplicate_keys` object_pairs_hook (dup keys are impossible on an already-parsed dict, so rejection is at parse time mirroring `save_load`); strict ASCII decode in `load_highscores` (so a non-ASCII byte starts empty); the RecursionError swallow tested by patching an inner dep while running the real `record_highscore`; `RecordResult` = `.document/.rank/.is_best`; `draw_best_indicator(surface, result)` gating on `result.is_best`; and `highscores` cross-imported into the isolation scan set. `save_highscores` RAISES on failure (the swallow lives at the main layer); the isolation-scan `SAVE_MODULE_NAMES += "highscores"` is the implementer's green-phase edit.

Ruff check/format and per-file pre-commit clean on all five modules; line counts 377/241/210/164.
