## Findings

1. BLOCKER — [src/game_session.py:87](C:/Users/38909/Documents/github/python_mini_metro/src/game_session.py:87), [src/game_clock.py:43](C:/Users/38909/Documents/github/python_mini_metro/src/game_clock.py:43), [PLAN.md:25](C:/Users/38909/Documents/github/python_mini_metro/docs/threads/current/game-maturity/2026-07-24/27/PLAN.md:25) — pause-invariance is false through the real `GameSession`. Pausing resets both backlog and the `17,17,16` cadence phase. Live counterexample: two ticks at 1×, then 300 at 4× reaches `(steps=1202,time_ms=20034)`. After the week pause and one resumed 4× tick, the paused run reaches `(1206,20102)` while the never-paused control reaches `(1206,20098)`. Movement and passenger waits can therefore diverge at identical `steps`, invalidating the no-trajectory-change and no-version-bump premise. Fix: preserve the cadence phase corresponding to applied ticks while discarding pause backlog, including partial `advance`/`advance_exact` batches; otherwise classify the mechanic as trajectory-affecting and apply D-026’s versioning consequences. Add this exact mixed-speed regression.

2. BLOCKER — [src/rl/player_env.py:142](C:/Users/38909/Documents/github/python_mini_metro/src/rl/player_env.py:142), [src/game_session.py:62](C:/Users/38909/Documents/github/python_mini_metro/src/game_session.py:62), [src/rl/protocol.py:318](C:/Users/38909/Documents/github/python_mini_metro/src/rl/protocol.py:318) — `env.py` auto-resolution does not reach `PlayerPixelEnv`, the official RL boundary. At constant 1×, decision 200 reaches step 1200 and every later decision applies zero ticks because no pixel action releases `"week"`. Worse, after 199 default decisions (`steps=1194`), pressing 4× crosses on internal tick two of the next six-tick action; `GameSession` drops the other four ticks, ending at 1202 instead of the contractually expected 1218. Resolving after `advance_exact` is too late. Fix: define an explicit policy for every non-offer shell that resolves synchronously before `GameSession` observes the hold, or consumes the exact remaining tick batch. Test constant and mixed-speed crossings, pixels, `steps`, `time_ms`, and pending state.

3. MAJOR — [src/mediator.py:729](C:/Users/38909/Documents/github/python_mini_metro/src/mediator.py:729), [src/fleet_management.py:368](C:/Users/38909/Documents/github/python_mini_metro/src/fleet_management.py:368), [PLAN.md:10](C:/Users/38909/Documents/github/python_mini_metro/docs/threads/current/game-maturity/2026-07-24/27/PLAN.md:10) — the specified boundary-check placement interrupts an existing tick before queued-return settlement. I reproduced the state from `test_gm06b_fleet_queue.py:155`: an empty queued Metro reaches its real station at step 1200, then an immediately held pause makes `FleetManagement.settle` return early, leaving the Metro assigned; baseline removes it that tick. That changes inventory exposed between actions. Fix: compute the crossing early if desired, but hold `"week"` only after the existing post-passenger `_drain_and_settle_queued_returns()` completes. Pin this queued-arrival boundary.

4. MAJOR — [src/main.py:235](C:/Users/38909/Documents/github/python_mini_metro/src/main.py:235), [src/app_controller.py:394](C:/Users/38909/Documents/github/python_mini_metro/src/app_controller.py:394), [src/tutorial.py:19](C:/Users/38909/Documents/github/python_mini_metro/src/tutorial.py:19) — the tutorial is another direct `Mediator`/`GameSession` shell, but planned reconciliation accepts only `PLAYING`. Around step 1200 it freezes permanently; `"week"` can also falsely satisfy the tutorial’s pause predicate. This breaks the named “sim never freezes and no step can soft-lock” contract. Fix: auto-resolve/suppress week holds for `TUTORIAL`, or support an OFFER that returns to its origin screen. Strengthen the existing 90-second tutorial test to assert continuing steps across multiple weeks and no pending reason.

5. MAJOR — [src/app_controller.py:233](C:/Users/38909/Documents/github/python_mini_metro/src/app_controller.py:233), [src/app_controller.py:270](C:/Users/38909/Documents/github/python_mini_metro/src/app_controller.py:270), [PLAN.md:18](C:/Users/38909/Documents/github/python_mini_metro/docs/threads/current/game-maturity/2026-07-24/27/PLAN.md:18) — asynchronous OFFER promotion lacks gesture cancellation and control arming. Concrete state: a mouse-down starts route creation, that frame crosses the boundary, and OFFER intercepts the later mouse-up. Continue can dismiss on that bare gameplay release, or play resumes with stale `is_mouse_down`/draft state. Fix: dispatch the established off-viewport mouse-up before switching screens and require an OFFER-local down/up pair for Continue. Cover creation, redraw, handles, and resource-button gestures.

6. MAJOR — [src/passenger_flow.py:193](C:/Users/38909/Documents/github/python_mini_metro/src/passenger_flow.py:193), [src/app_controller.py:173](C:/Users/38909/Documents/github/python_mini_metro/src/app_controller.py:173), [src/main.py:317](C:/Users/38909/Documents/github/python_mini_metro/src/main.py:317) — simultaneous terminal/week precedence is unspecified. Reproduced state: `steps=1199`, one rider at `wait_ms=983`, limit 1000, threshold 1, `dt_ms=17` produces `steps=1200`, game over, and a pending week. If week reconciliation runs first, `OFFER` prevents the PLAYING-only game-over promotion; QUIT then bypasses high-score recording and autosave deletion. Fix: game-over reconciliation must run first, and week reconciliation must reject terminal mediators. Add the simultaneous-boundary test.

7. MINOR — [src/save_game.py:228](C:/Users/38909/Documents/github/python_mini_metro/src/save_game.py:228), [src/main.py:313](C:/Users/38909/Documents/github/python_mini_metro/src/main.py:313), [README.md:66](C:/Users/38909/Documents/github/python_mini_metro/README.md:66) — deferring OFFER persistence is internally safe but temporarily breaks the documented window-close promise. An existing autosave survives unchanged; a run with no prior autosave closes from OFFER with nothing Continue can load. Also, with OFFER outside the current QUIT gate, the claimed “guard + swallow” never runs. Fix: choose and test one path explicitly, cover both an existing sentinel save and no prior save, and document that closing during OFFER resumes only the previous autosave, if any, until GM-10h.

8. MINOR — [src/main.py:108](C:/Users/38909/Documents/github/python_mini_metro/src/main.py:108) — OFFER is absent from the audio consumer’s gameplay-screen allowlist. A delivery/unlock on the boundary tick plays only after Continue, or is lost on QUIT. Fix: either consume/play deltas during OFFER or consume them silently, and test the chosen timing.

## No findings

- Derived `week_index` is sufficient: a mid-week save loads into the correct next crossing, while a resolved exact-boundary save does not re-fire. Blocking pending weeks keeps `"week"` out of the save vocabulary.
- Supported 1×/2×/4× arithmetic neither skips nor double-holds a boundary.
- `"week"` composes correctly with `"user"`/`"menu"`; Space and speed controls cannot release it.
- The observation sibling does not disturb the checkpoint’s exact fleet-key whitelist. Recursive and agent play already inherit `MiniMetroEnv._complete_step`; they need boundary regressions, not driver changes.
- `1200` is defensible as a provisional foundation value; escalation can remain GM-11.
- Mediator orchestration is the right ownership layer, provided the hold occurs after the whole tick.

Severity summary: BLOCKER 2; MAJOR 4; MINOR 2; NIT — no findings.

This was read-only. Three independent internal reviewers examined the live code, and I reproduced the clock, settlement, and simultaneous-terminal failures. External CLI egress was denied, so no external lane ran and no files changed. Prior roadmap memory was used only for continuity; all conclusions were reverified live.

REVISE
