===BEGIN-REVIEW===

Verdict: **FIX-FIRST**

1. **[BLOCKER] Stored v4 offers are load-stable but not re-save-stable across the GM-11 changes they are designed to survive.** The schema intentionally does not pin `OFFERS_PER_WEEK` (`src/save_schema.py:188-215`) and loading restores stored kinds verbatim (`src/save_load.py:406-431`), but every serialization re-derives using current rules and demands equality (`src/save_game.py:131-148`). Reproduced with the untouched pending fixture: after changing `OFFERS_PER_WEEK` from 2 to 1 in memory, its stored `["tunnel","new_line"]` loads correctly, then `serialize_game` rejects it against `["tunnel"]`. A second mid-offer QUIT silently swallows that failure (`src/main.py:154-161,331-337`). The prior autosave remains intact, but a valid loaded v4 state cannot be rewritten. **Fix:** record loaded-offer provenance and validate that the tuple remains unchanged from its loaded snapshot; retain canonical re-derivation only for newly generated live boundaries. Add an old-rules fixture → load → re-save → reload test.

2. **[MAJOR] The full validation/version matrix is not strongly tested.** Production behavior is correct at `src/save_load.py:63-76`, but the grown-fleet tests now serialize v4 (`test/test_gm10h_persistence.py:53-77`, `test/test_gm10i_pending.py:76-93`). Both a V4-only relaxation and an incorrect v1/v2 relaxation survived all 32 GM-10h/i tests. Removing unknown-kind, duplicate-kind, or week-with-empty validation at `src/save_schema.py:196-210` also left all 16 GM-10i tests green. The pending River fixture is called frozen but only receives a semantic load assertion (`test/test_gm10i_pending.py:193-204`), unlike the byte-pinned Classic fixture (`test/test_gm07b_save_determinism.py:401-410`). **Fix:** add table-driven v1–v4 fleet/map/tunnel/pause cases, direct unknown/duplicate/empty pending-offer rejects, and LF/length/SHA/idempotence pins for the River fixture.

3. **[MAJOR] The OFFER+QUIT test does not prove persistence.** Its fake reports `pending=True` while defaulting to `current_offers=()` (`test/test_gm10a_calendar.py:393-433`), a state the real serializer rejects, and replaces `write_autosave` with a list append (`test/test_gm10a_calendar.py:462,565-582`). It proves “call the seam without resolving,” not that loadable v4 bytes were written—important because production deliberately swallows write failures. **Fix:** drive a genuine boundary or loaded pending fixture through QUIT with a redirected `AUTOSAVE_PATH`, then load the resulting file and compare pause reason and ordered offers.

4. **[MINOR] Malformed live offer state produces accidental exceptions.** `current_offers=None` raises `TypeError`, while `("bogus",)` raises `AttributeError` because diagnostics assume `.kind.value` (`src/save_game.py:139-153`). Atomic safety holds, but this violates the repository’s actionable-`ValueError` error contract. **Fix:** validate the container and elements explicitly and format malformed values safely; add both cases.

5. **[MINOR] Documentation still contradicts v4 behavior.** `src/save_game.py:1-8,291-294` says schema v3; `src/mediator.py:171-175` says offers are transient/re-derived; `src/weekly_offers.py:61-64` still identifies window close as a forced resolve; and `README.md:230-234` omits the week pause and says only v3 accepts grown fleets/current support. **Fix:** update these contracts and mark historical schema descriptions clearly.

Confirmed correct:

- The production v4 capability matrix is complete: exact keys (`src/save_schema.py:115-131`), v4-only week vocabulary (`:81-84,233-239`), map/tunnel dispatch (`:375-385`), and v3/v4 fleet relaxation (`src/save_load.py:63-76`). No other save-version gates were found.
- For same-build live boundaries, one derivation source feeds both hold and integrity guard (`src/weekly_offers.py:40-58`, `src/mediator.py:736-739`, `src/save_game.py:142`). Serialization finishes validation before any directory/temp/write/replace effect (`src/save_game.py:296-300,362-392`).
- Load order is correct: map, RNG and scalars—including steps/pause state—are restored before map legality and pending offers (`src/save_load.py:378-403`). Restoration preserves order and never invokes derivation (`:406-431`). Week+game-over and unbounded-map TUNNEL fail closed (`src/save_schema.py:211-215`, `src/save_load.py:421-429`). No offer-count constant is imported by the loader.
- `_validate_pending_offers` correctly enforces known, distinct kinds and non-empty iff week, rejects finished games, and does not pin the count (`src/save_schema.py:188-215`).
- Frozen v1/v2/v3 fixtures remain unchanged and load; v4 Classic differs from v3 only by `schemaVersion` and `pendingOffers:[]`, and its bytes/hash are pinned (`test/test_gm07b_save_determinism.py:390-444`). The HEAD/v3 validator rejected both v4 fixtures as unsupported.
- Headless/RL defaults remain calendar-free (`src/mediator.py:166-175`, `src/weekly_offers.py:38-46`); the new test confirms empty pending state (`test/test_gm10i_pending.py:244-252`). Recursive-checkpoint code/schema has no GM-10i diff.
- `main.py`’s only behavioral QUIT change is removing forced resolution before autosave (`src/main.py:331-347`). Validation plus atomic replacement prevents an inconsistent file.
- All eight specifically requested mutants turned red: V3-only fleet gate, missing v4 key, week allowed in v3, re-derive-on-load, removed integrity guard, removed week+game-over reject, removed unbounded-TUNNEL reject, and accepted forward v5.

Validation: 46/46 core GM-10i/GM-10h/schema tests passed. The larger focused run was 97/100; the remaining three errored solely because the read-only environment exposed no writable temporary directory. Ruff check/format and `git diff --check` passed. No files were modified by this review.

===END-REVIEW===
