CLEAN — no unresolved findings in the assigned final GM-04b provenance/publication correctness scope.

Resolved during this review:

- [Medium, artifact consistency] `writeSourceStateArtifacts` formerly wrote `source-diff.patch` before exclusively creating `source-state.json`, so a dirty retry against an existing clean state could fail with `EEXIST` only after leaving a contradictory patch. The writer now reserves `source-state.json` with `wx` before any optional patch write and fills that owned handle afterward at `scripts/source-provenance.mjs:143-167`; the exact dirty-over-clean regression at `test/source-provenance.test.mjs:317-350` proves refusal leaves the original clean state and no patch.
- [High, publication integrity] Regular-file hard links formerly let a post-link source mutation change the destination through the shared inode, allowing source/destination equality to pass; an attached `.git/HEAD` at the expected commit also passed the later provenance-only check. Publication now uses only `COPYFILE_EXCL` at `scripts/civ-engine-setup-promotion.mjs:100-111`, the final comparison rejects source/destination divergence at `:137-204`, and the orchestrator reauthenticates the complete final-path checkout before root installation at `scripts/civ-engine-setup.mjs:127-147`. The regression at `test/civ-engine-setup-promotion.test.mjs:208-243` proves source mutation cannot alter the copied destination and is rejected, while `test/civ-engine-setup-contract.test.mjs:269-297` proves a failed final-path audit retains the transaction and stops before root installation.
- [Documentation] Iteration-4 `PLAN.md:39`, `ARCHITECTURE.md:297`, and `README.md:32` now describe exclusive-copy publication and final-path reauthentication rather than hard-link publication. The synthesis's stale live-sounding engine-module count is corrected from 482 to the current 496 lines; the historical parent-recorded raw ledger remains unchanged.

## A — provenance capture, artifacts, and start/end comparison: CLEAN

- Direct engine captures and enclosing source captures are registered in module-private WeakMaps, recursively reject accessors, recursively freeze their complete graphs, and compare against structured-clone snapshots before policy use: `scripts/source-provenance-engine.mjs:372-426` and `scripts/source-provenance.mjs:386-417`.
- Dirty provenance requires two consecutive identical complete state-plus-diff candidates within the bounded capture loop at `scripts/source-provenance.mjs:71-118`; the transient-diff regression remains green.
- Artifact serialization clones only an intact registered capture, validates the patch digest/contract, reserves the state name before patch creation, and rechecks capture identity after writes at `scripts/source-provenance.mjs:120-173`.
- Recapture authenticates the original fresh object before and after the asynchronous end capture, compares immutable portable summaries carrying local tree/status/diff identity plus the engine summary, and preserves both snapshots on mismatch at `scripts/source-provenance.mjs:208-247`.
- `test/civ-engine-provenance-immutability.test.mjs` passed 1/1. The adjacent `source-provenance` and `civ-engine-provenance` suites passed 29/29, including immutable policy/write/recapture, repeated state-plus-diff agreement, dirty-over-clean refusal, local drift, engine drift, physical identity, Git metadata, attributes/excludes, and categorical unavailable-state coverage.

## B — transaction-side promotion claim, equivalence, and cleanup: CLEAN

- Promotion exclusively creates the final directory, records its physical `dev`/`ino` plus the transaction token in a physical transaction-side claim, and rechecks both destination and claim identity/content throughout publication at `scripts/civ-engine-setup-promotion.mjs:5-88,206-252`.
- Directories use exclusive `mkdir`, files use exclusive copies with no shared inode, and contained links are remapped through exclusive symlink creation; every `EEXIST` winner is preserved rather than replaced at `scripts/civ-engine-setup-promotion.mjs:91-135`.
- Final equivalence compares complete names, directory/file/link types, directory and file permission modes, file bytes, and resolved remapped link targets before the last claim check at `scripts/civ-engine-setup-promotion.mjs:137-204`.
- Every post-claim error is tagged to preserve the source transaction and partial destination, while pre-claim refusal leaves the destination winner untouched and permits ordinary owned-transaction cleanup at `scripts/civ-engine-setup-promotion.mjs:19-63,276-290`. `scripts/civ-engine-setup.mjs:127-180` additionally retains the transaction on final-path reauthentication failure, releases only the owned lock, and does not begin root installation after that failure.
- `test/civ-engine-setup-promotion-cleanup.test.mjs` passed 2/2. Promotion, setup-contract, setup-operations, and setup-safety coverage passed 42 tests with 3 expected Windows-hosted POSIX-only skips, including no destructive namespace calls, claim-write failure retention, destination and child races, source-mutation isolation, mode/byte/link equivalence, claim tamper, post-promotion audit ordering, two-level cleanup ownership, and external-junction preservation.

## Documentation and validation

- `AGENTS.md`, `README.md`, `ARCHITECTURE.md`, and iteration-4 `PLAN.md` agree on immutable capture graphs, exclusive-copy no-replace publication, the transaction-side claim/manual recovery boundary, final-path reauthentication, marker-free success, and retained post-claim failure evidence.
- Integrated command: `node --test test/civ-engine-provenance-immutability.test.mjs test/source-provenance.test.mjs test/civ-engine-provenance.test.mjs test/civ-engine-setup-promotion.test.mjs test/civ-engine-setup-promotion-cleanup.test.mjs test/civ-engine-setup-contract.test.mjs test/civ-engine-setup-operations.test.mjs test/civ-engine-setup-safety.test.mjs` — 72 passed, 3 expected Windows-hosted POSIX-only skips, 0 failed.
- Syntax checks passed for the five reviewed production modules. The reviewed production and test files are all below 500 lines; `scripts/source-provenance-engine.mjs` is 496 lines and `test/source-provenance.test.mjs` is 494 lines.
- No review probe, setup lock, or setup transaction artifact remains.

This is an independent in-process correctness review of the assigned A/B scope. The fleet-pinned external multi-CLI workflow remains unavailable at the established repository-export authorization boundary, so no external review or approval is claimed. Parent-owned complete repository validation remains pending.
