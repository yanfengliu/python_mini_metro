Review complete with 3 findings:
1) HIGH (sent already): unrestricted `npm test` argv becomes executable Node CLI surface (`guard.mjs:16,60,74-79`; current test `civ-engine-guard.test.mjs:10-63` blesses it). Live `--import=data:` proof executed sentinel. This refutes fixed-body/untrusted-argv boundary.
2) MEDIUM: lease ownership is checked only before verification (`civ-engine-setup.mjs:203-216`) and next checked only during release (`:219-223`); guard launches immediately after verify (`civ-engine-guard.mjs:73-79`). If the token file is removed/replaced during verification, body executes under a lost lease and failure is discovered only after child effects. `test/civ-engine-setup-lease.test.mjs:29-36` explicitly expects only one ownership event. Add a post-verification ownership assertion (and RED mutation fixture) before returning to guard; acknowledge token lock is advisory during the child.
3) MEDIUM diagnostics: the public npm wrapper itself echoes untrusted forwarded argv before the categorical guard error. Live proof `npm.cmd run playtest:recursive --ignore-scripts -- --unknown secret-argv-sentinel` printed the entire expanded line containing `secret-argv-sentinel`, then `[civ-engine-guard] unknown argument`. Evidence package.json:14-16. This contradicts the public no-secret diagnostic claim unless scope explicitly excludes npm's lifecycle prelude; tracked npm loglevel/silent policy or corrected claim/test needed.
Pass evidence: 13/13 `civ-engine-guard`, `recursive-args`, and setup-lease tests; syntax checks for guard/parser/playtest passed. `--ignore-scripts` live invocation confirmed npm still executed the actual guard. Child exit/error/finally ordering is otherwise sound; direct parent-signal crash-left lock matches documented recovery policy; Windows self SIGINT/SIGTERM test exits 1 as expected.
