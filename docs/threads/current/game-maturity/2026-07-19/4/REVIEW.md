# GM-04b review synthesis

Status: Commit A failed exact remote setup on npm 10 omitted-development namespace residue; corrective Commit A2 is locally complete with two independent in-process `CLEAN` re-reviews plus all focused/full runtime, static, dependency, hook, and exact cached-staging gates green, while commit, push, and exact remote CI remain pending; the fleet-pinned external workflow remains unavailable and no external approval is claimed

## Baseline

GM-04a is remotely finalized at evidence-only Commit B `28a6c7e6082f03a0590fb27d99996837575d5062`, whose exact workflow run `29731075431` passed. The retained pin is a physical clean detached checkout at the descriptor commit, root package resolution points to it, and the unrelated sibling remains outside the setup boundary.

## Initial blockers

The supply-chain lane found that wrapping `npm.cmd` in `cmd.exe` reintroduces metacharacter parsing, credential/config denylists miss many injection surfaces, locking covered only missing-pin creation, a single staging directory could not carry an ownership marker while remaining an empty clone destination, verify-only Git reads could write the index, and broad wrong-resolution repair could mutate before discovering a nested shadow.

The test/documentation lane proved npm `ignore-scripts` can suppress pre-hooks while still executing a requested script body, so hooks alone are not a security boundary. It also required an explicit strict/canary sentinel matrix, real Windows execution of the missing-pin setup, exact ignored lock/staging crash behavior, and complete npm behavior-variable isolation.

The provenance lane found that current Git status/rev-parse calls occur before local metadata/config is proven safe, allowing fsmonitor/config/alternates indirection and optional index writes. It also required a physical npm tool derivation, recursively checked generated-tree boundaries, and reconciliation of older current-plan text that still forecast `npm.cmd`.

## Accepted disposition

D-017 records the corrected boundary: after a trusted top-level npm/Node bootstrap, every canonical public Node body enters the dependency-light guard even when lifecycle hooks alone are suppressed, while setup validates the npm distribution and launches `npm-cli.js` through exact `process.execPath` without `cmd.exe` on both supported platforms. The setup and guard mains share post-start taint detection, pre-hooks remain defense in depth, and no claim is made that Node code can prevent or undo pre-main bootstrap execution.

The accepted state machine locks every mutating setup, uses a two-level token-owned transaction parent and empty checkout child, applies a filesystem-only Git metadata audit before sanitized no-optional-lock reads, uses minimal allowlisted child environments, recursively checks generated paths, creates only a missing exact root dependency link, dogfoods missing-pin setup on Ubuntu and Windows CI, and documents exact manual recovery for crash-left ignored artifacts.

The older GM-04a plan/review forecast now explicitly says D-017 supersedes `npm.cmd`. The three plan-review lanes had converged clean before implementation; final implementation re-reviews remain pending. The fleet-pinned external workflow remains unavailable at the established repository-export authorization boundary, so no external approval is claimed.

## Implementation review

The test-only phase added six focused suites plus two fixture helpers, all below 500 lines and syntax-clean. Their combined baseline registered 11 tests with five passing and six expected failures: four missing production-module loader boundaries (`civ-engine-guard.mjs`, `civ-engine-setup.mjs`, `civ-engine-setup-process.mjs`, and `civ-engine-setup-safety.mjs`, with the process boundary reached from two suites) plus the not-yet-wired `setup:civ-engine` package command.

The first production implementation reached 52/52 focused setup tests and 106/106 complete Node tests, and independent local reproduction also passed strict verification, two no-op setup runs with an unchanged pin summary, 582 core Python tests with 12 expected optional-RL skips, and 585/585 exact-RL tests. Those green results did not override substantive review findings.

The first adversarial implementation lane found four high-severity blockers: Git status did not authenticate existing checkout bytes against the pinned tree; clone could consume unaudited parent-repository Git config and templates before destination audit; root package/lock validation did not constrain npm's complete install graph; and the reviewed build still delegated through npm lifecycle executable search. It also found two medium-severity blockers: guarded verification released its setup exclusion before the child completed, and lexical `--allow-dirty` detection mistook an option value for canary authorization. All six are accepted and TDD-fixed through complete non-generated `HEAD` byte authentication, isolated no-checkout clone trust, strict root install-graph validation, direct pin-local TypeScript execution, child-lifetime verification leases, and shared parser-aware canary selection. The raw review is preserved in `raw/implementation-supply-chain.md`; final implementation re-review remains pending after complete revalidation.

The guard re-review found one high-severity defect and two medium-severity defects. The unrestricted `npm test` forwarding surface and the missing post-verification lease-ownership assertion are accepted and TDD-fixed: the focused guard/parser/lease run moved from the expected 12/15 RED state to 15/15 green, and the expanded guard/parser/playtest/lease set passed 24/24. The npm-wrapper echo finding is also accepted and implemented through the exact tracked `loglevel=silent` root policy plus fail-closed root-contract validation; a live `npm.cmd test --ignore-scripts` invocation with a rejected `--import=data:` probe exited 1 and emitted only `[civ-engine-guard] public test command accepts no arguments`, without npm's expanded script line. The full review is preserved in `raw/implementation-guard-rereview.md`; all three dispositions await final adversarial re-review and complete validation before approval.

The final guard review independently passed the 24/24 expanded guard/parser/playtest/lease set and the 13/13 pin/setup-contract set and found all three direct corrections clean. It then found one remaining high-severity bootstrap-boundary issue: npm `--node-options`, ambient `NODE_OPTIONS`, or direct Node startup options can execute code before the guard module starts, so a Node guard cannot repair or attest those pre-main effects. The exact finding and live probe are preserved in `raw/implementation-guard-final.md`. The finding is accepted through explicit trust of the tracked package/`.npmrc`, selected top-level npm/Node executables, and pre-start environment/configuration; shared setup/guard assertions now detect non-empty `NODE_OPTIONS` or `process.execArgv` after startup and categorically stop later entry-point effects without reflecting values, while documentation expressly excludes caller-selected bootstrap overrides from attestation. The startup TDD set moved from the expected 22/24 RED state to 24/24 green; the combined guard/setup-contract/parser/playtest/lease set then passed 33/33 and the updated pin/setup-contract set passed 14/14. Exact live assertions proved that clean canonical npm reaches the fixed test-argv rejection, while npm `--node-options`, ambient `NODE_OPTIONS`, direct guard `process.execArgv`, and direct setup `process.execArgv` each exit 1 with only the categorical startup refusal before entry-point work. Final approval remains pending re-review of this disposition and complete validation.

The fresh final guard re-review found and accepted two medium-severity integration/documentation defects: the copied setup fixture omitted the new startup-contract import, and the synthesis overstated pending implementation re-reviews as already clean. The fixture dependency closure and reviewer-status wording are corrected, its 24/24 guard/setup-contract/lease/parser set and 14/14 setup-operations set pass, live startup/argument probes remain categorical and non-reflective, and the lane is now `CLEAN`; the exact review is preserved in `raw/implementation-guard-final-rereview.md`. The trusted pre-main bootstrap and advisory out-of-band lock limitations remain explicit, no external review is claimed, and overall approval stays pending the final supply-chain/provenance lanes and complete validation.

The provenance implementation re-review found four high-severity blockers: status-cache weakening could hide a same-length timestamp-restored source replacement, root Git failure diagnostics reflected invalid config values, engine attributes could activate and disclose a configured filter, and unavailable engine summaries copied arbitrary exception messages. A related supply-chain re-review also found that the default repository-owned `.git-read-home` exposed repo-controlled global config and `$HOME/.config/git/attributes`. All five findings are accepted and TDD-fixed through semantic config rejection, an independent relevant-source `HEAD` byte/OID crosscheck, categorical Git and unavailable-state diagnostics, per-call engine attribute authentication, OS-null default Git config/home/hooks, disabled system attributes, and inherited HOME/XDG/config isolation. The focused provenance/guard regression set passed 111 tests with one expected POSIX-only skip; the accepted finding substance is preserved as a clearly labelled parent-recorded ledger in `raw/implementation-provenance-rereview.md`, not as verbatim reviewer output. Fresh final provenance and supply-chain re-reviews plus complete validation remain pending before approval.

The fresh final provenance re-review remained `BLOCKED` on one high-severity and three medium-severity findings: active engine `.git/info/exclude` patterns could conceal untracked files, a stable A state could be returned with a transient B diff, the outer source state lacked the engine capture's mutation/forgery protection, and filesystem config rejection still reflected repository-controlled identifiers; it also required `scripts/source-provenance-engine.mjs` to return below the preferred 500-line boundary. All findings are accepted and TDD-fixed locally through per-call categorical engine exclude rejection, two consecutive identical stable state-plus-diff candidates, outer WeakMap structured-clone integrity at policy/write/recapture boundaries, one fixed config-rejection category, and relocation of safe unavailable-reason mapping into the engine safety module. The selected TDD run established the expected 0/33 RED state, the expanded corrected set passed 36/36, and the integrated provenance/process regression passed 116 tests with one expected POSIX-only skip; all affected production and test modules remain below 500 lines, including the subsequently hardened 496-line `source-provenance-engine.mjs`. The finding substance and dispositions are appended to the clearly labelled parent-recorded ledger in `raw/implementation-provenance-rereview.md`; fresh final provenance re-review and the parent-owned complete repository validation remain pending, so no final approval is claimed.

The assigned final in-process correctness review found and TDD-resolved two additional defects before converging `CLEAN`: dirty provenance could add `source-diff.patch` to an existing clean evidence directory before `source-state.json` refused overwrite, and hard-link publication let a post-link source mutation change the destination through the shared inode while an attached `HEAD` at the expected commit could pass the later provenance-only check. Source-state reservation now precedes any patch creation; publication now uses `COPYFILE_EXCL` without shared inodes; and the orchestrator reauthenticates the complete final-path checkout before root linking while retaining post-claim evidence on failure. The integrated assigned-scope run passed 72 tests with three expected Windows-hosted POSIX-only skips, including 3/3 in the two split regression files; exact evidence and dispositions are preserved in `raw/implementation-final-correctness.md`. Parent-owned final validation then passed the canonical guarded Node gate with 233 registered, 229 passed, four expected platform skips, and zero failures; full py313 passed 582 with 12 expected optional-RL skips; exact RL passed 585/585; and a fresh isolated copy-only missing-pin setup, strict verification, and no-op rerun each succeeded with a clean pin and zero setup or claim leftovers. The fleet-pinned external multi-CLI workflow remains unavailable, so no external review or approval is claimed.

Commit A `8cff6201dd4d0b2b07ae1fbf9068a5b179109457` exposed one additional cross-version root-layout defect in exact remote [run 29748574695](https://github.com/yanfengliu/python_mini_metro/actions/runs/29748574695): both Ubuntu `build` and Windows `rl-smoke` reached setup under Node 22.23.1/npm 10.9.8 and failed because root `npm ci --omit=dev` left eleven empty omitted-development scope directories that the exact root audit correctly refused. An isolated npm 10.9.8 reproduction retained those eleven empty scopes after `npm prune --omit=dev`, while npm 11.7.0 produced only the exact link plus metadata. Widening the allowlist is rejected. The prior supply-chain review had expressly offered the safer alternative of authenticating the complete package/lock contract and creating the exact root link directly; the correction follows that disposition, invokes npm only for the pin development graph, exclusively creates only a missing root symlink/junction below a physical container whose exact bigint identity is checked before and after creation, and refuses stale links, physical slots, or unexpected entries without deleting them. An `EEXIST` winner is preserved; a detected path-level container swap retains both containers and the link wherever atomic creation landed; and final contract validation rejects package/lock/`.npmrc` drift during link creation. The correction's operations and deterministic race suites pass 21/21; production is 466 lines and the split tests are 456 and 111 lines.

The independent A2 security and integration lanes initially blocked on the exact fast-path foreign-entry audit bypass, lossy numeric NTFS inode identity, missing deterministic race coverage, misleading no-root-npm spies, stale/overbroad documentation, and a reproduced root-contract drift race during link creation. All findings were accepted and TDD-fixed through shared classification auditing, bigint Stats, split `EEXIST`/container-swap/contract-drift tests, post-link contract validation, direct no-root-npm source plus filesystem evidence, and truthful retained-state language. Both fresh final re-reviews are `CLEAN`; their exact outputs are preserved in `raw/implementation-a2-security.md`, `raw/implementation-a2-security-final.md`, and `raw/implementation-a2-integration.md`.

Parent-owned final validation passed the setup-focused glob at 96 registered, 92 passed, four expected platform skips, and zero failures; canonical guarded Node at 239/235/four expected skips; full py313 at 582 with 12 expected optional-RL skips; and exact RL at 585/585. A fresh isolated missing-pin setup, strict verification, and no-op rerun all verified with one exact root junction, a clean descriptor pin, and zero setup leftovers. Changed-MJS syntax, exact resolution, dependency audits, diff checking, the exact 14-path hook set, and cached staging are also clean. Commit A2, push, and exact remote workflow remain pending.
