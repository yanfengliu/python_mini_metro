NOT CLEAN

1. **P1 — Partial line-removal accounting is still impossible under the stated global-only formula.** PLAN.md:29 claims every partial-failure order remains truthful, but `PathLifecycle.remove_path` removes Metros from `host.metros` at `src/path_lifecycle.py:73-79` before invalidation, color release, `host.paths.remove`, button reassignment, and replanning at `:80-84`. Any later failure leaves the Path active with its retained `path.metros`, while PLAN.md:18 immediately refunds those carriages because the Metro left the global list. That graph is active, not the detached historical graph described at PLAN.md:16/29. Either defer this known partial-failure case explicitly to GM-06d and limit GM-06c to successful removal, or transact/reorder removal now; D-023:143 must match.

2. **P1 — The capacity setter can still create passenger over-capacity.** PLAN.md:19 accepts any integer total at least equal to attached capacity. A six-passenger, zero-carriage Metro can therefore accept `metro.capacity = 0`, immediately violating the same line’s canonical `len(passengers) <= capacity` invariant. `Holder.capacity` is currently directly writable at `src/entity/holder.py:15-20`, so this is a real compatibility surface. Require the new total to be at least `max(attached_capacity, len(passengers))`, or explicitly classify such writes as malformed compatibility state and reject all subsequent mutations/checkpoints. D-023’s “invalid writes” wording is currently too vague to resolve this.

3. **P1 — The bounded transaction snapshot omits state that the transaction explicitly mutates.** PLAN.md:25/36 reconciles station service timing and fractional progress, but the enumerated snapshot does not name `stop_time_remaining_ms`, `boarding_progress_ms`, `speed`, or any new current-action state. A later postcondition/timer failure therefore cannot restore the promised exact prestate. Add every service-timing field changed by reconciliation to both attach and detach snapshots and rollback tests.

4. **P2 — The validator footprint remains broader than the named rollback footprint.** PLAN.md:24 validates exact active-path identity and complete geometry; PLAN.md:25 snapshots owner lists and vaguely named station/segment identities, but not explicitly the `host.paths` collection object/content, path IDs/creation flags, or exact geometry collection objects/content. A faulting carriage-list append can remove/rebind the target Path, causing postcondition failure that the listed snapshot cannot reverse. Enumerate the exact validator footprint and snapshot all of it, or explicitly narrow late-collaborator rollback guarantees.

The prior route-replacement, successful detached-identity, queue-validation, negative-base, and live-versus-persisted action-key findings are otherwise closed.
