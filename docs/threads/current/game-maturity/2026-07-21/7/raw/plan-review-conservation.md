NOT CLEAN

1. **P1 — Route-replacement preservation is only conditional, but live code proves extension is mandatory.** PLAN.md:30 promises exact carriage identity/order/base capacity on success and rollback, yet says to extend snapshots only “if live failure probes show” a need. `src/path_replacement.py:249-269` validates mutable aliases without future `Metro.carriages`; `src/path_replacement_snapshot.py:13-67` snapshots passenger lists and only three Metro motion fields; callbacks execute after the snapshot at `src/path_replacement.py:464-494`. A carriage list aliased to topology storage can change on successful replacement, while a graph/replanner callback can mutate/rebind composition or base capacity and then return or raise without restoration. Mandate carriage-list alias validation, full composition/base snapshots, a postcommit composition check, and rollback tests for aliasing, rebind, mutate-then-raise, and mutate-then-return—including semantic no-op replacement.

2. **P1 — Attach/detach rollback does not cover the state its postcondition validates.** PLAN.md:25-26 restores only carriage-list objects/content and base capacities. A faulting factory or custom list `append`/`remove` can also change `num_carriages`, `host.metros`, any `path.metros`, queue flags, or another Metro’s composition before the postcondition fails. Existing adversarial precedent deliberately exercises append-then-raise, owner rebinding, and substitution at `test/test_gm06b_fleet_assignment.py:17-35` and `:261-340`. This contradicts the all-failed-transitions guarantee at PLAN.md:73. Define a bounded full transaction snapshot over every validated ownership/composition field, including total inventory and global/path owner-list identity/content, and specify exception behavior: restore before ordinary failure return and before rethrowing non-`Exception` failures.

3. **P1 — Refund semantics conflict with intentionally retained detached object graphs.** PLAN.md:16 says unassigned carriages are never represented by dormant entities, while PLAN.md:29 says canonical locomotive removal refunds a whole consist. Current queued detachment removes only the Metro from owner lists (`src/fleet_management.py:365-400`), and line removal intentionally retains `path.metros` and each Metro’s graph (`src/path_lifecycle.py:71-84`; asserted at `test/test_path_lifecycle.py:103-155`). Without a decision, a refunded resource remains represented by `removed_metro.carriages`. Specify either transactional clearing/retirement of the exact carriage list at successful physical detachment and line-owner removal, with rollback/partial-order tests, or explicitly preserve noncanonical historical carriage objects and narrow the “no dormant entity” claim while proving they can never be counted or reattached. This is carriage bookkeeping, not GM-06d rider recovery.

4. **P1 — The writable capacity compatibility rule can create a state the plan calls canonical but cannot detach or checkpoint.** PLAN.md:19 requires exact integer types and `len(passengers) <= capacity`, but does not require nonnegative `_base_capacity`. With one six-seat carriage, `metro.capacity = 5` produces base `-1`, getter `5`, and an empty train satisfies the stated canonical predicate; detachment then leaves capacity `-1` and is permanently unsafe. PLAN.md:53 separately says v4 rejects negative capacities. Define atomic setter behavior for totals below attached capacity: either reject without mutation, or permit the compatibility write but classify the result as noncanonical so all carriage/fleet mutations and checkpoint generation reject it. Add zero, boolean, negative, and below-attached-capacity tests.

5. **P1 — Existing locomotive queue/settlement is not explicitly brought under carriage validation.** PLAN.md:24 applies canonical composition checks only to carriage queries, but PLAN.md:29 relies on the existing fleet transaction. `FleetManagement.unassignment_candidate`, `settle`, and `_detach` currently validate only locomotive ownership/queue/passengers (`src/fleet_management.py:272-400`). The plan must require shared carriage validation and exact consist retirement/refund in immediate return, delayed settlement, remove-then-raise rollback, and empty line removal. Corruption injected after queueing should fail closed and remain pending; recovery can remain GM-06d.

6. **P2 — Live structured-action key strictness is ambiguous.** PLAN.md:27 specifies exactly one selector but not whether unrelated keys are tolerated, while current `FleetInput` accepts them (`src/fleet_input.py:15-54`) and persisted v5 actions are explicitly exact-key-only at PLAN.md:55-56. State and test the distinction explicitly—prefer preserving current live-action extra-key tolerance while keeping replay documents exact-key-only, unless an intentional public API break is desired.

The balanced attach and reverse-order capacity-safe detach target rules, paused/terminal boundaries, and occupied-safe detach boundary are otherwise deterministic and consistent with GM-06d’s reserved scope.
