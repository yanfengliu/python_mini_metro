# GM-06c review synthesis

Status: implementation and local Python/Node/static review gates are `CLEAN`; exact hook/staging and Commit A remain pending

The runtime research establishes that the live model has no carriage entity, uses one fixed six-seat Metro holder, over-schedules capacity-limited boarding at both stop-start seams, draws all riders inside one locomotive body, exposes locomotive-only controls/observations, and persists aggregate capacity without composition. The schema research independently confirms that inserting carriage actions into current generic replay validation would silently change v1-v4 behavior, so checkpoint v4 plus recursive-input and agent-play v5 are mandatory.

The candidate resolves the research choices as two starting fungible units at six seats each, attached-only canonical entities, balanced path-level attachment, capacity-safe reverse-order detachment, whole-consist refund on safe owner removal, visible path-bound controls, route-following render geometry, structured-only observation expansion, and explicit legacy rejection/normalization. GM-06d remains the owner of occupied-locomotive queued return and destructive rider cleanup.

All three first-pass lanes returned `NOT CLEAN`; their verbatim outputs are preserved under `raw/` and none is treated as approval.

- Conservation found conditional route-replacement protection, an underspecified attach/detach snapshot, a conflict between fungible refunds and retained detached graphs, negative-base capacity states, missing queue/settlement carriage validation, and live-action extra-key ambiguity. The revision mandates focused replacement alias/snapshot/postcommit protection; a bounded full ownership/composition transaction snapshot with ordinary/non-`Exception` restoration rules; explicit retired historical identities; atomic capacity-write rejection; shared carriage validation for queue/settlement/removal; partial-order accounting tests; and existing live extra-key tolerance versus exact persisted keys.
- Simulation/UI found that the duration helper omitted approach stopping, an initial candidate list could diverge from transfer/replan execution, mid-stop composition was undefined, interpolated `MetroPose` is not a coherent path anchor, terminal extrapolation was too narrow, static control rows collide with seed-117 stations, control topology/snapshot fields were ambiguous, and the demonstrator lacked a behavioral assertion. The revision selects one just-in-time next-action oracle across approach/timing/execution, atomically reconciles fractional progress, samples whole consists from coherent endpoint snapshots, handles every insufficient-arclength/turnaround/rebase case, reserves a quantization-safe bottom control band for four stable controls per slot, preserves the locomotive snapshot field, adds a carriage field, and proves real attachment with two added decisions.
- Schema/replay found no UUID-free v4 wire shape, forward fields erased by permissive legacy normalization, no frozen v4 outcome oracle, incomplete agent-v5 requirements, and weak capacity/ID rejection. The revision defines exact index-only structured/top-level/motion records and prefix domains, rejects every v4-only field in v1-v3 before synthesis, freezes recursive-v4 and agent-v4 outcomes before production, requires both v5 contracts with pre-reset preflight, and rejects empty IDs plus nonpositive Carriage capacity.

The first re-review also returned `NOT CLEAN` in all three lanes. Conservation narrowed successful versus late-partial line-removal accounting, added current-occupancy capacity-write safety, required every service timer/cache field in rollback, and demanded the exact geometry/creation validator footprint. Simulation required a universal owned action identity, RNG/plan-pure queries, noncollapsing turnaround interpolation, and privileged carriage counters. Schema required nonvacuous attached-carriage v5 replay, an exhaustive reference/owner bijection, and UUID-free agent-v4 outcome projection. The twice-revised plan closes each item explicitly; verbatim outputs are preserved in `raw/plan-rereview-*.md`.

The final schema/replay and simulation/UI lanes returned `CLEAN`. Conservation found one final missing `host.is_game_over` rollback field and overbroad failed-transition acceptance wording; the plan now snapshots/restores that terminal flag and scopes exact attach/detach rollback separately from GM-06d's retained late line-removal recovery. Its narrow final recheck returned `CLEAN`. All three live-code plan lanes are clean, and frozen artifacts plus red tests may begin. Exact final outputs are preserved under `raw/plan-final-review-*.md`; the external multi-CLI lane remains unavailable at the established repository-export boundary, so no external approval is claimed.

The initial RED suites were then independently audited and returned `NOT CLEAN` in all three lanes. The resulting revisions eliminated vacuous rollback/corruption cases, underconstrained target ordering and conservation, aggregate-only timing probes, ambiguous consist/pixel regions, partial UUID-free bijections, permissive legacy/v5 preflight, and several baseline conflicts. Repeated cross-lane review found and closed narrower cache, alias, terminal extrapolation, layout rollback, full-record, raw-order, live-action, and checkpoint-purity omissions. The final core/conservation, simulation/render/UI, and schema/replay gates returned `CLEAN`; exact syntheses are preserved in `raw/red-*.md`.

The combined Python baseline is intentionally RED at 150 methods with 132 failures and 107 errors; the combined Node baseline has two historical passes and two intended v5 product failures. `raw/red-combined.md` records the exact commands, counts, and failure classes. No production file changed before this evidence was captured.

Production now implements the reviewed attached-carriage model, derived inventory and capacity, deterministic transactions and lifecycle accounting, capacity-aware just-in-time station service, four path-bound resource controls, whole-consist rendering, structured observation expansion, checkpoint v4, and recursive/agent v5. Frozen checkpoint-v3, recursive-v4, and pre-carriage outcome artifacts retain their exact LF bytes and historical versions reject forward carriage fields before mutation.

Implementation review was deliberately adversarial and iterative. Conservation review found and closed incomplete replacement snapshots, stale successful-replacement service caches, permissive service-cache validation, and no-op postcondition acceptance. Schema/replay review then proved the exhaustive serialized ownership/topology bijection, legacy forward-field rejection, raw-order independence, and nonempty v5 fresh-process redrive clean. Simulation/render review found real terminal-arrival detection, two-body hairpin, four-body adjacent, six-body nonadjacent, greedy global-projection, infeasible-polytope, and Dykstra limit-cycle defects; each received an exact real or production-shaped RED/GREEN regression. The final solver uses feasibility-checked radial expansion only when the fixed-order angular system is infeasible, deterministic cyclic halfspace projection with a convergence cap, and a Cartesian all-pair postcondition. Final mathematical review exercised 100,000 production-shaped two/four/six/eight-body cases without a failure, and the exact seven-body render-cap reproducer is finite, clear, and continuous across 21 dense samples.

The final local product evidence is 172/172 focused GM-06c Python tests, 998/998 full Python tests with 12 expected optional-stack skips, and 8/8 adjacent GM-06b/GM-06c Node replay tests. Ruff check and format-check pass all 84 changed Python files. Protocol, default-task, and fidelity-task identities remain exact; live content identity intentionally advances to `15b1a6ff56e74627803bce02b964cb5915afa9b65e13cd38d8621f894d398ffd`, while the canonical-LF training-source projection remains exact through its regression. Every changed handwritten production file remains below 500 physical lines except the explicit 757-line Mediator facade, below its 1,000-line ceiling.

The final hook and staged-integrity audit remain before Commit A. The declared archived GM-03f differential could not be rerun because its isolated `C:\tmp` output write was denied at the exhausted approval-usage boundary; no fresh differential pass is claimed. Its immutable expected artifact remains covered by the unchanged verifier and broader input tests. Canonical guarded `npm test` also remains unavailable locally because a pre-existing unowned civ-engine setup lock is preserved rather than deleted; focused Node contracts pass, and the exact clean-checkout hosted workflow remains the authoritative full Node/recursive gate. The external multi-CLI lane remains unavailable at the established repository-export boundary, so no external approval is claimed.
