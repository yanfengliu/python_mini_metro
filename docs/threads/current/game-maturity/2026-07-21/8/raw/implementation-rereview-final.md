# GM-06d implementation fix re-review — verdict: CLEAN (verbatim)

Every finding is genuinely fixed or honestly dispositioned; the fresh-hole hunt found no defects. Counts verified independently: full suite Ran 1043, OK, skipped=12, 0 failures; targeted GM-06d modules + test_gm06b_fleet_queue = 64 OK; other modified modules (gm06a inventory, gm06c carriage lifecycle, mediator path contract/paths, path_lifecycle) = 52 OK. Ruff check + format clean on all changed files.

Per-finding verification:

- F-1 FIXED. `src/fleet_queue_transition.py` (75 lines) mirrors the carriage attach/detach idiom exactly (compared against `src/carriage_management.py:102-201`): snapshot → reconcile → `transaction_state_matches(allow_service_change=metro)` → `service_cache_is_canonical(allow_unbound=False)` + queue-canonical postconditions → restore-all-plus-flag on BaseException, non-`Exception` rethrow with traceback. `fleet_management.py` queue (306-336) keeps the empty fast path semantically identical and routes at-station occupied selections through the transaction; cancel (356-379) symmetric. Both mediator facades (539-552) pass `_reconcile_station_service` and grep confirms they are the only production call sites. Regressions reproduce the reviewer probes end-to-end, including the previously-crashing v5 document; its pinned oracle findings sit at pre-pause steps 7,8, temporally impossible to originate from the queue op.
- F-2 FIXED. `remove_path` takes the resolver (mediator.py:461-466); `_reconcile_surviving_service` (path_lifecycle.py:192-206) runs inside the try after the replan sweep; `path_removal_snapshot._METRO_FIELDS` covers stop/boarding/speed/cache for all metros, so mid-loop failure restores already-reconciled survivors.
- F-3 justification VERIFIED TRUE, accepted. `Metro()` initializes `speed=max_speed`, `stop_time_remaining_ms=0` (entity/metro.py:45,50); the frozen drain fixtures park metros in exactly that shape and the batch-drain test fires the pre-flow drain on it — a speed gate would break them, and no other persistent stopped-vs-coasting signal exists. Post-queue, the stop-override means queued metros never coast, so the window is the queue-during-coast tick only; deterministic and conserving.
- F-4 FIXED. Drain gated on live `is_game_over` (mediator.py:647-652), settle untouched; terminal-tick behavior pinned; the flipped gm06b force-alight test unaffected.
- F-5 / OBS-2 / OBS-3 dispositions hold: `remove_path_by_index` unchanged with README documenting selector-validity plus whole-transaction restore; GAME_RULES enumerates the three drain sub-cases matching `_stranded_riders`; `replay_playthrough` still constructs the env before the lazy generator's live-only rejection — side-effect-free pre-existing shape.
- OBS-1 FIXED (ValueError refusal with exact-restoration regression). OBS-4 FIXED (import from recursive_contract, identity-asserted). MINOR-1 superseded by the full-oracle F-1 fix (handles all identities plus the cancel direction).

Fresh-hole hunt (12 scratchpad probes, all passed): (a) purity — successful paused queue+cancel is RNG-byte-neutral; two runs of paused queue/cancel + 20 resume ticks produce byte-identical v4 checkpoints; (b) restoration completeness — corrupting `Exception`, `KeyboardInterrupt` (propagates after restore), and silent-wrong-bind (caught by postcondition) all leave state byte-identical including flag/cache/timers/plan identities; snapshot-after-flip plus restore-state-then-flag ordering lands exactly on the original flag; (c) a sabotaged survivor reconcile rolls back the entire removal byte-identically — fail-closed refusal; (d) no settle-timing or flipped-test breakage; (e) `fleet_management.py` = 499 lines; no circular imports (fleet_management → fleet_queue_transition → carriage_transaction_snapshot/fleet_validation; path_lifecycle imports fleet_management lazily).

One non-blocking note: `FleetManagement.queue`/`cancel` take `reconcile_station_service` as optional-default-None (the carriage idiom makes it required); today the mediator facades are the only production callers and both pass it. Acceptable as-is; tightening to required is a hardening follow-up, not a defect.
