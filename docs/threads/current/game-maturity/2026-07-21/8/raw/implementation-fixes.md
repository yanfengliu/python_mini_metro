# GM-06d implementation-review fix round (verbatim summary)

- F-1 (MAJOR): new `src/fleet_queue_transition.py` (75 lines) provides `set_queue_flag` and `reconcile_queue_transition` mirroring the carriage transaction idiom: snapshot → reconcile callback → `transaction_state_matches(allow_service_change=metro)` → postconditions `service_cache_is_canonical(allow_unbound=False)` plus queue-state-canonical → restore-all-including-flag on failure, non-`Exception` rethrown. `fleet_management.py` `queue`/`cancel` take keyword-only `reconcile_station_service`, applied to the selected at-station metro inside the transaction; the empty-candidate immediate-detach fast path is byte-identical. Both `Mediator` facades pass `self._reconcile_station_service` — the exact callback carriage attach/detach use. Identity-matching fractions are preserved (in-progress unloads keep progress), a now-illegal BOARD binding is dropped on queue, and a newly-legal action binds on cancel.
- F-2 (MINOR): `remove_path` accepts `get_reconcile_station_service` and reconciles every surviving at-station metro inside the snapshot scope after the replan sweep; `Mediator.remove_path` passes the resolver; the facade signature is unchanged.
- F-4 (NOTE): `_drain_and_settle_queued_returns` gates the drain (both invocations) on `not is_game_over`; settle untouched.
- OBS-1: `_alight_metro_riders` raises an ordinary `ValueError` when riders exist and no alight station resolves; the removal transaction restores and refuses (silent drop → safe refusal).
- OBS-4: `agent_play.py` imports `_LIVE_ONLY_ACTION_TYPES` from `recursive_contract`; single canonical definition.
- F-3: left as-is with justification — the only persistent stopped-vs-coasting signal is `speed == 0`, which the frozen drain fixtures contradict (`make_two_station_game` parks metros at constructor `max_speed` and the batch-drain test depends on the pre-flow drain firing in that shape); behavior is deterministic and conserving. Dispositioned as accepted.
- F-5 / OBS-2 / OBS-3: dispositioned as documented pre-existing shape, plan-enumerated behavior for unreachable-live states, and pre-existing wrapper shape with proven zero mutation, respectively.

Seven red-first regressions (initial run FFFFEFF): paused-queue canonicality (checkpoint valid, count 1, cancellable while paused — reviewer recipe reproduced exactly); paused-cancel canonicality (checkpoint valid, BOARD binds and executes after unpause); the reviewer's schema-valid v5 recursive document running `run_scenario` without the checkpoint crash (new `test/test_gm06d_service_cache_reconcile.py`, which also pins pre-existing padding-segment oracle findings at steps 7 and 8 so they cannot silently change — filed as a separate background task); survivor reconcile on removal; terminal-tick drain gate; unplaceable-rider refusal; single-source live-only constant.

One necessitated test edit: the flipped `test_path_lifecycle` conservation test pinned the OBS-1 silent-drop branch through a stationless FakeMetro; the doomed metro now has a resolvable alight station, every original assertion survives, and the refusal contract is pinned by the new regression.

Verification: GM-06d + adjacent focused set 82/82 then extended 116 OK; full suite 1043 tests, 0 failures, 12 skipped (baseline 1036 + 7 regressions); ruff check/format and per-file pre-commit clean; line counts within limits (`fleet_management.py` 499 after helper extraction, `mediator.py` 791, `path_lifecycle.py` 433). Guarded `npm test` re-run after the fix round: 253 tests, 249 pass, 0 fail, 4 skip.
