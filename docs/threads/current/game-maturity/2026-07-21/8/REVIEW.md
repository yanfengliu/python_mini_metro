# GM-06d review synthesis

Status: delivered as Commit A `62d26a2`, exact [run 29893340731](https://github.com/yanfengliu/python_mini_metro/actions/runs/29893340731) green; evidence-only Commit B active

The plan went through three adversarial rounds before any test was written. Two independent first-pass lanes (conservation; simulation/lifecycle, preserved verbatim in `raw/plan-review-conservation.md` and `raw/plan-review-lifecycle.md`) both confirmed the central Case 1 risk absent — the service oracle unloads before the queue-gated boarding check, so a queued occupied locomotive cannot freeze riders aboard — and both independently refuted the original Case 4: forced alights ignored `station_capacity = 12` against consists of up to 18 seats, `Holder` placement hard-asserts room, and the nontransactional `remove_path` would strand a half-removed line no reconcile could safely repair. The owner resolved the capacity policy as D-024 overflow-permitted alighting; the revision made `remove_path` a snapshot/rollback transaction with alight-before-mutation ordering and narrowed reconcile to two provably-safe repairs. A combined re-review (`raw/plan-rereview-combined.md`, NOT CLEAN) verified all sixteen original findings closed and found three new holes in the revision itself — the delivery-credited alight touches a progression cluster the snapshot did not enumerate, `cancel_unassignment` needed explicit persisted-version gating against the unknown-type fall-through, and drain pacing was unspecified; it also disproved the hypothesized checkpoint-v4 station-capacity blocker. All three were dispositioned (complete footprint enumeration, live-only gating at v1-v5, one-atomic-batch holder-order drain that never touches the service cache) and a narrow final recheck (`raw/plan-final-recheck.md`) returned CLEAN.

Red evidence was captured before any production change (`raw/red-return-cancel.md`, `raw/red-removal-reconcile.md`): 46 genuinely red records across four new focused modules plus surgical contract flips, with guard greens marked, and a combined six-module baseline of 56 tests at failures=40, errors=6. Red authoring surfaced a third planned contract flip (`test_gm06b_fleet_queue`'s planless-rider-stays-aboard test enshrined exactly the freeze the drain rule eliminates); implementation later surfaced five more old-contract tests that are mutually unsatisfiable with transactional conserving removal (three gm06a removal-failure tests expecting propagated exceptions with persistent partial state, the self-scoped `..._until_gm06d` lifecycle test, and the rider-deletion/event-order contract test). All eight flips preserve their still-valid assertions and are recorded in the plan.

Implementation review was two independent adversarial lanes against the live code (`raw/implementation-review-conservation.md`, `raw/implementation-review-lifecycle.md`). Conservation returned CLEAN: seven-point failure injection through the removal chain — including after a milestone-crossing credited delivery that rebound `host.stations` — showed zero value-or-identity drift including both RNG streams; 24-seed drain fuzzing plus 100+ targeted runs could not surface a lost, duplicated, or double-counted rider or resource. Lifecycle returned NOT CLEAN with one major probe-backed defect: `queue()`/`cancel()` flipped boarding legality without reconciling the service cache, so a queue or cancel executed while paused left a stale cache that disagreed with the pure oracle — three public surfaces disagreed, cancel-while-paused was impossible, and v4 checkpoint generation crashed on a legitimately-reached state, reproduced end-to-end by a schema-valid v5 recursive document crashing the harness. The fix round (`raw/implementation-fixes.md`) closed it with the D-023 precedent — both facades now pass the same synchronous station-service reconcile carriage attach/detach use, through a new transactional `fleet_queue_transition.py` helper — plus the survivor-reconcile on removal, a terminal-tick drain gate, an explicit refusal replacing the dead rider-drop branch, and a single-source live-only constant, all landed with seven red-first regressions including the exact harness-crash document. The coasting-drain finding was left as accepted behavior with a verified justification (no persistent stopped-vs-coasting signal exists that the frozen fixtures permit; the window is one queue-during-coast tick; deterministic and conserving).

The final fix re-review (`raw/implementation-rereview-final.md`) returned CLEAN: every finding genuinely fixed or honestly dispositioned, a twelve-probe fresh-hole hunt on the fixes themselves passed (paused queue/cancel byte-neutral on both RNG streams and checkpoints; restoration complete through corrupting, silent-wrong-bind, and `KeyboardInterrupt` failures; a sabotaged survivor reconcile fail-closes the whole removal), and counts were independently verified. One non-blocking hardening note is recorded (the reconcile callback parameter is optional-default-None where the carriage idiom makes it required; only the passing facades call it in production).

Final local product evidence: full py313 suite 1043 tests, 0 failures, 12 expected skips (baseline 1036 plus seven review regressions); guarded `npm test` 253 tests, 249 pass, 0 fail, 4 expected skips, re-verified after the fix round; Ruff check/format and per-file pre-commit clean on every changed file; every changed production file within its line budget (`fleet_management.py` 499 after helper extraction, `mediator.py` 791 under the 1,000 facade ceiling). Two out-of-scope discoveries were filed as separate background tasks rather than expanding this unit: the CI Node-20 actions deprecation, and a pre-existing recursive-oracle false-positive for mid-padding-segment checkpoints that the new harness regression pins so it cannot drift silently. The external multi-CLI workflow remains unavailable at the established repository-export boundary, so no external approval is claimed.
