CLEAN.

The blocker is resolved: expected `.civ-engine-pin` and actual `../civ-engine` paths, `matches.path: false`, version/commit/digest mismatches, and the categorical-diagnostic boundary are now recorded consistently.

The broader corrections also hold: credential evidence is properly scoped with staged scanning still pending; the complete physical `scripts/` copy is truthfully adjudicated; ephemeral harness/source boundaries and reviewed SHA are explicit; sentinel/pre-body wording matches the proof; and both initial BLOCKED reviews are preserved verbatim.

Current delivery scope is 16 paths: five modified plus eleven new. `.agents/`, ignored output, pin, `node_modules`, caches, and temporary artifacts remain excluded.
