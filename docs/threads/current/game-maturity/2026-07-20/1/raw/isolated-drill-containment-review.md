CLEAN for controlled execution.

The isolated fixture contains all mutations; cleanup validates identity and reparse containment before removal. The live pin, sibling, and production root are read-only and fingerprinted before/after. The output also correctly distinguishes categorical guard evidence from independently captured mismatch metadata.

I did not edit or execute the harness.
