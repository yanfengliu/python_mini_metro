# Corrected-plan API/replay re-review 1

Result: NOT CLEAN

1. [MEDIUM] `PLAN.md:53,71-72` still promises a full station leaves the Python RNG untouched / asks full-station no-replan + RNG snapshots, but live `PassengerFlow.move_passengers` computes mutating boarding candidates before the `station.has_room()` transfer guard (`src/passenger_flow.py:313-323`, then guard at 368). A full station necessarily has waiting riders, and any rider lacking a current plan for this metro invokes constrained planning (`src/route_planner.py:183-194` -> `Mediator.get_travel_plan_starting_with_path`, `src/mediator.py:577-590`), which shuffles destinations and may advance Python RNG. The correct invariant is that blocked marker transfer does not invoke the new scoped marker planner and preserves the marker/rider fields; total RNG must match an equivalent control run including existing boarding-candidate side effects, not necessarily remain identical.

2. [MEDIUM] The new PassengerFlow single-rider planner's ownership/API remains underspecified. `PLAN.md:47,53,69,74-75` requires PathLifecycle replacement and PassengerFlow alight handling to call it, but specifies neither an exact Mediator facade method/signature nor an explicitly private resolver/callback boundary. Live PathLifecycle can currently reach passenger routing only through the public host hook `find_travel_plan_for_passengers` (`src/path_lifecycle.py:10-30,79`); current PassengerFlow transitions are similarly Mediator-wrapped (`src/mediator.py:598-605`). Decide and document one exact boundary: either an exact public Mediator scoped-replan method (plus host protocol and dynamic-dispatch tests) or a named private late-bound resolver passed into both operations with an explicit no-new-public-API promise. Otherwise the high-risk facade contract and ARCHITECTURE count are not determined.

3. [LOW/docs truthfulness] Parent `STATE.md:24` and iteration `REVIEW.md:3,9` say production remains gated on corrected-plan re-review, while `STATE.md:129` says there is "No blocker to ... implementation." Until review converges, change that to "No external blocker; implementation remains gated on corrected-plan review convergence" (and advance it after approval).

All earlier action subtype/XOR, facade replace signatures, late factory dispatch, all-state ambiguity, off-segment validation, fresh graph marker, stable waiting batch, scoped alight, rollback scope/snapshot, exact file-size/gates/geometry, docs counts, checkpoint identity, and two-commit findings appear closed in the current live draft. Review was read-only.
