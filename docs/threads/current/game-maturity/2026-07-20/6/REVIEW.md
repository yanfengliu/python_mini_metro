# GM-06b review synthesis

Status: plan and implementation review are `CLEAN`; initial Commit A's build failure is preserved, corrective A2 is exact-head remote green, and evidence-only Commit B is active

The three research lanes agree on the canonical ownership model, removal of transitional automatic allocation only alongside working replacement controls, visible plus/minus parity with strict structured actions, multiple locomotives per line, no early refund, and no privileged PlayerPixel shortcut. They also agree carriages remain GM-06c and destructive occupied/line-removal hardening remains GM-06d.

The main research disagreement concerns persisted queue intent. One lane proposed conditionally adding a queue field to checkpoint v2 because old inactive bytes could remain unchanged. The roadmap lane correctly identified that the parent invariant explicitly forbids treating an added persisted field as accidentally backward compatible. The candidate therefore selects checkpoint v3 plus recursive-input and agent-play v4, with genuine older versions normalized or replayed through explicit legacy boundaries.

The candidate selects the smallest GM-06d-safe queue: only an empty exact-owned Metro can be requested in GM-06b. A moving request remains assigned, boards nobody, stops at the next station, and detaches transactionally. An occupied candidate rejects rather than destroying riders; broader occupied semantics are not falsely claimed. Redistribution is the same queue-return-then-assign sequence for manual and structured users, without a destination reservation or third ownership class.

## First-pass plan review dispositions

- Conservation/failure review found a late legacy adapter, an uncovered direct boarding-candidate seam, unspecified terminal-tick settlement, coordinate-based fresh-train ambiguity, and an over-cap equality contradiction. The revised plan selects one shared pre-tick legacy transition with historical no-capacity behavior and no outward row on exceptional adapter failure; blocks queued boarding in both facade seams and both candidate modes; settles after the complete passenger-flow call including terminal evaluation; requires exact `current_station` identity rather than coordinate equality; and scopes arithmetic equality to within-cap states while retaining the universal clamped formula.
- Schema/replay review found ambiguous global-prefix/path-only checkpoint correspondence, missing Node v4 projection, UUID-capable persisted actions, no InputCoordinator extraction, and no exact v1/v3 artifact action. The revision specifies every v3 boolean location and prefix/suffix/count relation, full-union refusal for old generation, immutable v1/v2 normalization, all v4 verifier fields, index-only persisted fleet actions with preflight rejection, `fleet_input.py`, exact production line gates, named checkpoint-v1/v2 and recursive-v1/v2/v3 fixtures, and a nonzero-dt legacy oracle.
- Controls/Pixel review found an incomplete release matrix, ambiguous multi-queue styling, insufficient profile reachability, the same file-size risk, and no authoritative demonstrator coordinate source. The revision pins release-target behavior for direct/bare/cross-control/zero-station/redraw/handle cases plus `finally` cleanup, makes queue badges orthogonal to shared minus eligibility, requires canonical profile round-trip isolation and scaled discriminating pixels, selects the fleet input extraction, and extends only the explicit privileged snapshot with UUID-free path-indexed real-control coordinates.

The first-pass outputs are preserved verbatim in `raw/plan-review-conservation.md`, `raw/plan-review-schema.md`, and `raw/plan-review-ui.md`. None is treated as approval. Fresh reviewers must try to refute the revised contract against live code, and all three must return clean before the red phase begins.

The conservation and controls/Pixel re-reviews returned `CLEAN`. The schema re-review found one remaining total-inventory alias hole and requested executable prefix correspondence. The plan now requires exact equality between fleet total and `progression.limits.num_metros`, exact integer typing on both, ordered source-observation IDs matching the live global identities, and field-by-field prefix agreement between structured metros and motion records with declared-owner rather than actual-owner path semantics. The final narrow schema recheck returned `CLEAN`; all three plan lanes are now clean, and the next permitted action is the declared red-test phase.

The external multi-CLI workflow remains unavailable at the established repository-export authorization boundary. These in-process plan reviews are compensating evidence only; no external approval is claimed. Internal plan approval rests on the three live-code clean re-reviews preserved under `raw/`.

## Red evidence

The reviewed tests were added without production changes. Core fleet ran 29 methods, schema/replay ran 22 Python methods plus four Node tests, and controls/Pixel plus the demonstrator ran 24 methods. One UI harness-only invalid station index was corrected before the recorded lane result. The definitive 69-method `test_gm06b_*.py` discovery then failed with 45 failures and 34 errors/subtest errors exclusively at the intended missing product boundaries; frozen old checkpoint bytes, frozen six-lane legacy outcomes, and unchanged protocol/task/training controls remained green. Node retained three green controls and failed only the missing v4 projection. Production implementation is now permitted.

## Implementation and review

Production now removes transitional allocation and exposes explicit assignment plus empty-train queued return through the same public facades used by strict structured actions and real plus/minus controls. The queue remains an assigned subset until exact physical detachment at a real station; queued trains cannot board, cross synthetic padding without stopping, and settle after the complete passenger-flow tick. Checkpoint v3 persists full fleet/queue state, recursive-input and agent-play v4 persist replay-safe index-only fleet actions, and one shared pre-tick adapter preserves genuine v1/v2/v3 outcomes without weakening new schemas.

Three independent implementation lanes returned final `CLEAN`. Schema/replay passed 22 Python and four Node tests, all five frozen hashes, exact historical v3 bytes, and additional stale/alias/index/legacy probes. Controls/Pixel passed 153 focused and adjacent tests after accepting and fixing synthetic-padding stopping and locked-purchase-text overlap. Conservation initially found malformed active geometry could reach construction or install a stationless train; a new red regression covers both reproducers, and assignment now requires the existing canonical geometry validator before factory resolution. Its clean re-review passed 212 tests plus valid-loop/replacement, malformed-queue, import-isolation, and Ruff probes. Exact outputs are preserved under `raw/`; the unavailable external multi-CLI boundary is still not claimed as approval.

## Local gates

After corrective A2, GM-06b discovery passes 73 Python tests in 1.397 seconds and its four Node replay tests pass in 123.366 milliseconds. The definitive py313 suite passes 826 tests in 9.509 seconds with 12 expected optional-stack skips. The direct canonical Node suite registered 249 tests, passed 245, skipped four platform-specific tests, and failed zero in 160.924 seconds before Commit A. All 47 implementation Python files passed Ruff check and format-check before Commit A; the corrective test file passes both again, and the exact nine-path pre-commit run passes EOF, trailing-whitespace, Ruff, and Ruff-format hooks without edits. The frozen GM-03f differential remains exact at four cases, 90 events, 16 records, 7,123 bytes, and SHA-256 `147f90d827a9b4c3fb17f0aae212e2603c5c6bdc99915a87bbfde29f8d699f05`. Protocol, default-task, and fidelity-task fingerprints remain exact; canonical-LF materialization proves unchanged training-source identity at `f6fa3ad50bb992152ea0f24dff35603e8e906714cf58c5fcc359ede4af54f65c`, while a copied LF-to-CRLF mutation proves the unchanged production algorithm remains raw-byte sensitive. The intentional content fingerprint remains `78bd55440ffa49f65559aa15c2e806913fea59aca17f610ccb11083468c4ff21`. Every changed handwritten production file remains below 500 physical lines except the established 695-line Mediator facade, which remains below 1,000. Both corrective live-code/evidence reviewers returned final `CLEAN`. Commit A's exact 88-path stage had 4,882 insertions and 313 deletions with zero forbidden paths, credential matches, or unstaged drift. Corrective A2's exact nine-path stage has 93 insertions and 25 deletions with cached whitespace clean, zero forbidden/dependency/workflow/credential matches, zero unstaged tracked drift, and only the preserved `.agents/skills/multi-cli-review/SKILL.md` untracked.

## Commit A hosted gate and correction

Commit A `df04fb6b191f3d8609e316623950b06416d61c19` triggered exact [run 29801037756](https://github.com/yanfengliu/python_mini_metro/actions/runs/29801037756). `rl-smoke` job `88542015196` passed every step. `build` job `88542015227` passed setup, recursive contracts, and the clean pass, then failed one Python assertion because the new test hardcoded a mixed local raw-byte training hash while the unchanged LF checkout has a different exact hash. Adversarial review proved fresh `core.autocrlf=true` bytes have a third valid hash, so A2 removes the live-worktree assertion and instead freezes one deterministic canonical-LF materialization at exact `f6fa...`; production hashing and artifact compatibility remain unchanged. Corrective A2 `29cb64cdfea0fecbfcee2a1831c6f3cc47ef6ccd` passed exact [run 29808842268](https://github.com/yanfengliu/python_mini_metro/actions/runs/29808842268), run number 139: exact-head `build` job `88565158756` passed in 1m18s and exact-head `rl-smoke` job `88565158714` passed in 4m29s. Evidence-only Commit B records both immutable outcomes and changes only parent STATE/EVIDENCE plus this iteration's PLAN/REVIEW/diff; GM-06c remains closed through B's own exact workflow.
