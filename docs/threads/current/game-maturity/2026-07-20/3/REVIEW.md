# GM-05b review synthesis

Status: plan and implementation review are `CLEAN`; Commit A is exact-head remote green and evidence-only Commit B is active

The first research lane recommends a continuous press-drag-release from an assigned colored path button through stations. It preserves the current plain-click and bare-up deletion paths, uses only existing low-level pixel actions, and occupies event branches that are otherwise no-ops. The draft remains off-live and dispatches exactly once through GM-05a's public atomic replacement hook.

Two independent probes reproduced the same immediate interpolation defect with different networks: replacement preserved the metro's physical pose and path ID while moving its retained edge to another segment index, after which stale snapshots projected the metro roughly 14.55 pixels away in one probe and from `(622.5, 818.5)` to `(544, 942)` in the other. The plan therefore requires exact live-segment identity validation before cached snapshot reuse.

Initial adversarial review found and the live plan now addresses armed-redraw replacement preflight, creation/repeated-down precedence, rebound-hook rollback overclaiming, stale hover-cross cleanup, explicit converter coordinates, PlayerPixel tick/profile/determinism controls, immutable checkpoint/protocol hashes versus changed content identity, D-007 downstream reconciliation, safe orientation-preserving interpolation rebase, normal-transition coverage, two-pass preview parity, exact cache counters, cursor-independent pixels, bounded retention, and file-size pressure. Fresh review then exposed ambiguous zero-station button releases, same-route no-op cache semantics, and insufficient padding-transition identity; the plan now delegates the complete historical release-target matrix before any station is captured, separates topology changes from successful no-ops, and captures normalized adjacent-edge context for padding. Three fresh live-plan rereads are clean.

The first executable PlayerPixel red test caught a transcription error in one raw review suggestion: the live `TaskSpec(render_profile=FIDELITY_RENDER_PROFILE)` value is the 64-digit `cd713a6891d8e74dab1aac2ded2edc88a727cb2b5b420948c65731d3a0eb3418`, not the same value with a trailing `f`. The plan and executable assertion use the live value; raw review remains verbatim.

The external multi-CLI workflow remains unavailable at the established repository-export authorization boundary; in-process review is compensating evidence only.

Implementation proceeded TDD-first across real converted pygame events, immutable draft transitions, fast and fidelity `PlayerPixelEnv` actions, canonical checkpoints and structured views, software-surface preview parity, cache/reference bounds, and exact no-tick metro poses. The initial station-center snap defect found during integration was corrected before the full suite, and the final implementation keeps all gameplay topology, riders, fleets, clocks, RNG, and structured state behind the existing atomic replacement boundary.

Independent implementation review found six actionable edge classes: captured-draft button cancellation cleared release-position hover; malformed creation with no path asserted; assigned-but-locked buttons could arm; wrong-type redraw values crashed and could strand a legitimate creation; zero-station release actions failed to restore post-action hover; and an unlock-blink off phase hid selected/invalid feedback. Each claim was reproduced against live code, fixed with a real-event or pixel RED/GREEN regression, and then retested by a reviewer who did not author that lane. Final input/state, rendering/interpolation, and integrated re-reviews are all `CLEAN`; verbatim transcripts are preserved under `raw/`.

The final py313 core run passed 674 tests in 6.887 seconds with 12 expected optional-stack skips. The retained exact RL environment passed 677 tests in 13.696 seconds with no skips. The named checkpoint and recursive/agent v1/v2/v3 compatibility run passed 34 tests. The GM-03f differential retained exact four-case, 90-event, 16-record equality at SHA-256 `147f90d827a9b4c3fb17f0aae212e2603c5c6bdc99915a87bbfde29f8d699f05`. Protocol/default/fidelity task fingerprints remain exact while the intentional content fingerprint changes to `81671dfe5457eaec53c4e64b3e3921587b470db470b2a7c6c54cf394da5dd4a0`.

All 18 changed Python files pass exact Ruff check and format validation. The typing-only host split leaves `InputCoordinator` at 442 lines and its structural protocol at 92; all other changed handwritten files remain below 500 except the already explicit 655-line Mediator facade, below the 1,000 hard ceiling. The exact 38-path pre-commit set passed EOF, trailing-whitespace, Ruff, and Ruff-format hooks, with YAML correctly skipped. No dependency or checkpoint schema changed.

Implementation Commit A `37865d454eb93887a726af74441eb2bc282174fb` passed exact [run 29786749550](https://github.com/yanfengliu/python_mini_metro/actions/runs/29786749550), run number 132. Exact-head `build` job `88499822031` passed in 1m08s and exact-head `rl-smoke` job `88499822004` passed in 1m07s. Evidence-only Commit B records that immutable result and leaves GM-05c closed; only GM-05c's opening transaction may reconcile B's own exact workflow and open the next substep.

Commit A's exact staged payload contained 38 reviewed paths, no forbidden or credential-bearing path, zero cached whitespace or high-confidence credential matches, and no unstaged tracked drift. Commit B changes only the parent STATE/EVIDENCE and this iteration's PLAN/REVIEW documents.
