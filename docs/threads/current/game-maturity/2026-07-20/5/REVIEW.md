# GM-06a review synthesis

Status: live research, independent plan review, red-first implementation, full local/frozen validation, independent implementation review, exact hooks, and scoped staging audits are clean; Commit A delivery is active

All three research lanes agree that `Mediator.metros` must remain assigned-only and that unassigned inventory must not be represented by preconstructed Metro entities. They also agree current automatic allocation must remain temporarily so GM-06a does not make new lines unserved before GM-06b supplies explicit assignment controls.

The inventory and UI lanes recommend a late-derived available count with no backing state. The state lane proposed a validated value object plus checkpoint v3, recursive-input v4, and an array mirror. The plan rejects those additional schemas for this substep: total and assigned are already canonical checkpoint-v2 facts, availability is exact arithmetic rather than new latent state, direct callers intentionally mutate `num_metros`, and the current arrays do not mirror session scalars. Duplicating the count would create disagreement and migration surfaces without improving reconstructibility. Direct runtime/structured/checkpoint equality and legacy-version tests replace that proposed duplication.

The selected product boundary includes player-visible HUD feedback and a labeled structured `fleet` object because a conserved resource that exists only as an internal property would not yet be an explicit game inventory. It excludes controls/actions, privileged Pixel info, and count arrays so GM-06b and GM-12 retain their accepted ownership.

The highest compatibility risk is not the arithmetic but transition timing. Finish and removal intentionally expose partial effects and rebound collections. The plan therefore derives availability from the current global list at every read and tests the value at late factory, route-add, global-append, button, and removal failure points rather than adding rollback or changing PathLifecycle. First-pass inventory and state reviewers found that the original one-for-one refund wording was false in an explicitly supported artificial over-cap state; the corrected contract says removals always reduce assigned count, while availability can remain clamped at zero until the deficit clears.

HUD feedback expands the collision exclusion used by route handles. The first HUD/state review found the original plan did not select legacy fallback behavior, an exact exclusion, a supported count envelope, a proof against silent handle omission, a count-glyph-only CHW assertion, or the renderer file-size gate. The corrected plan resolves property-first then derives from complete legacy total/assigned surfaces then defaults to zero; fixes the exclusion at `(0, 0, 840, 200)` for counts through 999 under the renderer's actual bundled `Font(None, 50)`; drives actual low-level `4 -> 3 -> 4` observations in both profiles; and keeps `game_renderer.py` below 500 physical lines. UI re-review then identified that kind-only preservation could miss a silently dropped insertion slot, so the final contract compares the exact expected `(kind, slot)` descriptor set and cardinality under old versus new exclusions and requires quantized clearance plus hit reachability for every descriptor.

The state review also required the no-schema claim to prove its full premise. The corrected plan pins the same clamped derivation from genuine checkpoint v1 and v2, including an over-cap graph and a detached path-only Metro where `metroMotion` is one but canonical global `structured.metros` is zero. This strengthens rather than changes the selected no-migration boundary.

Exact first-pass outputs are preserved in `raw/plan-review-inventory.md`, `raw/plan-review-state.md`, and `raw/plan-review-ui.md`. No code or tests were written before these corrections. Inventory/failure, state/checkpoint, and HUD/Pixel final re-reviews each returned `CLEAN`; their exact conclusions are preserved in `raw/plan-rereview-inventory.md`, `raw/plan-rereview-state.md`, and `raw/plan-rereview-ui.md`. Red tests may now begin.

The combined red pass ran 34 tests in 0.425 seconds with 13 missing-property errors and 20 intended product failures; the two baseline geometry/purity methods passed. After only the four planned production surfaces and the directly affected legacy HUD expectations changed, all 34 passed in 0.359 seconds and the adjacent 119-test slice passed in 1.343 seconds. Implementation review and full/frozen gates are now clean; exact hooks, staged audits, and delivery remain pending.

Three fresh implementation lanes independently audited inventory/lifecycle failures, structured/checkpoint/protocol/docs compatibility, and HUD/PlayerPixel/handle rendering. Inventory returned `CLEAN` after 34 focused plus 42 lifecycle/rollback tests and extra abort/detached probes. State found one documentation-only physical-line defect: the touched architecture sentence inherited `InputCoordinator` as 476 lines while live and baseline are 475; after correction its re-review returned `CLEAN`, with the 753-test suite, hashes, genuine schemas, arrays/actions/info, purity, and file counts confirmed. UI returned `CLEAN` after 40 focused/adjacent tests, canonical and compact layout checks, genuine low-level glyph proof, cache/purity review, and 100,000 sampled route layouts. Its reproduced smaller arbitrary-viewport fail-closed omission is the existing GM-05c boundary with full redraw fallback, not a GM-06a defect because the canonical player/Pixel surface remains 1920x1080.

The external multi-CLI workflow remains unavailable at the established repository-export authorization boundary. These are compensating in-process reviews only; no external reviewer read or approved GM-06a. Exact outputs are preserved in `raw/implementation-review-inventory.md`, `raw/implementation-review-state.md`, and `raw/implementation-review-ui.md`.

The definitive py313 suite passes 753 tests in 8.907 seconds with 12 expected optional-stack skips; 32 genuine checkpoint/recursive/agent compatibility tests and the exact frozen four-case/90-event/16-record GM-03f differential pass. Protocol/default/fidelity/training fingerprints remain exact, while content advances intentionally to `f776cb1f049bffa6b4a958d9c3c8b936dd224eb2e729ce6dfce0bdb5a8923e9f`. All eight changed Python paths pass Ruff check/format. The exact 34-path hook payload passes EOF, trailing whitespace, Ruff, and Ruff format with no edits after an isolated dependency bootstrap.

Exact staging contains all and only the reviewed 34 paths with 1,403 insertions and 41 deletions. Cached whitespace, forbidden-path, and high-confidence credential audits pass with zero matches, there is no unstaged tracked drift, and the preserved `.agents/` tree is the only untracked path. Commit A is ready.

The external multi-CLI workflow remains unavailable at the established repository-export authorization boundary. In-process plan review is compensating evidence only, and no external reviewer approval is claimed.

Executable red/green evidence, plan-review findings/dispositions, implementation review, final validation, staging, and delivery results will be added before Commit A.
