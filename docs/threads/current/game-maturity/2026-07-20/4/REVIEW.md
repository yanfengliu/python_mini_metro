# GM-05c review synthesis

Status: plan and implementation review are `CLEAN`; Commit A and evidence-only Commit B are exact-head remote green

The parent roadmap fixes the product outcome but leaves activation, hit precedence, loop endpoints, shortening range, and persistence unspecified. The first candidate tried to select handles during GM-05b's held motion. Both live-code lanes independently falsified that rule with seeded routes where a dense desktop pointer crosses another handle before the intended station while PlayerPixel jumps directly to the station. The revised contract separates selection from operation: assigned-button to empty release latches a weak transient selection, and only a fresh down beginning on a visible handle can start an edit. Plain click deletion and GM-05b full redraw therefore keep their existing event meaning.

Initial review also found station/control-hidden short-edge handles, exact coincident two-station-loop edges, same-route crossing ties, ambiguous hits falling into bare-up actions, style-authority drift, quantized centers crossing blockers, and a shortened candidate that could not hide or identify the still-live terminal edge underneath. The revised plan uses one canonical config-driven collision resolver and visible hit envelopes, collapses the two-station loop to one physical operation, consumes ambiguous handle downs, makes injected renderer style non-authoritative for interactivity, requires registered-profile round-trip clearance, draws cache-free markers above entities, and adds a non-erasing dashed/strike removal overlay.

The first revised reread found one further sampling defect: a dense button-to-empty selection stroke can cross a station and capture it, while PlayerPixel jumps directly to the empty destination. The final contract makes an in-viewport empty release authoritative regardless of intermediate draft samples; it always discards the uncommitted draft and latches selection, while off-viewport release remains cancellation. Seed-374/11 dense-versus-jump regressions now pin that terminal-target rule.

Review also caught a false pixel-evidence pattern: `PlayerPixelEnv.render()` returns the canonical 1920-by-1080 frame for both profiles, while only `step_result[0]` is downscaled to fast or fidelity resolution. The revised tests must assert cursor-masked marker and removal feedback in the actual CHW observation for each profile.

The final input reread exposed two lifecycle gaps. Desktop `main.py` drops a letterboxed mouse-up before conversion, which can leave redraw and pointer-down state armed; the revised adapter must dispatch one canonical outside-position release for cancellation. Separately, a passenger-wait tick can enter game over after handle-down, after which gameplay mouse events stop; the existing Mediator passenger-flow wrapper must clear redraw, selection, pointer, hover, and strong handle references exactly on the false-to-true transition. Resized-window and real tick-driven regressions now pin both boundaries.

The source edit captures exact path/topology identity but deliberately ignores button rebinding; the operation targets the selected path rather than its current control slot. Malformed handle/source state cancels before the hook, while broader canonical network malformation remains GM-05a preflight responsibility and may receive one false-returning call. These narrower claims match implementable evidence instead of duplicating the atomic replacement validator.

The external multi-CLI workflow remains unavailable at the established repository-export authorization boundary. In-process review is compensating evidence only, and no external reviewer approval is claimed.

Implementation proceeded TDD-first through pure handle derivation, real converted pygame events, cache-free deterministic surfaces, actual fast/fidelity CHW observations, canonical checkpoints/arrays/RNG, and live desktop adapter boundaries. The final code exposes linear endpoint extension and one-step shortening, physical-edge insertion including loop closure and the canonical two-station loop edge, and one exact public replacement call after complete transient cleanup.

Independent implementation review found six concrete gaps: an empty removal descriptor could crash rendering; pygame's exact-radius ring omitted positive cardinal pixels; shortening feedback used the raw centerline instead of the selected centered visual lane; invalid handle red differed from established network/button invalid red; opaque legacy event positions were misclassified outside without layout metadata; and cleanup assumed every test facade exposed path buttons. Each finding was reproduced, fixed, and covered before fresh review. A structural dependency whitelist regression was also updated for the intended focused modules.

Fresh input/state review verified the two-phase release/down contract, all cleanup boundaries, the live letterbox adapter, structured and array equality, both endpoint directions, fast/fidelity precedence clearance, and the frozen differential. Fresh rendering/cache review verified unified invalid color, fail-closed empty removal, endpoint orientation, visual-lane removal projection, lazy package imports, cache bounds, and deterministic pixels. Both final lanes returned `CLEAN` on live code; their transcripts are preserved under `raw/`.

A final independent integration pass then reproduced an active-handle empty-space render crash: `PathHandleEdit` correctly stored a primitive position tuple, but preview layout still assumed an entity with `left`/`top`. A real `GameRenderer.draw` regression failed with that exact traceback before production changed; the layout boundary now accepts its own primitive `Position` form, and the focused rendering/input/pixel suites plus Ruff pass. Fresh rereview returned `CLEAN` after the reviewer re-ran the original real-mediator reproducer and 63 additional real-Mediator render cases across every linear/loop handle kind and target class without exception. The final focused rendering module is 498 lines.

The definitive local py313 suite passes 719 tests in 8.045 seconds with 12 expected optional-stack skips. The 34-test compatibility run and frozen four-case/90-event/16-record GM-03f differential pass exactly; protocol and task fingerprints remain fixed while content identity advances to `7a5401c1c901ba7bdfb0283c5e8c482535c9b04d339c4ed0a5d9528944f2bfbc`. All 20 changed Python files pass exact Ruff check/format and remain under 500 lines except the explicit 660-line Mediator facade. After two hook Ruff auto-fixes were inspected and their affected tests rerun, the exact 38-path pre-commit set passes all configured hooks. Exact scoped staging plus cached exclusion, credential, whitespace, and unstaged-drift audits are clean.

Implementation Commit A `242f400bb64c9b08aa0d26de2ea2ff47c8bb4823` passed exact [run 29792200360](https://github.com/yanfengliu/python_mini_metro/actions/runs/29792200360), run number 134. Exact-head `build` job `88516178960` and exact-head `rl-smoke` job `88516179007` each passed in 4m03s. Evidence-only Commit B `b5295c0304f99cd02105cb45755235769b18275d` then passed exact [run 29792542962](https://github.com/yanfengliu/python_mini_metro/actions/runs/29792542962), run number 135: `build` job `88517182528` and `rl-smoke` job `88517182555` each passed in 4m28s. GM-06a's opening transaction reconciled that exact result and marked GM-05/GM-05c remotely finalized.

Commit A's exact staged payload contained 38 reviewed paths, no forbidden or credential-bearing path, zero cached whitespace or high-confidence credential matches, and no unstaged tracked drift. Commit B changed only the parent STATE/EVIDENCE and this iteration's PLAN/REVIEW documents.
