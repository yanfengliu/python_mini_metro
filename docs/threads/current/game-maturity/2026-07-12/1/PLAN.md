# GM-02 strategically spaced visual-history plan

Status: GM-02a through GM-02c are remotely finalized; GM-02d1 Commit A `02ceb543f251e4a84eba37d3818838f012527536` is green in run `29293092427`, with evidence-only Commit B pending

Baseline: GM-01c Commit B `14050af71df5c6cad8035904da467959767f68bb`, green in GitHub Actions run `29181130841`

## Intent

Give fresh recurrent policies materially longer route context without pretending that a contiguous RGB stack alone supplies strategic memory. Preserve the existing eight newest 10 Hz decisions, add older route-state anchors, keep episode-long memory in the recurrent policy, and bind the exact temporal interpretation to every model artifact. The twelve-frame `decision-history-v1` candidate is not promoted until measured against two declared controls under the same rollout workload.

## Non-negotiable contracts

- The player-equivalent task remains one channel-first RGB frame with the current protocol and task fingerprints. Temporal history is a trainer-side observation transform with a separate descriptor and `historyFingerprint`; GM-02 must not rewrite the task identity merely because a model consumes more channels.
- A history descriptor is immutable and canonical. Callers supply only a versioned layout name and non-negative decision offsets ordered strictly oldest to newest and ending at zero; booleans, duplicates, and unsupported names fail closed. The descriptor schema fixes the offset unit to agent decisions, sample/channel order to oldest-to-newest sample-major channel-first RGB, pre-history fill to zeros, episode reset to clear-then-insert-initial, and terminal behavior to append/copy-before-auto-reset. These closed semantic literals serialize explicitly and cannot be replaced by arbitrary fingerprinted strings the runtime ignores. The fingerprint is SHA-256 over canonical JSON and the frame count is derived from the offsets.
- `decision-history-v1` uses oldest-to-newest offsets `[128, 64, 32, 16, 7, 6, 5, 4, 3, 2, 1, 0]`. At the default 10 Hz decision rate its anchors reach 12.8 wall-clock seconds at 1x and 51.2 simulated seconds at 4x while retaining the current eight recent frames. Before sufficient history exists, unavailable samples are zero-filled rather than duplicating the first frame.
- Training-manifest v2 stores both the descriptor and its independently recomputed `historyFingerprint`. It retains `frameStack` as a compatibility summary but rejects disagreement with the descriptor length. Manifest creation takes one descriptor and derives both summary and fingerprint rather than accepting contradictory sources. The existing v1 key set remains exact: parsing a genuine v1 manifest derives an internal contiguous descriptor with offsets `[N-1, ..., 0]`, and serializing that parsed v1 object reproduces v1 without injecting v2 keys. Fresh manifests are v2.
- Manifest-v2 parsing recomputes and verifies its stored fingerprint immediately. Compatibility validation also accepts an expected history fingerprint and reports history mismatch separately from task mismatch. Resume and evaluation reconstruct the history transform from the authenticated manifest; an explicit requested history must match the saved descriptor fingerprint, not only its channel count. Genuine manifest-v1 models with any positive contiguous stack, including 1, 4, 8, and another arbitrary N, remain loadable and keep their old SB3 stacking semantics.
- GM-02a and GM-02b still execute SB3 `VecFrameStack`, so their runtime boundary accepts only a descriptor exactly equal to contiguous `[frameStack-1, ..., 0]` and rejects any other otherwise-valid v2 layout before environment/model construction. Those phases emit only the actual contiguous descriptor. GM-02c removes this temporary guard only when descriptor-driven train, resume, and evaluation construction all land together.
- The dependency-light descriptor lives in `src/rl/history.py`; the SB3/Gymnasium wrapper lives separately in `src/rl/temporal_history.py` and is imported locally inside `build_vector_env()` after dependency validation. Both modules enter the closed training-source fingerprint allowlist while the environment-content fingerprint remains unaffected by trainer-only history code.
- The temporal wrapper operates on channel-first `uint8` box observations and preserves dtype/bounds. It owns one bounded circular ring, write cursor, and maximum-valid-age counter per vector slot; it never uses `np.roll`, copies a full parent ring per decision, or shares episode history between workers. Unsupported observation spaces or malformed observations fail closed.
- Wrapper order is `DummyVecEnv` or spawned `SubprocVecEnv`, then `VecMonitor`, then temporal history. On reset, every slot is cleared and only the new initial observation occupies offset zero. On an ordinary step, the returned stack is sampled after appending that step's observation. On an auto-resetting terminal/truncated step, the wrapper validates the single-frame terminal observation, appends it to the ending ring, assembles and owns a copy of the terminal stack, and only then clears/reuses that slot for the returned replacement episode's initial observation. Missing or wrong-shape/dtype terminal observations fail closed; non-done slots, rewards, dones, and `TimeLimit.truncated` pass through unchanged. Returned stacks and terminal stacks must not alias storage that a later step mutates.
- Time-limit terminal observations remain available for Stable-Baselines3 timeout bootstrapping, and recurrent `episode_start` masks continue to come from `dones`. Training, evaluation, save/resume, and both `DummyVecEnv` and spawn-based `SubprocVecEnv` use the same descriptor.
- History storage stays `uint8`; no full-ring float conversion or per-step unbounded allocation is allowed. Default promotion is conditional on measured process-tree peak RSS and throughput, not only analytical tensor size.

## TDD and transaction sequence

### GM-02a - immutable descriptor and manifest-v2 migration

1. Add red unit tests for descriptor canonicalization, exact fingerprints, strict offsets/order/semantics/type validation, immutability, and distinct fingerprints for equal-count contiguous versus multiscale layouts.
2. Add red manifest tests for nested and top-level exact keys, descriptor/fingerprint agreement, valid-format-but-wrong digest, `frameStack` agreement, closed-semantic tampering, expected-history mismatch, malformed data failing closed, and unchanged task/protocol identity. Preserve one literal canonical pre-GM-02 v1 payload byte-for-byte and hash-for-hash; use separate parser cases to prove derived contiguous histories for N values 1, 4, 8, and another positive N.
3. Implement dependency-light `src/rl/history.py`, migrate `src/rl/manifest.py` to schema-aware v1/v2 parsing, and add the descriptor module to `TRAINING_SOURCE_PATHS`. Make fresh manifest creation accept one descriptor and derive its redundant fields. Keep the runtime on `VecFrameStack`, emit only its actual contiguous descriptor, and fail before environment/model construction when a v2 manifest requests any non-contiguous layout.
4. Update manifest, CLI, provenance, and documentation assertions. Run focused tests, full core and exact-RL suites, full Ruff/format, changed-file pre-commit, fingerprints, and a genuine old manifest smoke. Complete Commit A, pinned remote CI, Commit B evidence, and pinned remote CI before GM-02b.

### GM-02b - vectorized temporal ring

1. Build a synthetic vector environment whose pixels encode slot, episode, and decision index. Write exact-byte chronology tests at reset/t0, ordinary steps through and beyond t129, offset-128 wraparound, repeated explicit reset, true termination, truncation, missing/malformed terminal observations, and staggered done slots.
2. Implement `src/rl/temporal_history.py` as the smallest `VecEnvWrapper` with bounded per-slot `uint8` circular rings, cursors, and valid-age counters. Sample selected offsets directly into owned outputs; avoid materializing redundant historical stacks or copying the entire ring on advancement.
3. Refute cross-slot, cross-episode, and output-alias leakage with at least two independently scheduled slots and retained arrays across later steps. Prove N=1, 4, 8, and another contiguous N are byte-equivalent to SB3 `VecFrameStack`; pin output shape/dtype/bounds, `TimeLimit.truncated`, terminal-versus-reset bytes, and cleanup after close or construction failure.
4. Run focused/full gates and the A/B remote transaction before GM-02c.

### GM-02c - train, evaluate, recurrent, and legacy integration

1. Replace runtime `VecFrameStack` construction with descriptor-driven history construction and remove the temporary non-contiguous guard only after train/resume/evaluate share it. Keep the public `--frame-stack N` control as a contiguous-layout ablation and add an explicit history-layout selector. Fresh runtime remains 8-contiguous through GM-02c; `decision-history-v1` is opt-in, and only GM-02d may promote or change the default. Reject ambiguous or mismatched combinations.
2. Resolve fresh and resumed algorithm/history only after reading the manifest. Resume/evaluation must rebuild exact v1 contiguous or v2 descriptor layouts; model observation-space mismatch must fail before learning or evaluation.
3. Extend recurrent tests to cover `episode_start`, timeout bootstrap with the transformed terminal observation, deterministic evaluation, save/load, and a spawn-based multi-environment smoke. Exercise genuine pre-GM-02 model/manifest bytes rather than regenerating all compatibility evidence with current code.
4. Update CI RL smoke and user-facing commands, then run focused/full gates and the A/B remote transaction before profiling.

### GM-02d1 - remotely verified benchmark harness

1. Build and test the benchmark/supervisor before observing candidate results, then complete its own Commit A/Commit B remote transaction. The resulting clean Commit B is the exact source commit for every measured worker. Cleanliness means no staged or tracked modifications and no unexpected untracked files; the pre-existing `.agents/**` tree and ignored benchmark outputs are the only explicit exclusions, and every worker artifact records the exclusions plus the checked status. Raw results also record the commit, profile-script digests, task/protocol/training fingerprints, dependency versions, CPU/RAM, seed, both Torch thread counts, candidate order, and exact command.
2. Benchmark workers run one 8-environment x 128-step CPU `RecurrentPPO` workload with recurrent batch 64 and four epochs. Do not use `learn(total_timesteps=2048)`, which would make the linear schedule reach zero on the measured update. Pin the production 1,000,000-transition schedule horizon, call the live model's `_setup_learn()`, then drive exactly two verified `collect_rollouts(..., n_rollout_steps=128)` / progress-update / `train()` iterations: iteration one is the 1,024-transition warm-up and iteration two is measured. Assert exactly two successful collections and updates, 1,024 transitions each, nonzero measured learning rate, no early stop, unchanged batch/epoch settings, and each public per-slot valid age equal to `max(history.offsets)` after warm-up (7 for contiguous eight, 128 for multiscale layouts). This proves every candidate's retained ring is fully populated and the offset-128 anchor exists where declared. Setup/reset is recorded separately and excluded from throughput.
3. Report analytical and actual storage: raw observation payload, pre-training `rollout_buffer.observations` shape/dtype/bytes, wrapper ring `.nbytes`, one-step output shape/dtype/bytes, and every measured optimizer minibatch's observation shape/dtype/bytes, valid mask rows, and padded rows. Label 182.25 MiB only as the nominal unpadded twelve-frame batch-64 tensor; recurrent sequence padding can make actual minibatches larger. Report valid transition-row rate as `4096 / training_seconds`, padded compute-row rate separately from the summed actual padded rows, collection FPS as `1024 / collection_seconds`, and end-to-end FPS as `1024 / (collection_seconds + training_seconds)`.
4. Count instantiated trainable parameters and estimate inference-forward MACs per one valid observation row only: one shared CNN pass, separate actor/critic LSTMs, both post-LSTM `256 -> 64 -> 64` actor/critic MLP stacks, and the 64-feature action/value heads. Publish the exact Conv/Linear/LSTM formulas and assumptions; never label this estimate as training MACs or scale it by valid rows without accounting for padding and backward work.
5. Measure the entire worker lifecycle from a pre-import supervisor handshake through setup, warm-up, measured update, and teardown as the maximum sampled instantaneous sum of current `WorkingSetSize` for the root and all live descendants. Use dependency-free Toolhelp32 process discovery, retained process handles and creation times to defend against PID reuse, `GetProcessMemoryInfo`, and `GetPerformanceInfo` at an absolute 50 ms cadence. Record maximum observed sample-start gap and acquisition duration; invalidate a repeat if a live root/descendant cannot be queried, any gap exceeds 100 ms, or any acquisition itself exceeds 100 ms. Aggregate working set is a comparative metric that double-counts shared pages, not unique RAM or proof about paging. Preserve full sample JSONL under ignored `output/`; commit compact worker/aggregate summaries plus SHA-256 digests and exact commands so repository evidence stays bounded.

### GM-02d2 - matched campaign and safe default promotion

1. From the clean GM-02d1 Commit B, run at least three fresh-process repeats per candidate in counterbalanced order for 8-contiguous `[7..0]`, 8-multiscale `[128, 64, 32, 16, 3, 2, 1, 0]`, and 12-multiscale `[128, 64, 32, 16, 7..0]`. Every candidate uses the new wrapper and identical seed/task/horizon/device/thread settings. Keep raw worker JSON, supervisor samples or lossless summaries, exact commands, and one aggregate decision artifact.
2. Pre-register promotion gates before observing results: all expected repeats and contract checks pass; recurrent batch 64 and four epochs are unchanged; median full-lifecycle aggregate process-tree working-set peak is at most 1.25 times the freshly remeasured 8-contiguous median and below the conservative historical guardrail of 3.909 GiB (`4,197,256,790` bytes, the floor of the rounded published value); and median end-to-end FPS is at least 0.75 times the fresh 8-contiguous median. Equality passes for the relative memory and throughput gates, while the historical cap remains strict. The historical guardrail is not method-equivalent evidence; the fresh relative control is primary.
3. Test twelve frames first. If it passes every gate, it may be promoted. If it fails, run a new counterbalanced campaign for the ten-frame fallback `[128, 64, 7, 6, 5, 4, 3, 2, 1, 0]` with fresh interleaved 8-contiguous controls; ten may promote only under the same all-repeats, batch/epoch, memory, and throughput gates. Otherwise GM-02d remains open or a reduced workload is declared and all candidates are rerun. Neither eight-frame control is promotion-eligible and no unmeasured layout may be selected.
4. Promotion uses one shared default-history descriptor/factory in `rl.history`; both fresh CLI resolution and `build_vector_env(history=None, frame_stack=None)` consume it. Never promote by changing `DEFAULT_FRAME_STACK` alone, which would silently create contiguous history. Preserve `--frame-stack N` as an explicit contiguous ablation and add fresh CLI -> environment -> manifest assertions for the exact default descriptor/fingerprint plus resume assertions for persisted history.
5. Update `README.md`, `ARCHITECTURE.md`, the model-selection research note, `PROGRESS.md`, state/evidence, and manifests. State that GM-02d proves an engineering-safe observation contract, not improved passenger deliveries; policy efficacy remains GM-12's matched multi-seed evaluation. Run all gates, adversarial review, and the GM-02d2 A/B remote transaction before starting GM-03.

## Expected files and size boundaries

- New focused source: dependency-light `src/rl/history.py`, optional-dependency `src/rl/temporal_history.py`, a dependency-light benchmark contract module, a Windows process-resource sampler, and a narrowly scoped profile CLI; split each by role and keep each below 500 lines.
- Existing integration: `src/rl/manifest.py`, `src/rl/training.py`, `scripts/train_rl.py`, `scripts/evaluate_rl.py`, and narrowly `src/rl/policy.py` for the promoted default.
- Focused tests: new history/manifest lifecycle modules plus targeted changes in CLI, training, evaluation, and legacy-compatibility tests. Split rather than extending any test file toward the 500-line ceiling.
- Evidence/docs: this iteration's raw reviews, `diff.md`, `REVIEW.md`, benchmark artifacts, parent `STATE.md`/`EVIDENCE.md`, and public architecture/training guidance.
- No dependency declaration changes are planned. If implementation proves one necessary, stop that unit and run the repository dependency lock/audit protocol before proceeding.

## Review and validation

- Before code, three independent live-code reviewers try to refute manifest compatibility, vector lifecycle semantics, and resource/default claims. Codex and Claude CLI plan review follows the canonical runbook; any unavailable service is recorded rather than treated as approval.
- Each behavior change is test-first. After implementation, independent finder/refuter lanes inspect manifest trust boundaries, terminal/reset behavior, resume/evaluation compatibility, memory ownership, docs, and test gaps; every real finding is fixed and re-reviewed.
- Per transaction: focused tests, `python -m unittest -v` in core Python, the same full suite in `output/venv-rl`, full Ruff check/format, changed-file pre-commit, a dummy-video app smoke when shared runtime code changes, protocol/task/content/training fingerprint checks, legacy artifact smoke, and the relevant CLI/CI RL smoke.
- Local Node verification is rerun to confirm no unrelated recursive-contract regression. Until GM-04 isolates civ-engine 2.2.0 locally, the known live-sibling 2.4.1 pin mismatch is recorded exactly and pinned GitHub Actions remains authoritative for all Node tests.
- Every substep uses Commit A for implementation/tests/docs and Commit B for A's exact SHA/CI evidence. No following substep starts before the preceding Commit B is remotely green.

## Acceptance

- Manifests cannot silently reinterpret equal-channel temporal layouts; v2 descriptor and fingerprint tampering fails closed while genuine v1 contiguous artifacts remain exact and usable.
- Synthetic and real vector tests prove chronology, zero pre-history, staggered reset isolation, terminal/truncated stacks, timeout bootstrap, recurrent masks, and spawn behavior.
- Fresh training observes more than eight strategically spaced frames only after the matched resource profile proves the selected default safe. Evaluation and resume reconstruct precisely the saved layout.
- The single-frame task fingerprint remains unchanged; trainer-source fingerprint changes intentionally; environment-content fingerprint changes only if separately justified. All local applicable gates and pinned remote A/B gates pass, with limitations reported precisely.
