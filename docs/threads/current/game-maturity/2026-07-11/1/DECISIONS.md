# Game maturity decision log

## D-001 - Repository-backed persistence

Decision: `PLAN.md` is the durable scope and acceptance contract; `STATE.md` is the sole resume cursor; `DECISIONS.md` prevents design drift; `EVIDENCE.md` records verification. All four are committed and updated incrementally.

Reason: chat context and active-process state are not sufficient evidence for a multi-session effort. The repository must say what is complete, what remains, and exactly where to resume.

## D-002 - Deliveries and currency are separate concepts

Decision: `Mediator.deliveries` is the canonical cumulative objective and `Mediator.line_credits` is the spendable balance. A delivery increments both; a purchase subtracts only line credits. `total_travels_handled` remains a compatibility property alias to deliveries and `score` remains a deprecated compatibility property alias to line credits, so callers that fund purchases by assigning `score` do not silently change meaning.

Reason: the current HUD and game-over overlay call remaining spendable currency `score`, while the user and RL objective define success as total passengers delivered. One value cannot truthfully represent both after a purchase.

Status: accepted implementation contract after adversarial plan review.

## D-003 - Longer history must be temporally meaningful

Decision: the fresh policy will receive more than eight frames, but the implementation will not blindly multiply contiguous RGB frames. It will preserve immediate interaction context and add older snapshots through a manifest-bound history layout selected after resource profiling.

Reason: eight consecutive 10 Hz frames cover only 0.8 seconds. Doubling contiguous frames covers 1.6 seconds while roughly doubling rollout observation memory, which is still too short for route and reroute planning. Recurrent state remains necessary for episode-long memory.

Result: the operationally valid fallback campaign promoted `decision-history-10-fallback-v1` with ten oldest-to-newest offsets `[128, 64, 7, 6, 5, 4, 3, 2, 1, 0]`, zero-filled missing history, and explicit channel ordering. This retains the last eight 10 Hz samples plus anchors 6.4 and 12.8 seconds back while the LSTM carries episode-long memory. Its measured median peak process-tree working set was 4,043,184,128 bytes versus 3,636,346,880 for the matched eight-contiguous control (1.1119x and below both RAM gates), while throughput retained 0.8482x of the control. The original twelve-frame primary campaign was operationally invalid and its target also exceeded the strict historical RAM cap, so it remains an explicit unpromoted ablation.

Status: accepted fresh recurrent default after GM-02d profiling; passenger-delivery efficacy remains pending GM-12.

## D-004 - Product features share one simulation

Decision: menus may wrap sessions, but route editing, resources, maps, upgrades, saves, manual controls, structured actions, recursive play, and pixel RL must all drive one canonical gameplay model.

Reason: parallel feature implementations would invalidate determinism, tests, and the claim that the pixel policy faces the same game as a player.

## D-005 - Compatibility before decomposition

Decision: complete the score/reward and history-contract migrations before splitting `Mediator`, then keep `Mediator` as a facade over composed collaborators.

Reason: doing the same contract migration across temporary and final module boundaries would increase risk and make artifact fingerprint changes harder to reason about.

## D-006 - Initial overload-pressure correction

Decision: rename the mechanic around an explicit overdue-passenger threshold, use two as the initial default, and retain `max_waiting_passengers` as a compatibility alias. One overdue passenger warns but does not end a default game; the second ends it. An explicitly configured threshold of one preserves the old behavior.

Reason: two is the smallest change that removes the current one-passenger failure cliff while retaining visible time pressure. The scripted paired-seed evidence was independently reproduced and remains directional only; GM-11 still owns final balance tuning from broader human and policy evidence.

## D-007 - Two-commit remote finalization

Decision: each independently green work unit uses implementation Commit A followed, after green CI, by metadata Commit B that records A's SHA/CI and advances the resume cursor. Commit messages carry exact `[<work-unit>:A]` and `[<work-unit>:B]` markers; B is located as the newest commit that changed the goal's `STATE.md` with the expected marker. Commit B is pushed and its CI must be green before new implementation starts; a later session queries B's CI and reopens the unit if it failed. Failing TDD tests are working-tree phases inside a work unit, not remotely finalized commits.

Reason: a local cursor advanced only after CI is not durable on `origin/main`, while endlessly committing the latest metadata CI result has no terminal transaction.

## D-008 - Explicit persisted-schema compatibility

Decision: GM-01 adds checkpoint schema v2 plus v1 normalization, recursive input schema v2 with explicit reward contract and legacy v1 score-delta replay, versioned agent-play records, writable compatibility aliases, and reconstructable pixel terminal-metrics versions. GM-02 adds manifest v2 with a separate validated history fingerprint while retaining the single-frame task fingerprint and arbitrary v1 contiguous stacks.

Reason: source/content drift flags cannot repair a protocol or persisted-document schema that the loader no longer understands.

## D-009 - Runtime selections belong to task identity

Decision: map ID and map-definition version will be included in RL task identity, manifests, train/resume/evaluate CLI reconstruction, saves, and high-score keys. Task descriptors and fingerprints become explicitly versioned; genuine legacy artifacts reconstruct and hash the exact pre-map descriptor rather than inserting fields and expecting the old hash to match.

Reason: hashing source code does not distinguish two maps selectable from the same revision.

## D-010 - Pause reasons precede modal product systems

Decision: GM-07a replaces internal boolean-only pause ownership with explicit pause reasons while retaining the `is_paused` compatibility facade. Menus/save, tutorial, and weekly choices build on that established contract.

Reason: introducing pause reasons only with weekly progression would force save and tutorial schema migrations and could let the Space key dismiss a non-manual modal pause.

## D-011 - Network progression has one internal owner behind the facade

Decision: GM-03b introduces a dependency-free `NetworkProgression` aggregate as the sole owner of current line/station/economy progression scalars, rules, and cached counts. `Mediator` retains explicit writable properties, real public method wrappers, all station/button collections and effects, and the exact per-delivery public-hook order. The aggregate holds no mediator backreference, entities, UI objects, RNG, or clocks.

Reason: a stateless controller over a broad mutable mediator protocol would relocate functions without extracting ownership, while duplicated controller state would break direct writes, checkpoints, and cached unlock semantics. A single internal store plus an explicit facade preserves the current API and gives GM-07 saves and GM-10 upgrades a narrow evolvable boundary without speculative serialization.

## D-012 - Route planning is stateless; mutation stays explicit

Decision: GM-03c extracts deterministic route filtering, search, compression, constrained selection, and lazy boarding/bulk proposals into a stateless dependency-light `RoutePlanner`. It receives fresh graphs, ordered candidates, factories, and resolver thunks; it owns no RNG, entity collection, map, graph, route, clock, or cache. `Mediator` keeps every public route method, RNG shuffle, graph build, `travel_plans` mutation, arrival/removal, topology, and passenger/metro effect. Newly created constrained plans may be wired while unowned, while bulk plans are installed before the public next-hop hook exactly as at baseline. Bulk proposals explicitly distinguish raw arrival, route, and fallback; arrival effects run while the destination iterator is suspended, iterator finalization then precedes the same-passenger fallback guard, and the in-frame selection loop retains baseline locals through those facade effects. Callable getters are resolved before argument evaluation and invoked without retaining the temporary callable after the call.

Reason: search-only extraction cannot restore the required mediator size trajectory, but moving passenger or topology ownership early would blur GM-03d/e and risk identity/timing drift. Lazy proposals move enough deterministic planning while letting the facade apply each result before iteration resumes, preserving callback rebinding, RNG, live-list mutation quirks, sentinel identity, checkpoint-visible object identity, iterator finalization, and Python local/callable lifetime side effects. Resolver thunks also preserve baseline reads of mutable path IDs at each short-circuited comparison and re-read a rebound `travel_plans` mapping between membership and indexed access.

## D-013 - Path lifecycle is stateless over canonical facade state

Decision: GM-03d extracts exactly 12 path creation, completion, invalidation, removal, and button-assignment transition algorithms into a stateless, non-retaining `PathLifecycle`. Each call receives an explicit topology-limited host; `Mediator` keeps every real public method and remains the sole canonical owner of directly writable lists, maps, flags, RNG, entities, and effects. The lifecycle re-reads host state at baseline expression points, dynamically invokes nested public hooks, and resolves `Path`/`Metro` factories through getter thunks only at their original construction points.

Reason: mirrored aggregate state or proxy properties would consume the below-1,000 line budget and enlarge the identity/rebinding surface used directly by tests, environments, checkpoints, routing, passenger flow, and rendering. A call-scoped transition coordinator moves the coherent behavior boundary without hidden delegation, retained backreferences, duplicated collections, or cross-call cache. The 168-line source envelope and 57-line hard replacement/wiring budget guarantee the required reduction without stealing GM-03e passenger-flow or GM-03f input/layout scope.

## D-014 - Passenger flow is stateless over canonical facade state

Decision: GM-03e extracts 16 passenger spawning, simulation tick, metro exchange, waiting/game-over, and bulk route-proposal application algorithms into a stateless, non-retaining `PassengerFlow`. Each call receives an explicit call-scoped host plus getter thunks that preserve late module-global, graph/search/plan, router-iterator, and progression-hook lookup. `Mediator` keeps every exact public signature and remains the canonical direct owner of entities, collections, maps, RNG, clocks, progression, routing, pause/speed/game-over state, overload configuration, and public effects.

Reason: passenger spawning and exchange form one temporal mutation boundary whose observable behavior depends on three fresh graph phases, live collection iteration, generator suspension/finalization, per-rider collaborator rebinding, partial failures, and exact delivery/transfer/boarding order. A retained controller or eager wrapper capture would change those seams, while moving route queries or input/layout behavior would steal GM-03c or GM-03f ownership. The reviewed 346-line removal and 97-line explicit facade/wiring model projects `Mediator` to 735 lines without proxy state or magic delegation.

## D-015 - Input coordination is stateless over canonical facade state

Decision: GM-03f extracts exactly 19 path-button UI, timing/layout/render, mouse/keyboard, pause/speed, and structured-action algorithms into a stateless, non-retaining `InputCoordinator`. Each call receives an explicit call-scoped host plus getter thunks that resolve numeric configuration, pygame factories and keys, update helpers, runtime types, event enums, and the lazy renderer factory at their original expression points. `Mediator` keeps every exact public signature and remains the canonical direct owner of UI collections, hit rectangles, compatibility renderer, progression, topology, clocks, input flags, pause/speed/game-over state, and public effects.

Reason: human mouse/keyboard input, programmatic actions, layout hitboxes, compatibility rendering, and path-button effects form one player-interface coordination boundary whose observable behavior depends on live-list order, `isinstance` subclass precedence, short-circuiting, public-hook rebinding, partial failures, and Python bound-method evaluation order. Retained controller state, eager dependency capture, adjacent ownership theft, or generated delegation would break those seams. The cohesive boundary reduces `Mediator` from 735 to 605 lines; the reviewed mathematical facade floor makes the parent under-500 target impractical without hiding behavior, while the explicit implementation remains below the 625 hard ceiling and the new component remains below 500 lines.

## D-016 - Recursive execution uses one isolated physical engine pin

Decision: the canonical civ-engine descriptor is checked in under `scripts/`, while its retained Git checkout lives at the explicitly ignored repository root `/.civ-engine-pin/`. Package metadata, regenerated lock metadata, npm link semantics, CI checkout/build, provenance, and runtime resolution must agree on that physical root. Provenance resolves ESM without executing it and rejects a wrong physical location even under `--allow-dirty`; GM-04b owns the credential-free idempotent setup/verifier command and public pre-hooks. The unrelated `../civ-engine` sibling is never setup input or mutation scope.

Reason: a version string or descriptor alone cannot attest the code Node executes. Local-file installation is configuration-sensitive, bare ESM resolution can be shadowed below `scripts/`, and a mutable sibling is independent user state. One dedicated ignored checkout plus realpath/runtime-entry identity, Git commit, clean status, and complete runtime digest makes the local and CI boundary reproducible without changing historical persisted summary schemas.

## D-017 - Public Node bodies guard after a trusted bootstrap and child execution stays shell-free

Decision: GM-04b treats the tracked package scripts and `.npmrc`, selected top-level npm and Node executables, and their pre-start environment/configuration as an explicit trusted bootstrap boundary. The actual setup and guard mains share a dependency-light assertion that requires `NODE_OPTIONS` to be unset or empty and `process.execArgv` to be empty before their own effects. After that clean startup, each canonical Node test/playtest body passes through the identity guard, `npm test` has the complete zero-argument `node --test` child body, and npm lifecycle pre-hooks remain defense in depth. The exact root `.npmrc` fixes link semantics and silences the standard expanded-script prelude. On Windows and POSIX, setup validates the active Node distribution's npm launcher/CLI and launches its physical `npm-cli.js` through exact `process.execPath`; the engine build likewise launches only the physical pin-local TypeScript CLI through that Node executable, never `npm.cmd`, `cmd.exe`, an npm build lifecycle, or caller `PATH`.

Reason: npm's `ignore-scripts` setting can suppress pre-hooks while still executing a requested script body, unrestricted post-start test arguments can become child Node CLI surfaces, and npm can echo forwarded arguments before the guard runs. However, npm `--node-options`, ambient `NODE_OPTIONS`, or direct Node startup options can execute caller-selected code before any repository module starts; no Node assertion can prevent or undo those effects, so a pre-Node trusted launcher would be required to distrust that boundary. The shared assertion detects observable taint and stops later entry-point effects without claiming pre-main attestation. Separately, live Windows evidence proves direct `npm.cmd` with `shell: false` returns `EINVAL`, while wrapping it in `cmd.exe` reintroduces metacharacter parsing, and an npm build lifecycle would restore executable search. Within the trusted bootstrap, fixed guarded bodies, silent tracked npm policy, validated JavaScript CLIs, and exact Node execution preserve the intended child-process boundary.

## D-018 - Route replacement is an atomic identity-preserving transaction

Decision: GM-05a preserves the exact live path, public ID, color/button ownership, fleet, riders, and physical metro poses while replacing unique normalized station order and loop state only after an off-live candidate and semantic metro bindings validate. Moving logical edges map by oriented station trip, padding maps by its complete retained station transition, and stopped metros map from a reconstructed arrival edge. Unsafe or ambiguous edits reject without effects. Every waiting rider in a stable captured holder order receives one scoped refresh against the committed graph, while every onboard plan becomes a checkpoint-visible one-alight marker on its current line using a Node from that fresh graph and receives one scoped fresh route only after that safe alight; removal of an edited-line rider's target alight rejects. No new pending field or canonical checkpoint/replay schema is introduced.

Reason: live `Path.update_segments()` preserves a numeric segment index rather than a semantic edge and can teleport a metro after insertion or reorder. Clearing an onboard plan can strand the rider because unloading depends on its target station, while retaining the old route tail can make post-edit routing stale. A two-phase transaction plus an existing-field one-alight marker makes continuity, route freshness, rollback, RNG timing, and replay state explicit without changing public identities or the canonical checkpoint/replay schema.

## D-019 - Manual redraw is transient facade state with topology-aware rendering

Decision: GM-05b uses the existing converted primary-pointer stream to arm an assigned path button, accumulate an immutable identity-based `PathRedrawGesture` off the live network, and dispatch the existing public atomic replacement hook exactly once only on a valid station release. The draft is direct transient `Mediator` UI state and is deliberately absent from checkpoint v2, structured observations, reference oracles, RNG, clocks, and topology. The old line stays operational while a separate bounded render cache draws the candidate with live centered-lane order; interpolation snapshots retain exact segment identity and normalized physical-transition keys, rebase only a proven retained transition, and otherwise render the live pose.

Reason: a persistent selection mode or new structured action would enlarge the protocol and save schemas before GM-05c establishes ergonomic handles, while mutating a live `Path` during drag would expose partial topology to routing and simulation. Numeric segment indexes alone are not stable across atomic replacement and caused a confirmed zero-fixed-step visual jump. One facade-owned immutable draft plus identity-checked render snapshots preserves legacy click deletion, purchase, speed, creation, canonical replay, cache bounds, and metro continuity without hiding gameplay state in the renderer.

## D-020 - Route handles use two-phase weak selection and shared primitive geometry

Decision: GM-05c latches a weak exact-path selection only when an assigned-button redraw terminates on empty in-viewport space, then starts an edit only from a distinct fresh down on one visible canonical handle. Linear routes expose outward endpoint extension/one-step shortening plus physical-edge insertion; loops expose insertion only, with coincident reverse edges of a two-station loop collapsed to one canonical slot. One config-owned collision-resolved builder returns immutable primitive-only descriptors used by both hit testing and cache-free rendering. An active edit alone holds the exact source topology strongly, remains off-live, clears before dispatch, and commits through the existing public atomic `replace_path` hook exactly once.

Reason: motion-activated handles make dense desktop sampling diverge from destination-jump pixel control, and raw route anchors can collide with stations, controls, short edges, crossings, or quantized action coordinates. A persistent selection or new action/save field would unnecessarily enlarge protocol and checkpoint state. Two-phase activation, weak idle selection, shared render/hit descriptors, and exact-source validation preserve click deletion, full redraw, station precedence, replay identity, cache bounds, and player/pixel-policy reachability while keeping incomplete edits out of gameplay state.

## D-021 - Locomotive availability is derived from canonical total and assignment

Decision: GM-06a retains writable `Mediator.num_metros` as total locomotive capacity and assigned-only `Mediator.metros` as the canonical live collection, then exposes read-only `available_locomotives = max(0, num_metros - len(metros))`. Fresh and reset games construct no unassigned Metro entities; line completion keeps its transitional automatic one-locomotive assignment until GM-06b adds explicit controls, and line removal changes availability only through actual removals from the global assigned collection. The third HUD line and labeled structured `fleet` object expose the resource, while entity arrays, PlayerPixel info/actions, checkpoints, replay schemas, and task fingerprints remain unchanged.

Reason: total and assigned locomotive facts already exist at every runtime and checkpoint-v1/v2 boundary, so a mutable available counter, preconstructed object pool, or new persisted field would duplicate state and create disagreement, identity, migration, and rollback hazards. A late-derived clamped value preserves legacy direct cap writes and partial lifecycle failures exactly, remains reconstructible from `progression.limits.num_metros` plus canonical `structured.metros`, and gives both players and structured callers explicit inventory feedback without stealing GM-06b assignment, GM-06c carriage, or GM-06d removal-hardening scope.

## D-022 - Explicit fleet control uses assigned entities and versioned queue intent

Decision: GM-06b replaces transitional route-completion allocation with explicit player-visible plus/minus controls and strict `assign_locomotive`/`unassign_locomotive` actions over the same canonical total and assigned collections. Assignment creates one fresh Metro only after validation and installs the exact identity transactionally into one active path plus the global collection. Unassignment selects the last empty nonqueued Metro on that path, keeps it assigned while moving, blocks both boarding permission and candidate production, forces the next real-station stop by identity, and returns availability only after exact detachment from both collections after the complete passenger-flow tick. Redistribution is the composition of source queued return and destination assignment; there is no target reservation or third resource pool. Checkpoint v3 explicitly maps queue booleans across its global structured prefix and full motion union. Recursive-input v4 and agent-play v4 require index-only persisted fleet actions and an explicit fleet contract; genuine older versions use immutable normalization or one shared pre-tick legacy automatic-assignment adapter and reject new fleet fields/actions before mutation.

Reason: a mutable free pool, destination reservation, or early refund would contradict D-021 and create duplicate ownership. Immediate removal while moving can teleport a resource, and accepting an occupied train before GM-06d can destroy or strand riders. Queue intent changes future simulation despite identical assigned/available counts, so silently adding it to checkpoint v2 would violate the roadmap's versioning invariant. Dedicated visible controls preserve player equivalence; index-only records avoid UUID-dependent fresh-process replay; and a pre-tick adapter preserves historical action meaning and timing without keeping automatic assignment in current gameplay.

## D-023 - Carriages are attached entities over a derived fungible inventory

Decision: GM-06c starts with two carriage units, each adding six seats. `Mediator.num_carriages` is the writable total; unassigned units remain fungible and are never materialized in a second live pool. Each assigned `Carriage` exists exactly once in one canonical global `Metro.carriages` list, and availability is derived as `max(0, num_carriages - assigned_carriages)`. A detached identity reachable only through a historical object graph is retired, never counted or reattached. `Metro` remains the sole passenger holder, while its writable-compatible total capacity is derived from a nonnegative base plus ordered attached carriage capacities; total writes below attached capacity or current occupancy reject atomically. Path-level attach selects the eligible nonqueued Metro with the fewest carriages, then earliest path order; detach selects the eligible capacity-safe nonqueued Metro with the most carriages, then latest path order, and removes its last carriage. Paused play permits both actions, terminal play rejects, and queued locomotives are ineligible. Safe locomotive detachment and successful line removal refund an attached whole consist by removing its owning Metro from the canonical global collection; existing late partial line-removal recovery, occupied-locomotive return, cancellation/recovery, and destructive rider cleanup remain GM-06d.

Checkpoint v4 persists complete carriage inventory and indexed owner/attachment identity, capacity, and motion references without serializing entity IDs. Recursive-input v5 and agent-play v5 require an explicit carriage-action contract and index-only persisted `attach_carriage`/`detach_carriage` actions. Checkpoint v1-v3, recursive v1-v4, and agent-play v1-v4 reject carriage state/actions and preserve their exact historical projections and behavior. PlayerPixel gains visible reachable carriage controls and pixels through the existing mouse protocol, but no privileged info or new low-level action kind.

Implementation correction: the reviewed y=1050 path-button row and y=1014 resource row were infeasible with the existing 30-pixel path radius, 8-pixel resource radius, y=972 station-center maximum, 30-pixel station extent, 8-pixel quantization margin, and 1080-pixel viewport. GM-06c preserves the 80-pixel slot pitch while using 24-pixel-radius path buttons at y=1056 and 8-pixel-radius resource controls at y=1019, satisfying every strict station/control/path/viewport inequality without weakening the adversarial boundary tests or changing station generation.

Service and rendering correction: one identity-bound station-service cache applies destination, then executable transfer, then boarding priority. Pure queries and reconciliation restore RNG, travel-plan, mapping, and holder effects; one executable identity consumes each 500-millisecond interval, and composition changes plus successful route replacements reconcile stopped trains. Rendering samples ordered bodies from coherent interpolation endpoints, follows route arclength through bends and loops, and uses a clearance-constrained terminal-turn trajectory so folded consists do not collapse during reversal.

Reason: attached entities provide visible identity and deterministic composition without duplicating unassigned resource state. Equal per-unit capacity matches the existing six-seat locomotive and keeps the feature playable before GM-10 rewards; GM-11 retains authority to tune these provisional balance values from evidence. Deterministic path-level targeting preserves manual/structured parity without process-local Metro selectors. Derived capacity and inventory prevent rollback drift, while explicit schema versions prevent new actions or future-affecting composition from silently changing old recordings.

## D-024 - Station capacity is a soft cap for forced alights; line removal and queued return never delete riders

Decision: GM-06d makes station capacity a soft cap for forced alights while leaving every ordinary spawn, boarding, and transfer path capacity-gated exactly as today. Removing a line alights every onboard rider at a deterministic real station through an overflow-permitted placement that may briefly exceed `station_capacity`, credits a delivery when the alight station matches the rider's destination shape, clears the rider's travel plan, resets its wait clock, and re-plans it through the ordinary sweep; unreachable riders remain ordinary waiting passengers and the overflowed station drains through normal boarding with no new spawn pressure while over capacity. A queued locomotive stopped at a real station applies the same overflow-permitted forced alight to riders with no executable exit on its line, so an occupied queued return always drains within bounded travel and then settles through the existing empty-at-station rule. `remove_path` becomes a snapshot/rollback transaction over its complete touched footprint, and a narrow reconcile seam repairs only provably-safe residual fleet shapes (clearing a queue flag on a globally absent Metro and dropping only an empty globally absent `path.metros` entry) while refusing everything else.

Reason: the owner selected overflow-permitted alighting over blocking busy-line removal or dropping riders, because rider conservation is the GM-06 acceptance criterion and a brief station crowd that drains naturally is the least surprising player outcome. Independent conservation and lifecycle reviews proved the hard-cap alternative asserts mid-removal on any crowded train (`station_capacity` 12 against up to 18 seats per consist) and that no reconcile can safely repair a half-removed line, so transactionality plus a soft cap eliminates the malformed states instead of repairing them after the fact. Station capacity remains a provisional GM-11 balance value.

## D-025 - GM-07a ships four screen states; the settings screen moves to GM-08a with its store

Decision: GM-07a's `AppController` ships `TITLE`, `PLAYING`, `PAUSE_MENU`, and `GAME_OVER` as the explicit screen states, and the `SETTINGS` navigation state listed in the GM-07 roadmap scope moves to GM-08a, landing together with the typed settings store it edits. The controller's state enum is extensible and nothing references a settings screen until GM-08a exists. Pause reasons stay process-local runtime state in GM-07a (`user` and `menu` only); serializing reasons remains GM-07b scope, and the tutorial and weekly-choice reasons arrive with their owning increments.

Reason: a settings screen without the GM-08a store would be an empty navigation stub referencing configuration that does not exist, violating the no-speculative-surface convention; GM-08 already depends on GM-07, so landing the screen beside its store adds no ordering risk. Recording the scope amendment here keeps the roadmap's GM-07 bullet honest rather than silently narrowing it inside one iteration's plan.

## D-026 - Save v1 pins ASCII canonical bytes, a config-owned save directory, and rules identity

Decision: GM-07b persists games as strict schema-v1 JSON documents encoded exclusively by the pinned ASCII recipe — `json.dumps(..., allow_nan=False, ensure_ascii=True, separators=(",", ":"), sort_keys=True).encode("ascii")` plus one trailing LF, no CR — written through a save-local mkstemp→fsync→`os.replace` atomic writer rather than the RL `write_json_atomic` helper. The save directory is config-owned: `config.save_dir_name = "saves"`, repo-root-relative, created on demand, ignored via an anchored `/saves/` line, and every save/load function also accepts explicit paths so callers and tests choose their own locations. Each document pins the `"stateContract": "mini-metro-save-v1"` and `"rulesVersion": "rules-v1"` literals, and the accompanying policy is that any trajectory-affecting balance-config change must bump the save schema version in the same commit; a v1 save whose `numPaths` or fleet totals disagree with the running config is rejected outright. Map identity is deliberately absent from schema v1; a future schema version that introduces map selection synthesizes `classic` for older saves.

Reason: the byte-pinned frozen fixture and cross-process replay oracles require one exact encoding, and the RL writer re-dumps UTF-8 (`ensure_ascii=False`), so reusing it would diverge fail-open on the first non-ASCII value; a dedicated ignored directory keeps user saves out of the provenance-guarded trees without touching `.gitattributes`; the pinned rules literal plus the bump-in-same-commit policy makes a stale save fail closed instead of silently replaying under changed balance rules; and deferring map identity avoids inventing a speculative surface while the game has exactly one implicit map.

## D-027 - GM-07c autosaves at quiescent boundaries to a single file, and game over deletes it

Decision: GM-07c wires the GM-07b save API into the app shell as a single canonical autosave at `saves/autosave.json` under `save_dir_name`. Autosave writes on entering the pause menu (immediately after the menu reason is held, where the letterbox cancel has already cleared every armed gesture so the saver's quiescence preflight always passes), on Exit-to-title before the menu reason is released (a byte-identical redundant write), and on a window-close `pygame.QUIT` only when the game is mid-run — the pause menu, or un-over play. The controller's own `SystemExit` sites write nothing: a title-screen exit would overwrite the real autosave with a pristine new game, and the game-over exits are post-delete. Game over deletes the autosave — at the PLAYING-to-GAME_OVER promotion for the interactive path, and on any exit surface (including window close) whenever `mediator.is_game_over` is true — so a finished run can never be Continued. Continue appears on the title screen when the file exists, proves loadability at click, releases the `menu` reason while preserving a persisted `user` pause, and enters play; a corrupt or unreadable save shows a notice and stays on the title screen. Every autosave and delete swallows `(ValueError, OSError)` so persistence never blocks play or exit, and the atomic writer guarantees an interrupted write retains the previous valid save.

Reason: a single autosave slot with delete-on-game-over matches Mini Metro's roguelike resume model — you can leave and come back to a live run, but a finished run is gone — and avoids a save-scumming path where closing the window at the game-over screen would otherwise resurrect the run; the forgiving alternative (keep the final save) is a one-constant future change. Autosaving only at quiescent, mid-run boundaries keeps every write legal against the saver's gesture and canonical-fleet preflights without new saver logic, and the state-gated window-close policy closes the frame-accurate race where `is_game_over` flips during a tick with no promoting event before the quit. The screen-reconciliation `release_pause_reason("menu")` on Continue is exactly the lever GM-07b deferred to this unit. Save-directory naming and a multi-slot model remain open for later units; D-026's config-owned directory and rules identity are unchanged.

## D-028 - GM-07d high scores persist a ranked deliveries leaderboard keyed by map and rules version

Decision: GM-07d records a persistent high-score leaderboard to `saves/highscores.json` (already ignored) as a strict versioned document `{schemaVersion: 1, stateContract: "mini-metro-highscores-v1", entries: [...]}` written through a save-local copy of the reviewed GM-07b atomic writer (mkstemp then fsync then `os.replace`, validating the board before every write) and the D-026 ASCII canonical-bytes recipe. Each entry carries a `map` and a `rulesVersion` as separate fields plus a nonnegative-integer `deliveries`; `map` is the placeholder `"classic"` until GM-09 introduces real maps, and `rulesVersion` is `SAVE_RULES_VERSION` (`"rules-v1"`). Entries rank by `deliveries` descending within each `(map, rulesVersion)` key, and at most ten entries are retained per key. The loader validates structure strictly but is start-empty tolerant: a missing, unreadable, malformed, or forward-version file yields an empty leaderboard and never crashes or blocks the game — unlike a save, which fails closed. A completed run is recorded at game over exactly once, reading only `mediator.deliveries` and mutating no mediator state: at the PLAYING-to-GAME_OVER promotion, and, mirroring the D-027 autosave delete, at the window-close game-over race in `main` gated identically (state PLAYING or PAUSE_MENU and `is_game_over`). The recorder returns a result carrying the new entry's rank and whether it is the key's new best; the game-over screen shows a compact best indicator, drawn in `main` after the renderer's game-over frame so the near-ceiling `game_renderer` is untouched. The recorder lives in a new `src/highscores.py`, injected into `AppController` as a seam like autosave so the controller and every headless or RL surface stay import-clean; the persistence isolation scan is extended to forbid the module there.

Reason: reusing the reviewed GM-07b persistence and D-026 canonical-bytes and rules identity repeats its reviewed atomicity shape (as a small save-local copy, since the GM-07b writer is coupled to `serialize_game` and not importable) and its fail-closed structure validation; deliveries rather than credits is D-002's lifetime objective; the map-plus-rules key predates GM-09 through the `"classic"` placeholder D-026 already reserved and stays extensible to real map identity; start-empty-on-corrupt is the correct fail mode for a cosmetic leaderboard because a bad high-score file must never block play the way a bad save legitimately refuses to load; and drawing the best indicator in `main` keeps the game-over overlay renderer under its line ceiling without a speculative split. Score-table pruning depth, additional per-map boards, and a full ranked-list screen remain open for later units.

## D-029 - GM-08a ships a typed settings store plus a SETTINGS screen with all three roadmap categories

Decision: GM-08a adds `src/settings.py` — a typed frozen `Settings` value persisted to `saves/settings.json` as a strict versioned document `{schemaVersion: 1, stateContract: "mini-metro-settings-v1", ...}` written through a save-local copy of the reviewed GM-07b canonical-ASCII atomic writer and the shared scalar validators, validating before every write. Loading is START-EMPTY tolerant like the highscores leaderboard and unlike a save: any missing, unreadable, malformed, or forward-version file falls back to the typed defaults and never raises; saving RAISES on failure with the best-effort swallow at the `main` layer. The store carries all three roadmap categories (the user chose the full three-category store over a real-consumers-only subset): display (`fullscreen: bool`), audio levels (`masterVolume`/`musicVolume`/`sfxVolume` as integer percents 0-100 so canonical bytes stay unambiguous), and accessibility (`reducedMotion: bool`). GM-08a wires the two categories with a live consumer today — `fullscreen` applied by `main.run_game` through `pygame.display.set_mode` flags when it changes, and `reducedMotion` threaded into the render path to hold the passenger-warning and unlock blinks steady rather than flashing and to suppress the one-shot snap-blip rings — while the audio-level fields are defined now and consumed by GM-08b's mixer/`NullAudio` backend (a roadmap-sanctioned define-then-consume split, not a schema change). A new `AppScreen.SETTINGS` state, a byte-stable `draw_settings_menu` primitive, and settings entries in the title and pause layouts navigate to and edit the store through an optional inert `settings` seam injected only by `main.run_game`, so headless, agent, and RL entries never construct or read a settings file. Settings stay presentation-only and never touch `Mediator` or `config` balance, so no save-schema version bump is implied (D-026). If threading `reducedMotion` would push the near-ceiling `game_renderer` past 500 lines, the blink-phase decision is extracted into a small helper rather than growing that file.

Reason: the user chose the full three-category store, so the persisted schema is complete at v1 and GM-08b adds only the audio consumer, never a schema migration; integer-percent volumes avoid float canonical-representation ambiguity. Keeping settings presentation-only and injected as a main-only seam inherits the proven save/highscores isolation and sidesteps D-026's balance-schema coupling. Fail-safe-to-defaults matches the roadmap acceptance and the cosmetic-file stance already established for high scores, and the SETTINGS screen lands beside its store per D-025. Real key rebinding, per-map or per-rules settings, and additional accessibility presentation options remain open for later units.

## D-030 - GM-08b adds procedural-tone audio through a main-only backend seam driven by a per-frame counter differ

Decision: GM-08b makes the game audibly reactive with short deterministic PROCEDURAL tones and no external audio assets (the owner chose procedural-tones-now over silent-until-assets), through a new main-only `src/audio.py` holding its own tone constants (importing only `pygame`/`numpy`, never `config`), a `ProceduralAudio` backend that builds `pygame.mixer.Sound`s from fixed numpy int16 waveforms via `pygame.sndarray` — generated against the mixer's ACTUAL negotiated sample-rate and channel count read from `pygame.mixer.get_init()`, so a pre-initialized mixer never detunes or mis-shapes them — a `NullAudio` no-op backend, and a factory that initializes the mixer AND builds every sound inside one `try/except` and returns `NullAudio` on any failure, so audio-init failure can never block gameplay or tests (the roadmap acceptance). Audio is a pure `main.run_game` loop-level consumer, NOT an `AppController` seam (the controller surface is unchanged — audio is not a screen): `run_game` takes an optional `audio_backend` that DEFAULTS to an inert `NullAudio`, and the real mixer is constructed only at the `__main__` entry point, so no test or embedder — even one driving `run_game` unbounded — ever opens a mixer (a structural isolation mechanism, not a `max_frames` gate; two independent review lanes flagged the gate as leaky against tests that patch only `main.pygame` while `audio.py` holds its own). A per-frame consumer at the existing post-advance hook (beside `reconcile_game_over`, reading the post-reconcile state) diffs a small counter set on the live mediator — `deliveries`, `unlocked_num_paths`, `unlocked_num_stations`, `is_game_over` (False->True), and a total snap-blip count — playing one SFX tone per newly-occurred event; it owns its OWN session reference and re-baselines the snapshot on a session change so Continue/New Game/Restart never replay a stored delta as a spurious burst (the snap tone and a purchase in the exact same event batch as a session swap are accepted best-effort misses, like snap, and never a schema-bumping counter). Because those counters already exist, no domain-event bus is added and the `Mediator`, `GameSession`, and `rendering` layer stay 100% audio-free; audio lives OUTSIDE `rendering/` (which `rl/player_env.py` imports transitively) in a main-only module that no headless, agent, recursive, or RL surface imports, extended into both persistence-isolation scans. The backend reads the GM-08a volumes each frame — event tones are SFX at effective gain `(master/100) * (sfx/100)`; `music_volume` is defined but has no consumer until a future music track (a roadmap-sanctioned define-then-consume split, as the volumes themselves were in GM-08a). Tones are byte-stable given the fixed generator, so generation is unit-testable, and an `SDL_AUDIODRIVER=dummy` path exercises a real mixer headlessly in CI while the NullAudio path covers no-device degradation.

Reason: the owner chose audible procedural tones, so the game reacts now without sourced assets and recorded `.wav`/`.ogg` can replace the generator later behind the same backend. A per-frame counter differ over the existing lifetime counters is the lightest-touch, best-isolated integration — it captures both tick-time (delivery, station unlock, game over) and input-time (path purchase, snap) events uniformly without a new event bus, without touching the mediator/session/renderer, and without any risk of a headless surface pulling in a mixer. Fail-safe-to-`NullAudio` matches the roadmap's audio-init-cannot-block acceptance and the established best-effort persistence-swallow stance, and the main-only seam plus extended isolation scans inherit the proven GM-08a/GM-07 isolation. SFX-only keeps GM-08b tight; background music, richer per-event mixing, and real recorded assets remain open for later units.

## D-031 - GM-08c adds a coached in-game tutorial: a real seeded game with a real-control-gated overlay step machine

Decision: GM-08c teaches the game through a COACHED SEEDED GAME (the owner chose this over a dedicated curated tutorial scenario): a new `AppScreen.TUTORIAL` runs a real `Mediator(seed=42)` game with a coaching overlay, and a pure `src/tutorial.py` step machine observes the live mediator each frame (the GM-08b snapshot-differ pattern, reading attributes only) and advances seven lessons as the player performs each real action — draw a line, reroute it, add a train, deliver a passenger, observe overload pressure, pause, and change speed. It is menu-launched from a title "Tutorial" entry APPENDED after Settings (so the prior four rects stay byte-identical), skippable via Escape (with the `_handle_playing` letterbox-cancel), and stateless across runs (no autosave, no high score, no first-run flag, no settings/schema change). The tutorial is an optional inert `build_tutorial` seam on `AppController` exactly like `build_from`/`autosave`; a seam-less controller keeps the entry inert. `main.run_game` supplies the seam, calls a public idempotent `advance_tutorial(elapsed_ms)` once per frame beside `reconcile_game_over`, and paints `draw_tutorial_overlay` over the real game frame; the controller exposes the overlay strings so `main` never imports the tutorial module, and `tutorial` joins the isolation scan so no headless/agent/recursive/RL surface constructs it. `TUTORIAL_SEED = 42` is the empirically-probed seed whose three initial stations are all distinct shapes, so any line the player draws enables deliveries. The one deviation from "reads the mediator only" is that `build_tutorial` sets `mediator.overdue_passenger_threshold = 10**9` on the seeded INSTANCE (verified to keep the sim alive 120 s+ while warning rings still show): without it the game would flip `is_game_over` at ~40 s of inattention, which permanently freezes the sim and paints "Game Over" chrome, soft-locking the time-dependent lessons — a per-instance write, exactly as `seed=42` is, touching no Mediator class, no config, and no other game. Two empirically-forced design choices: reroute PRECEDES the train (a metro mid-service persistently blocks `replace_path` under the strict GM-07b path-lifecycle default, so a train-less line reroutes reliably), and the overload lesson is an OBSERVATIONAL dwell step (auto-advancing after ~4.5 s of unpaused play, or sooner if a passenger enters the warning window) because a good player never overloads and a pure crowd/wait gate would soft-lock. Line purchases are taught conceptually through the deliver-to-earn step, since the coached-normal-game approach cannot force an actual line purchase (~90 deliveries) without altering the economy.

Reason: the owner chose a coached real game, so a first-timer learns the actual controls (not a stripped scenario), and the roadmap's "deterministic tutorial state machine driven entirely by real controls and events" maps directly onto a per-frame observer over the existing mediator counters — the same lightest-touch, best-isolated integration as GM-08b audio, with no domain-event bus, no `Mediator`/`GameSession`/`rendering`/schema change, and the mediator's code unchanged. The step machine reuses the proven duck-typed tolerant snapshot pattern so it can never crash the loop on a partial host. An empirical seed/sim probe and a combined adversarial plan review drove the two load-bearing corrections (game-over suppression to prevent the freeze; reroute-before-train to prevent the reroute block) BEFORE code, and an end-to-end scripted-gesture headless run confirmed every lesson advances on its real signal and the tutorial never game-overs. Menu-launched (not auto-forced) keeps first launch unchanged and needs no persisted "seen" flag; skippable-from-any-step and never-game-over guarantee no soft-lock. Dedicated-scenario polish, auto-launch-on-first-run, tutorial audio, and an actual in-tutorial line purchase remain open for later.

## D-032 - GM-09a introduces a data-only versioned `MapDefinition` and a behavior-preserving `Classic` map

Decision: GM-09 (maps and geographic constraints) opens with GM-09a, split — after a DUAL adversarial plan review recommended it — into the map ABSTRACTION (this decision) and the versioned task-descriptor IDENTITY (GM-09a2, deferred). GM-09a adds `src/maps.py`: an immutable frozen `MapDefinition` (`map_id`, `map_definition_version`, and the station-shape palette `shape_types`/`unique_shape_types`/`unique_spawn_start_index`/`unique_spawn_chance`, the palettes coerced to tuples in `__post_init__` for enforced deep immutability), the `CLASSIC` definition capturing today's exact config values, and a VERSION-AWARE `resolve_map(map_id, version)` that raises a clear named error for an unknown id or unsupported version (never falling back to the wrong map). `maps.py` imports ONLY `config` + `geometry.type` — never `pygame`, `entity`, or `mediator` — so `get_entity`/`mediator` consume a `MapDefinition` ONE-WAY, the module stays import-safe for every headless/RL path, and there is no cycle. `Mediator.__init__` gains an optional `map_definition` param (default `CLASSIC`) and threads the map's palette into `get_initial_station_pool` → `get_random_stations`, which gained keyword-only palette params defaulting (None-sentinel) to the config globals so every existing caller draws byte-identically. The change is BEHAVIOR-PRESERVING: the Classic map reproduces pre-change construction AND a 300-step trajectory byte-for-byte (proven by pre-change fingerprint regression locks over stations + path colors + both RNG states + trajectory, seeds 0 and 1, plus a 60-seed list-vs-tuple equivalence check and an independent pre-change reconstruction in review). `save_game.serialize_game` gains a fail-closed `_require_classic_map` guard: only `classic@1` (or a `map_definition`-less Mediator) may serialize, so a future non-Classic map can never be silently written as Classic before the save-schema migration — the guard adds no save bytes and the frozen `save-v1.json` stays byte-identical. Station counts and spawn bounds stay global (deferred to the terrain/progression integration units); the save-schema map field and the high-score `mapDefinitionVersion` defer to GM-09f; the versioned RL task descriptor/manifest/CLI is GM-09a2.

Reason: the roadmap's "versioned task-descriptor identity and a Classic map definition that preserves current behavior" spans the RL protocol, the training manifest, the save schema, the CLI, and station spawning — the highest-risk surface in the roadmap, all under strict legacy-byte-compatibility. The dual plan review (harness + external Codex ultra, both NOT CLEAN) empirically verified the byte-compat core but recommended splitting to isolate the deterministic-behavior change (this unit) from the persistence/versioning change (GM-09a2), so each is separately reviewable; it also drove `maps.py` data-only (Codex flagged that wrapping the spawn pipeline would pull pygame), the version-aware lookup, the tuple immutability, the fail-closed save guard, and a determinism proof over RNG states + colors + trajectory rather than station bytes (station IDs are unseeded UUIDs, and station and color draws share one `python_random` stream). Parameterizing the shape palette via keyword-only args defaulting to config globals keeps every existing caller and construction path (env, RL, recursive, save-load) byte-identical, so the abstraction lands with zero behavior change and a foundation the river/terrain maps extend. The external review lane was unavailable for the implementation (a safety egress decline); a rigorous harness lane that independently reconstructed the pre-change code plus a 60-seed empirical byte-identity proof substituted, appropriate for a behavior-preserving refactor.

## D-033

Decision: GM-09a2 completes the GM-09 opening with the VERSIONED task-descriptor IDENTITY (the second half of the roadmap's GM-09a, split out at D-032). The load-bearing invariant: a map-ABSENT descriptor is byte-identical to the pre-map code, so every genuine pre-map manifest keeps its exact fingerprint. `TaskSpec` (`src/rl/protocol.py`) gains `map_id`/`map_definition_version` APPENDED after `max_episode_steps` (default `None`, so every positional call site and a bare `TaskSpec()`/`PlayerPixelEnv()` stay legacy) with a `_validate_map_identity` guard (exactly `(None, None)` or a non-empty ASCII id + positive non-bool version); `task_descriptor` adds `mapId`/`mapDefinitionVersion`/`descriptorVersion:2` ONLY when `map_id is not None` — `canonical_json` sorts keys per descriptor independently, so the presence of the map keys IS the version signal and no key is ever added to the legacy descriptor. Verified: the reference `TaskSpec(fast,6,deliveries,4)` keeps its exact legacy fingerprint `c2ef342f…`, and the map-bound classic@1 spec hashes to `efec72da…`. The training manifest gains an explicit v3 (`src/rl/manifest_schema.py`: `TRAINING_MANIFEST_SCHEMA_V3`, `_V3_KEYS = _V2_KEYS | {mapId, mapDefinitionVersion}`); v1/v2 stay exact-key-valid and map-free; `to_dict` emits the history block for BOTH v2 and v3 (widened) and the map keys for v3; `__post_init__` keeps schema and map in lockstep (v1/v2 map-free, v3 map-bound). `create_training_manifest` selects v3 only for a map-bound task; `task_spec_from_manifest` reads the manifest's map fields (None for v1/v2 → legacy hash; set for v3 → map-bound), and `PlayerEnvThunk`/`make_env_thunks` thread the pair to subprocess workers so they rebuild the same map. `PlayerPixelEnv` gains map params (default None), resolving the map once and passing `map_definition` to the Mediator. The CLI `scripts/train_rl.py` gains `--map` (choices from `KNOWN_MAP_IDS`, default None): a FRESH run stays map-free (v2) when omitted or binds to the named map's current version when given; a RESUMED run INHERITS the map identity from its manifest (`_resolve_map_identity`, which reads the manifest tolerantly via `getattr` and rejects a conflicting explicit `--map`), so a genuine pre-map run still resumes — the resume manifest is now parsed BEFORE the spec is built. The real git-ignored legacy manifest is committed sanitized to `scripts/fixtures/legacy-training-manifest-v1.json` so CI runs the highest-risk regression (it reconstructs to exactly `c2ef342f…`). `EXPECTED_LF_TRAINING` is re-pinned (`4f4b8f32…`) because the training-pipeline sources legitimately changed; `maps.py` stays out of `TRAINING_SOURCE_PATHS` but is captured by `compute_content_fingerprint`'s `src/**` glob, so map-code changes still invalidate a task's content fingerprint. The high-score `mapDefinitionVersion` and the save-schema map field remain deferred to GM-09f; structured-observation map identity and the profiler/README "fresh is v2" flip also defer there.

Reason: the roadmap requires that "genuine pre-map manifests reconstruct and hash the exact legacy descriptor bytes, while new manifests use the map-bound descriptor." The additive-keys-only descriptor switch achieves byte-exact legacy compatibility (both review lanes independently reproduced the two hashes), and folding it through an explicit manifest v3 keeps v1/v2 untouched. The dual plan review's two MAJORs are addressed structurally: resume inherits the map from the manifest rather than a forced `--map` default (a non-None default would fail a legacy resume's fingerprint check), and the manifest history conditionals are widened so a v3 (a v2 superset) never drops history. Committing the real legacy fixture closes the Codex gap that the highest-risk regression lived only in git-ignored `/output/`. Keeping `--map` opt-in (fresh omitted → v2) rather than defaulting fresh runs to Classic scopes GM-09a2 to the versioning mechanism without the broader "every fresh artifact is map-bound" flip (README/profiler/observation), which lands with the GM-09f integration where the map field becomes meaningful across save/high-score/observation together.

## D-034

Decision: GM-09b adds the FIRST alternate map — a `RIVER` map with terrain/station regions — and DEFERS the save-schema/high-score map fields to GM-09f (the roadmap's save/high-score integration unit), keeping the fail-closed save guard. `src/maps.py` `MapDefinition` gains two additive, deeply-immutable terrain fields defaulting to empty — `spawn_regions` (the land rects where stations may spawn) and `rivers` (obstacle bands to render), each a tuple of `(left, top, right, bottom)` float rects tuple-coerced and positive-area-validated in `__post_init__`. `RIVER = MapDefinition(map_id="river", version=1, <CLASSIC palette>, spawn_regions=(left_bank, right_bank), rivers=(centre_band,))` is a single vertical river down the screen centre (x in [0.46, 0.54]·width) splitting the play area into two banks, registered so `resolve_map("river", 1)`, `map_by_id("river")`, and `KNOWN_MAP_IDS == ("classic", "river")` expose it. The banks are ERODED inward by `station_size` so a 30-px station glyph never overlaps the water. Region-aware spawning is STRICTLY ADDITIVE (like GM-09a's palette): `get_station_spawn_position` gains a keyword-only `spawn_regions` threaded through the full `get_random_stations → get_random_station → get_station_spawn_position` chain; `_sample_position` REJECTION-samples a candidate outside every region (bounded at 64 tries per candidate, raising a named error rather than hanging), and the no-region fast path is FALSY (`if not spawn_regions`) so the empty tuple CLASSIC passes returns the first draw unconditionally — the CLASSIC RNG stream and the frozen `save-v1.json` positions are byte-identical (proven by the unchanged `test_gm09a_maps` `_CONSTRUCT_FP`/`_TRAJ_FP` and the save-determinism fixture). `get_random_position` is UNTOUCHED (its draws are the pinned bytes; rejection lives in the caller). A new small `src/rendering/terrain_renderer.py` (`draw_terrain`) paints the river bands with `pygame.draw.rect` at the TOP of `GameRenderer.draw` (before the network) — so the human loop, the RL pixel observation (`player_env` calls `GameRenderer.draw` directly), the compat path, and tests all see the river once, under the lines/stations; CLASSIC (empty `rivers`, or an attr-less state via `getattr`) paints nothing (byte-identical, RNG-free). The save guard `_require_classic_map` is HARDENED to STRUCTURAL equality against canonical `CLASSIC` (the frozen `MapDefinition.__eq__` compares all fields), so a forged `MapDefinition("classic", 1, rivers=…)` is rejected, not silently written as plain Classic. No `geometry.Polygon` is used (it imports shapely, mints a uuid, and has a broken `contains`): regions are plain tuples, rendered with pygame and tested with a pure point-in-rect helper; `maps.py` stays import-safe (the scan now forbids `shapely`/`geometry.polygon` too).

Reason: the dual plan review (harness + external Codex ultra, both NOT CLEAN) exhaustively confirmed no save-capable path constructs a river `Mediator` in GM-09b — the human shell builds only the default Classic map (menu selection is GM-09f) and the RL path uses authenticated checkpoints, not `save_game` — so deferring the high-risk save-v2 migration (which must synthesize `classic` for the byte-frozen `save-v1.json`) to GM-09f, where map selection makes the field meaningful, is roadmap- and D-032/D-033-aligned and keeps the CLASSIC determinism locks untouched. The review drove the falsy `()` sentinel (else CLASSIC hangs / the top lock moves), the full spawn chain including `get_random_station`, the bounded rejection + eroded banks + positive-area assertion, the structural guard (Codex found the forged-Classic bypass via a live probe), the `GameRenderer.draw` hook (else the RL river task is blind to its own river), and avoiding the broken `geometry.Polygon`. GM-09c adds the crossing/tunnel MECHANICS that make the river a real obstacle; the river is spawn-constraining + visual here.

## D-035

Decision: GM-09c makes the river a REAL obstacle via a finite per-map tunnel budget whose remaining count is DERIVED from the live network rather than stored. `MapDefinition` gains an additive `tunnel_budget: int | None` (validated non-negative-int-or-None in `__post_init__`, rejecting `bool`); `RIVER` sets `3`, `CLASSIC` stays `None` (unbounded). A new dependency-light `src/crossings.py` — importing ONLY `geometry.point`, reading hosts by duck-typed `getattr` — owns (a) the pure geometry: `segment_crosses_band(start, end, band)` is a Liang-Barsky slab returning the ENTRY point (a zero-length grazing touch, `t_enter >= t_exit`, deliberately does NOT count — a determinism tie-break), and `path_crossings(positions, is_looped, rivers)` counts one entry per band on a path's CENTERLINE of consecutive `station.position` pairs plus the loop closure, where a 2-station loop's closure RETRACES its single segment and is skipped so it cannot double-charge; and (b) the SINGLE route-edit gate `within_tunnel_budget(host, stations, is_looped, *, exclude=)` shared by both edit paths (unifying what began as two identical helpers). `Mediator` exposes `num_tunnels`, `consumed_tunnels`, and `available_tunnels` as three DERIVED properties — `num_tunnels` reads `map_definition.tunnel_budget`, `consumed_tunnels` sums `path_crossings` over every committed, non-drafting line, and `available_tunnels` is `max(0, num_tunnels - consumed)` (or `None` when unbounded). NONE is a cached field: reading the budget live from `map_definition` (the same single source as `rivers`) means a swapped map stays consistent and a stale copy can never fail open (impl-review Codex MAJOR), and the derived-count pattern (like `available_locomotives`) makes removal/reroute REFUND for free with ZERO snapshot fields. The read-only gate `within_tunnel_budget` reads the budget + rivers from `map_definition` and counts the REAL resolved draft (`creating.stations`/`is_looped`) — never a route predicted from raw indices, since the construction dedups an explicit-closure `[X,Y,X]` to the 2-station loop `[X,Y]` and can loop-form on a repeated station, so a raw-index pre-check both false-rejects a valid loop and false-accepts a mid-repeat (re-review Codex/harness MAJORs). It runs at the two points that see the final draft, BEFORE any extend/loop mutation: `path_lifecycle.end_path_on_station` on the interactive/programmatic release, and `finish_path_creation` at the COMMIT boundary where `is_being_created` clears (catching a direct `start/add/finish` that bypasses `end_path`). An over-budget release therefore adds no station and commits no crossing; the rejection is count-, path-, and RNG-inert (draft construction draws no RNG). It may leave a transient snap-blip on a station the aborted drag touched — exactly the feedback any drag-abort paints, UNCHANGED from pre-GM-09c: the shared `abort_path_creation`/`assign_paths_to_buttons` are byte-for-byte the pre-change code, so CLASSIC (whose gates always pass) stays byte-identical. A re-review proved that scoping a snap-blip rollback (or a draft-skipping button pass) INTO those shared methods broke CLASSIC's abort/button bytes and mis-owned a reclaimed-color blip — so the pre-existing transient-blip-on-abort and the pre-existing ghost-button-after-mid-draft-removal are left to a focused follow-up, not folded here. `path_replacement.replace_path` gates in preflight (excluding the line being rerouted) before `build_candidate`. `env.observe` adds a `tunnels` block (`total`/`consumed`/`available`) as a SIBLING of `fleet` — never a fleet key, because `recursive_checkpoint`'s v4 pass-through asserts an EXACT fleet-key set (`set(fleet) != _FLEET_KEYS_V4` would raise for EVERY map incl CLASSIC); `_normalize_observation` reconstructs from a fixed whitelist and ignores the sibling, so the canonical checkpoint and every save fixture stay valid. `rendering/terrain_renderer.draw_crossings` paints a tunnel-portal marker at each crossing ON TOP of the network (lazy-importing `crossings` inside the function, as `network_renderer` does with `config`, so `src.rendering` stays importable during unittest discovery); CLASSIC (no rivers) draws nothing, so its frame stays byte-identical.

Reason: a DERIVED count (not a stored counter) is the load-bearing choice — because `available_tunnels` is recomputed from the live network on every read, removing or rerouting a line refunds its crossings automatically and a rejected or rolled-back edit can never leave a stale charge, so GM-09c adds NO new snapshot/transaction state to the already-complex path-edit rollback machinery. Counting on the logical CENTERLINE (station positions) rather than the lane-offset, `path_order`-dependent, `round()`-quantized `PathSegment` geometry makes a line's crossing count depend only on its own stations, keeping the mechanic deterministic and draw-order-independent (the entry point's `round()` sets only the marker pixel, never the count, which uses `len()`). The budget of 3 for RIVER was empirically verified to leave a connected cross-river network buildable while making the water a real constraint — a same-bank line needs no tunnel and is always allowed even at zero remaining, and a 4th distinct crossing is rejected. The gate-before-mutation placement and the SIBLING observation block were both driven by the dual plan review (a harness MAJOR corroborated by Codex on the checkpoint whitelist; two Codex MAJORs on two-station-loop double-charge and the rejected-creation snap-blip leak). GM-09c completes the river as an obstacle; in-game map-menu selection and the save-schema/high-score map fields remain GM-09f.

## D-036

Decision: GM-09d adds the SECOND alternate map, `DELTA`, as a PURE `MapDefinition` addition with NO new machinery — its whole purpose is to prove the GM-09b/GM-09c map layer generalizes beyond one river/two banks. `DELTA` places TWO vertical channels (`rivers` = two bands centred at 0.32·width and 0.68·width, each 0.03·width half-width) that split the play area into THREE `station_size`-eroded land banks (`spawn_regions` = left/mid/right), with `tunnel_budget=4`; it is registered so `resolve_map("delta", 1)`, `map_by_id("delta")`, and `KNOWN_MAP_IDS == ("classic", "delta", "river")` expose it. A line spanning the whole map crosses BOTH channels and so consumes two tunnels, exercising the multi-band `path_crossings` count and the finite budget more than the single-river `RIVER`. The addition is provably additive: the region-aware spawn (`_sample_position` accepts a candidate inside ANY of the regions), `terrain_renderer.draw_terrain`/`draw_crossings` and `crossings.path_crossings` (loop over ALL bands), the derived `consumed_tunnels`/`available_tunnels`, the observation `tunnels` block, and the fail-closed `_require_classic_map` save guard already handle N regions/rivers with no code change — so CLASSIC and RIVER stay byte-identical (proven by the unchanged `test_gm09a_maps` fingerprints and frozen `save-v1.json`, and a direct CLASSIC first-station guard). The GM-09b test's exact-`KNOWN_MAP_IDS == ("classic", "river")` assertion was loosened to membership so each later map does not require editing an unrelated test.

Reason: the map layer was designed in GM-09b (regions/rivers) and GM-09c (tunnel_budget/crossings) to be data-only and N-ary; GM-09d is the cheapest, highest-value confirmation that a second, mechanically-distinct map needs only a new `MapDefinition` — no changes to spawn, render, crossing-count, gate, save, or observation code. Two channels (rather than a rotated single river) was chosen because it stresses the newly-proven MULTI-band crossing count (a full-span line = two tunnels) and the finite budget across three banks, where a horizontal river would only re-test a rotation the axis-aligned `segment_crosses_band` already handles. `tunnel_budget=4` was empirically verified to keep the three banks connectable in the early game (a left-mid-right network uses two tunnels, leaving two spare) while still bounding the network; the two channels at 0.32/0.68·width leave a positive-width mid bank (~516 px at 1920×1080) with `_coerce_rects` enforcing positive area at import. The in-game map-menu selection and the save-schema/high-score map fields still defer to GM-09f; GM-09e (third map) is next.

## D-037

Decision: GM-09e adds the THIRD alternate map, `LAKE`, as another PURE `MapDefinition` addition with NO new machinery — but chosen to exercise the ONE generality dimension RIVER (one full-height channel) and DELTA (two full-height channels) never did: a PARTIAL band. `LAKE.rivers` is a SINGLE bounded rect — a central lake at x∈[0.40, 0.60]·width, y∈[0.34, 0.66]·height — that spans NO screen edge, so `crossings.segment_crosses_band` (axis-aligned Liang-Barsky) is exercised on a rect bounded in both x AND y: a line whose centerline passes through the lake counts one crossing; a line routed around it counts none. `spawn_regions` is a FRAME of four overlapping land strips — top/bottom (full-width), left/right (full-height), each eroded from the lake by `station_size` — whose union is the whole screen minus the lake vicinity; the region-accept spawn (`_point_in_rects`, accept a candidate inside ANY region) needs no change, and the strips' overlap at the corners is harmless. `tunnel_budget=3`. Registered so `KNOWN_MAP_IDS == ("classic", "delta", "lake", "river")`; CLASSIC/RIVER/DELTA stay byte-identical (unchanged `test_gm09a_maps` fingerprints + frozen `save-v1.json`).

Reason: with RIVER/DELTA the map layer was proven for full-screen-height channels only; a bounded lake is the cheapest way to prove `segment_crosses_band`/`path_crossings` are correct for a band bounded on all four sides — the geometry a future irregular coastline or island would rely on. The lake is also mechanically DISTINCT: because it spans no edge, a line can OFTEN be DETOURED around it, so crossing is frequently a SHORTCUT (a line tunnelling straight through saves distance) rather than mandatory. But — a line bends only at STATIONS, so a dry detour needs an intermediate station beside the lake; the budget therefore still limits TOTAL crossings, and a station whose only routes all cross the lake is gated until a tunnel is freed by rerouting/removing a line, exactly as on the channels (re-review Codex MAJOR corrected an earlier over-claim that the lake "never gates connectivity"). `tunnel_budget=3` is a genuine constraint — the lake makes crossings MORE OFTEN avoidable than the full-screen channels, not always. This softer, more-routable flavour is the intended distinction, documented in `GAME_RULES.md`.

## D-038

Decision: GM-09f begins the map/save integration (roadmap GM-09f, SPLIT into save-schema (this) → high-score identity → menu, per the dual plan review) with the SAVE-SCHEMA v2 map field. The save schema gains `SAVE_SCHEMA_VERSION_V2 = 2` (a strict SUPERSET of v1) that adds two additive top-level keys `mapId`/`mapDefinitionVersion`, so a non-Classic game (river/delta/lake) can be saved and loaded; `SAVE_SCHEMA_VERSION` becomes 2 (new saves are v2) and `SUPPORTED_SAVE_SCHEMA_VERSIONS = {1, 2}`. `stateContract`/`rulesVersion` are STABLE across v1/v2 — only `schemaVersion` and the two map keys change. `validate_save` is TWO-PHASE: it reads + support-checks `schemaVersion` (named `ValueError`, never a `KeyError`) BEFORE choosing the version-aware exact-key set (`_TOP_LEVEL_KEYS_V1` vs `_V2`), so a v1 doc carrying map keys AND a v2 doc missing them both fail closed; the v2 map identity is scalar-validated (non-empty ASCII `mapId`, positive non-bool `mapDefinitionVersion`). `serialize_game` drops the old `_require_classic_map` guard for a fail-closed pair: (1) STRUCTURAL — `mediator.map_definition == resolve_map(id, version)` (generalizing GM-09b's `== CLASSIC`), since a v2 save records only the IDENTITY and rebuilds terrain from the registry on load, so a forged/drifted `MapDefinition("classic", 1, rivers=...)` that would reload as the real terrain-free CLASSIC is rejected; and (2) STATE-LEGAL — `_require_legal_map_state` (every station on the map's `spawn_regions`, `consumed_tunnels <= num_tunnels`), applied on serialize AND after reconstruction on load, so a hand-forged v2 doc (a CLASSIC state relabeled `river@1`, whose stations sit in the water, or an over-budget state) is refused. `deserialize_game` reads `mapId`/`mapDefinitionVersion` for a v2 doc and SYNTHESIZES `classic@1` for a v1 doc (keys absent), resolves via `resolve_map` (fail-closed on unknown id / unsupported version — never a silent Classic fallback), and threads `map_definition` into the `Mediator`. Tunnel counts stay DERIVED (no persisted counter). The byte-frozen `scripts/fixtures/save-v1.json` is UNCHANGED on disk (its SHA/length pins hold) and still loads as CLASSIC; because a v1→v2 upgrade only changes the header, a v1 load RE-SAVES as exactly the new frozen `scripts/fixtures/save-v2-classic.json` (15485 bytes, SHA `60f2bc16…`) — a deterministic transform pinned by the idempotence + cross-process determinism tests, both committed LF (no `.gitattributes` eol pin, per the recursive-source-provenance guard).

Reason: the map layer (GM-09a–e) built maps behind a fail-closed save guard that deferred persistence to "when map selection makes the field meaningful". Three maps now exist, so a save must record which one. The DUAL plan review (both lanes REVISE, direction + split confirmed) drove the two load-bearing choices: the guard must be STRUCTURAL (mere resolvability fails open into the GM-09b forged-Classic bug — verified: 2 of 20 seed-0 CLASSIC stations sit in RIVER's band), and identity alone is insufficient without STATE-legality (a valid identity + illegal state is still corrupt). Recording only `(mapId, version)` and rebuilding terrain from the import-safe registry keeps the save minimal and the tunnel budget derived; synthesizing `classic@1` for v1 keeps the frozen fixture valid, exactly as D-026 blessed. The high-score `mapDefinitionVersion` and the in-game map menu are the next two sub-units, IN THAT ORDER — the menu must land last so it can never expose an alternate map to the still-classic-hardcoded high-score recorder.

## D-039

Decision: GM-09f2 (second GM-09f sub-unit) makes the HIGH-SCORE leaderboard record the MAP identity, so the recorder is map-aware BEFORE the in-game menu (GM-09f3) makes non-Classic maps selectable. Both game-over surfaces are UNIFIED on the live mediator: `app_controller._record_highscore` hands the seam `self.mediator` (not `self.mediator.deliveries`), and `main.run_game`'s promotion closure drops its `SimpleNamespace(deliveries=...)` wrapper, so the frame-accurate reconcile and the window-close QUIT both call the IDENTICAL `record_highscore(mediator)`. `main.record_highscore` reads `mediator.deliveries` AND `mediator.map_definition.{map_id, map_definition_version}` — DIRECTLY (no `or classic` default): a real `Mediator` always has `map_definition` (default CLASSIC, GM-09a), and an exotic mediator lacking it records NOTHING (swallowed to None) rather than mislabelling a score. The `highscores` schema becomes v2: `HIGHSCORES_SCHEMA_VERSION = 2`, each entry keyed by the full `(map, mapDefinitionVersion, rulesVersion)` identity via ONE shared `_identity` helper used by the canonical sort, the per-key cap, AND the rank count (so no predicate keys on a subset and miscounts a score across definition versions); `record_score` gains a required `map_definition_version` (never defaulted) validated by `_positive_int`, and the entry `map` is tightened to the save's mapId grammar (`_map_id`: non-empty ASCII, no whitespace) in both `record_score` and `validate_highscores`. `stateContract` stays `mini-metro-highscores-v1` across the additive version (as the save kept `mini-metro-save-v1`). A legacy v1 board is NOT migrated: `load_highscores` (unchanged) validates as v2, so a v1 board fails and START-EMPTIES like any other unreadable format. `highscores` stays gameplay-free (no `maps` import — `record_score` takes the version as a plain int) and in the persistence isolation set.

Reason: the leaderboard schema already keyed by `map` (GM-07d) but the recorder hardcoded `classic`, and `mapDefinitionVersion` was deferred here. Landing map-awareness NOW (while the human shell still only builds Classic) means GM-09f3 adds the menu with ZERO recorder change — the ordering the D-038 split mandated. The DUAL plan review (both lanes REVISE, design UPHELD) drove three load-bearing choices. (1) The whole-mediator seam is REQUIRED, not merely convenient: a minimal `(deliveries, map_id, version)` context would force the CONTROLLER to read `map_definition`, reintroducing the attribute access the GM-07d MAJOR-3 invariant forbids; passing the mediator lets the controller touch NOTHING (the recorder reads), satisfying MAJOR-3 more cleanly and making both game-over surfaces provably identical. (2) A v1 board START-EMPTIES rather than migrating to `classic@1`, because — UNLIKE the save, where GM-09a's `_require_classic_map` guard PROVED a v1 save was genuinely Classic — a v1 highscores `map="classic"` label is NOT provably accurate: GM-09f made non-Classic saves loadable (verified: `load_autosave → load_game → resolve_map → Mediator(map_definition=non-classic)`, `build_from` plays it), and the classic-hardcoded recorder would have labelled such a Continue run `classic`. Synthesizing `classic@1` would preserve that contamination and assert precision the data lacks; discarding a cosmetic, START-EMPTY-tolerant board is honest and also eliminates a migrate-before-validate normalization hazard (a forged `schemaVersion: true`/`1.0`/extra-key v1 board). (3) The leaderboard reads the map DIRECTLY with no `resolve_map` structural guard — a DELIBERATE asymmetry with the save: highscores stores identity only and reconstructs no terrain, so a syntactically-valid-but-unknown or drifted id recording under its claimed key is harmless (a save reloading wrong terrain is not). The `or DEFAULT` fail-open lesson from GM-09f is honored: defaulting to Classic on a missing map would MISATTRIBUTE the already-reachable non-Classic Continue run, so the read is direct + fail-safe. GM-09f3 (in-game map menu) is the final sub-unit.

## D-040

Decision: GM-09f3 (the FINAL GM-09f sub-unit) adds the in-game MAP MENU — a title-screen picker so a human can actually select `classic`/`river`/`delta`/`lake`, completing the map/save integration. `AppController` gains `current_map_id` (default `classic`), cycled in place by a new appended title control `map` (the picker button shows `Map: {Name}` and clicking it advances over `KNOWN_MAP_IDS`, wrapping). The `build_game` seam becomes uniformly `Callable[[str], GameTriple]`: `__init__` seeds `current_map_id` first and calls `build_game(current_map_id)`, and `main.run_game`'s `build_game(map_id)` resolves `map_by_id(map_id)` into `Mediator(map_definition=…)` (the whole terrain/crossing/save/high-score/RL stack was already map-aware, GM-09a–f2). NEW GAME (the title `new_game` click and the `Enter` shortcut) builds `current_map_id`; RESTART (pause-menu Restart, game-over `R`, game-over Restart) rebuilds the CURRENT game's map read live off `self.mediator.map_definition.map_id`, via a dedicated `_restart_current_game`; and Continue (`_continue_game`/`build_from`) installs the SAVED map and never consults the picker. The tutorial stays Classic (`build_tutorial` unchanged — a fixed coached lesson). The appended `map` key keeps `new_game`/`continue`/`exit`/`settings`/`tutorial` rects byte-identical (`_stacked_buttons` positions by ordinal); `draw_title_screen` gains a `current_map_id` param and `main` threads `controller.current_map_id` into it.

Reason: the save (GM-09f) and high-score (GM-09f2) recorders were deliberately made map-aware FIRST, so the menu — landing LAST — needs zero recorder change: selection simply flows the chosen `MapDefinition` into the one place that builds the game. The DUAL plan review (both REVISE, architecture UPHELD) drove three load-bearing choices. (1) The EXPLICIT `build_game(map_id)` seam over a zero-arg closure that reads `controller.current_map_id`: `build_game` is passed INTO `AppController` before the controller exists, so a back-reference would need a late-binding forward/nonlocal ref; the cost is a uniform (grep-driven) update of every controller `build_game` test fake. (2) RESTART preserves the CURRENT game's map, NOT the picker (Codex MAJOR-1, verified reachable): with the picker on `lake`, Continuing a River save then Restarting must replay River — so restart reads `self.mediator.map_definition.map_id`, while only New Game/Enter use the picker. (3) The crossing/tunnel GATE must compose with selection end-to-end (Codex MINOR-3): a selected River game's committed crossing consumes a tunnel against the budget, not just paints terrain. Alphabetical cycle order (`classic→delta→lake→river`) is kept (deterministic, violates no curated-order contract); Classic leads and is the default. Editing `app_controller`/`main`/`menu_screens` rotates the live environment-content fingerprint, so a pre-GM-09f3 RL manifest fails resume/eval by default — expected and correct; no frozen fixture is repinned (`EXPECTED_LF_TRAINING` pins only the RL training sources, untouched here). This COMPLETES GM-09f (maps + save/high-score/menu integration); GM-10 (weekly progression) opens next.

## D-041

Decision: GM-10a opens GM-10 (weekly progression) with the simulation CALENDAR — a deterministic week boundary that pauses the sim for an explicit player continue, the foundation the later sub-units (GM-10b offers, GM-10c choice UI, GM-10d-g upgrades, GM-10h persistence) build on. A "week" is `config.WEEK_LENGTH_STEPS` sim steps (provisional 1200 ≈ 20 s at 1×, a GM-11 balance target). `Mediator.increment_time`, AFTER the complete tick (post queued-return settlement — placement matters, review MAJOR), holds a new `"week"` pause reason when the calendar is enabled, this tick crossed a new boundary (`old_steps // W < steps // W`, integer division so 1×/2×/4× never skip), and the run is NOT game over (so a terminal tick promotes to GAME_OVER, review MAJOR). `"week"` joins `_PAUSE_REASONS` and freezes the sim through the existing gate; unlike `"user"`/`"menu"` it is never cleared by the Space toggle or speed buttons. `week_index` is DERIVED from the already-persisted `steps` (no new stored scalar). The human shell promotes a pending boundary to a new `AppScreen.OFFER` modal via `AppController.reconcile_week_boundary()` — run per-frame AFTER `reconcile_game_over`, no-op unless PLAYING/not-terminal, cancelling any armed gameplay gesture through the pinned letterbox-cancel before switching (review MAJOR) — whose armed Continue (down→up on the control) calls `mediator.resolve_week_boundary()` and resumes PLAYING (review MAJOR). Closing the window mid-offer resolves then autosaves (window-close→Continue promise, review MINOR); the offer frame's audio deltas are consumed silently (review MINOR); saving is blocked while a boundary is pending (a clearer error over `validate_save`'s existing vocabulary rejection). The calendar is OPT-IN, default OFF: only INTERACTIVE `main.run_game` (`build_game`/`build_from`, gated on `max_frames is None`) enables it; RL (`MiniMetroEnv`/`PlayerPixelEnv`), the tutorial, frame-limited/headless smoke runs, and all tests leave it off. NO save-schema and NO checkpoint-schema change; the RL observation of the week and mid-offer persistence are deferred to GM-10b/GM-10h.

Reason: the DUAL plan review (both REVISE) was decisive — the harness caught a BLOCKER and Codex went far deeper (2 BLOCKER + 4 MAJOR with reproduced live counterexamples). (1) My first plan resolved a headless freeze only in `MiniMetroEnv._complete_step`, but `PlayerPixelEnv` (the first-class RL boundary) drives via `GameSession.advance_exact` and the tutorial is a third direct-Mediator shell — all would soft-lock permanently at step 1200. GATING the calendar to interactive human play (a `week_calendar` flag, default OFF) resolves every headless shell structurally (the hold never occurs) and removes the `env.py` change entirely. (2) My "a pause is trajectory-invariant" premise was FALSE — my probe used direct `increment_time` (fixed 17 ms) and bypassed the `(17,17,16)` `FixedStepClock` cadence, which a pause resets, so `time_ms` diverges at identical `steps`; but gating keeps the calendar out of every deterministic/RL/exact-tick path, so their trajectory is byte-identical (the branch is never taken), and the cadence reset is PRE-EXISTING behavior shared by the `user`/`menu` pauses on the already-non-deterministic human wall-clock path — so no version bump and no clock fix here. (3) The hold placement, terminal precedence, gesture-cancel/arming, window-close, and audio edges were all reproduced by Codex and folded with pinned regressions. Persistence is deferred because GM-10h owns it and `week_index` rides on `steps`; the RL offer integration is GM-10b/GM-12. `WEEK_LENGTH_STEPS=1200` is a provisional foundation default (fixed, not escalating — escalation is GM-11). GM-10b (deterministic dedicated-RNG offers) opens next.
