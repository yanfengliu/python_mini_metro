- **High — Verification is too weak.** `DESIGN.md` accepts equality of only the finding-class multiset before promoting findings to verified. Identical classes at different steps, with different severity/routing/data, could therefore verify incorrectly. Compare canonicalized full findings—including addressed step refs—after stripping only run-specific fields.

- **High — CI/file dependency is not reproducibly pinned.** `file:../civ-engine` does not lock sibling contents, yet the design relies specifically on civ-engine 2.1.0. Pin the CI checkout to an exact commit and assert its package version. Also explicitly checkout both repositories into sibling directories inside `$GITHUB_WORKSPACE`, set `python_mini_metro` as the working directory, and install a declared Node ≥20 via `actions/setup-node`; the current workflow checks this repository out at workspace root, where `../civ-engine` cannot be produced portably.

- **Medium — PLAN steps 2–3 invert the required setup/TDD order.** Step 2 expects Node tests and scripts importing civ-engine before step 3 creates `package.json`, installs the file dependency, and produces the lock. Establish the minimal Node manifest/install first, then write failing Node tests, implement, and generate/finalize the lock and audits. The end-to-end scenario should likewise be introduced through a failing test before its implementation fixture.

- **Medium — Required review artifacts are absent from the plan.** Step 7 runs high-risk Codex/Claude review but never requires preserving `raw/codex.md`, `raw/opus.md`, a path-limited `diff.md`, or synthesized `REVIEW.md` before moving the thread. Those artifacts are mandatory under the repository’s thread policy.
