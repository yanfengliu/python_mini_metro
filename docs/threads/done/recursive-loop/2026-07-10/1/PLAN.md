# Recursive loop implementation plan

1. Write failing Python tests for ordered BFS tie-breaking, agent action deep-copying, strict scenario validation, N operations to N transcript rows, canonical UUID-free JSON checkpoints, oracle authorship as unverified, and the required cross-view topology oracle. Add latent-state perturbation cases for spawn counters/intervals, travel plans, unlock/progression, metro motion/dwell, and Python/NumPy RNG state. Implement only enough graph, recorder, and `src/recursive_playtest.py` behavior to pass them.
2. Add the minimal ESM `package.json` with `civ-engine: "file:../civ-engine"`, install it to create the initial lock, and prove the live engine import/version before any Node test imports it. Then write failing built-in Node tests for exact semantic finding re-derivation, strict verified evidence, full checkpoint-vector comparison, nondeterminism findings, candidate filtering/severity order, exact outcome vocabulary, repo-level manifest completeness/read-back, portable truthful artifact paths, and valid `run-failed` ledger rows. Implement `scripts/playtest-verify.mjs`, `scripts/recursive-pass.mjs`, and `scripts/playtest-recursive.mjs` to pass them.
3. Write a failing end-to-end test that requires the checked-in deterministic scenario, then add the fixture and separate-process drive. Add black-box orchestration tests for a nonexistent `PYTHON`, a nonzero child, and an unparseable scenario; each must prove exit 1 and exactly one new complete, strict/read-back-valid `run-failed` run/pass row. Finalize `package-lock.json`, add output/cache ignores, regenerate the hashed Python lock, then run `npm audit` and `pip-audit`.
4. Update CI for sibling civ-engine checkout/build plus Node and Python gates. Run the smallest affected tests after each change, then all Node tests and the full 174-test-or-larger Python suite.
5. Read `loop-ops/DIRECTIVES.md`, run the acceptance drill exactly as designed, and retain evidence only under ignored `output/`: two clean passes; temporary observation defect to exact-semantics-verified proposal; promoted regression added and run red while the defect remains; restored fix with the regression green and the class absent on rerun; and unparseable/spawn/nonzero failure-ledger proof.
6. Update root documentation and progress. Run changed-file Ruff checks, Ruff format checks, and pre-commit; run full unit and recursive-loop gates; inspect the complete diff and verify no output, caches, `.agents/`, credentials, or unrelated changes are staged.
7. Preserve path-limited `diff.md`, raw reviewer reports, and a synthesized `REVIEW.md`. Run the required in-process adversarial finder/verifier workflow. The external Codex and Claude plan reviewers were both unavailable because the security layer denied repository-context export before any content was sent; record that limitation and do not retry without explicit user approval. Verify every in-process claim against live files, fix all real findings, rerun affected and full gates, and iterate until only nitpicks remain.
8. Move the completed thread from `docs/threads/current/recursive-loop/` to `docs/threads/done/recursive-loop/`, stage only the coherent task, inspect the cached diff, commit directly to `main` with the dependency audit result, and push `main` to `origin`.
