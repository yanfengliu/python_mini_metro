You are a senior code reviewer reviewing a substantive implementation plan before any code is written. Do not modify files or propose patches. Review `docs/threads/current/recursive-loop/2026-07-10/1/DESIGN.md` and `PLAN.md` for onboarding this Python pygame repository into the fleet recursive self-improvement loop. Verify each claim in the plan/diff against the live codebase — grep for the symbols, function signatures, column names, and file paths it references; do not approve based on prompt text alone.

Task intent: build a deterministic scripted MiniMetroEnv drive, complete evidence artifacts, civ-engine 2.1.0 manifest/finding validation, fresh-process full-checkpoint verification, proposal-only candidate selection, failure-safe ledgers, dependency locks/audits, CI gates, and the canonical acceptance drill. The current baseline is 174/174 Python tests passing; only `.agents/` is pre-existing untracked state. Relevant fleet sources are `../loop-ops/docs/skills/building-recursive-loop.md`, `../loop-ops/docs/skills/recursive-playtest.md`, and `../civ-engine/src/improvement-loop.ts`.

Flag only real, important issues in correctness, deterministic proof strength, engine schema use, failure handling, test-first sequencing, dependency/CI portability, scope, documentation/process compliance, or missing acceptance evidence. Also flag process regressions, stale documentation, and missing validation. Return findings with severity and concrete file/section evidence, or `APPROVE` if there are no substantive findings.

Begin your review with the literal token "===BEGIN-REVIEW===" on its own line and end with "===END-REVIEW===" on its own line. Do not emit those markers anywhere else in your output.
