# Recurrent RL memory implementation plan

1. Add red contract tests before production edits for an eight-frame default stack, `3 * frame_stack` channel shape, zeroed history after reset/automatic episode rollover, preserved terminal stacked observation, recurrent hidden-state propagation during custom evaluation, exact episode-boundary memory masks that prevent cross-game leakage, legacy four-frame PPO resume inheritance, explicit resume mismatch rejection, and real `RecurrentPPO` learn/save/in-memory-load/predict behavior on the pixel `MultiDiscrete` environment.
2. Make `recurrent_ppo` the fresh-run default while retaining `ppo` as a supported ablation and legacy artifact format. Give `--algorithm` and `--frame-stack` `None` sentinels so fresh runs resolve to the new defaults while resumes inherit the saved values unless the user supplied a conflicting override. Add algorithm-aware hyperparameter manifests, constructors, authenticated loaders, checkpoint naming, evaluation dispatch, and resume validation; existing `ppo`/four-frame manifests remain evaluable through dispatch, with the existing explicit `--allow-training-drift` and `--allow-runtime-drift` flags required when their pre-change fingerprints differ.
3. Increase the fresh-run frame-stack default from four to eight. At the fast profile this doubles local pixel history from roughly 0.4 to 0.8 seconds while bounding the eight-environment, 128-step uint8 rollout observation buffer near 486 MiB; the episode-persistent LSTM supplies longer context without the roughly 972 MiB observation buffer of a sixteen-frame stack.
4. Add separate 256-unit, one-layer actor and critic LSTMs through `sb3-contrib` `CnnLstmPolicy` and the existing `MiniMetroCNN`, explicitly recording `lstm_hidden_size=256`, `n_lstm_layers=1`, `shared_lstm=false`, and `enable_critic_lstm=true` in the manifest hyperparameters. Carry recurrent state across every decision and PPO rollout, mask it at episode boundaries, save learned recurrent weights in normal authenticated model artifacts, and document that live hidden state is intentionally not carried across games or exact mid-episode process restarts.
5. Make the optimization contract explicit: fresh training continues to use only newly delivered passengers as step reward; the episode sum and primary evaluation metric are total deliveries. Challenge and validate the long-horizon estimator settings against the finite configured horizon, with `gamma=1.0` and a longer-credit `gae_lambda=0.99` as the proposed defaults; retain recorded hyperparameters and a feed-forward PPO ablation so empirical multi-seed comparison can overturn these defaults without changing the environment reward contract.
6. Add and pin `sb3-contrib==2.9.0`, add `sb3-contrib` to both `DEFAULT_PACKAGE_NAMES` and `COMPATIBILITY_PACKAGE_NAMES` with a runtime-drift regression, regenerate the universal hashed RL lock with the repository's recorded `uv pip compile --python-version 3.13 --universal --generate-hashes` command, install/test the exact lock in an ignored workspace environment if the managed interpreter cannot be changed, run `pip check`, and complete the mandatory `pip-audit` with zero unresolved CVEs.
7. Write a primary-source model-selection note comparing feed-forward CNN PPO, CNN-LSTM RecurrentPPO, IMPALA-style recurrent CNNs, Vision Transformers, GTrXL/temporal Transformers, and DreamerV3. Recommend CNN-LSTM RecurrentPPO as the pragmatic integrated baseline, DreamerV3/RSSM as the research-grade candidate, and a conditional pointer/action head plus multi-seed ablation as higher-priority future experiments than an unvalidated ViT swap.
8. Update README, architecture, progress, CI, and RL smoke coverage. Run the focused red/green loop, real fresh and resumed recurrent lifecycle, legacy PPO evaluation coverage, full 305+ Python suite, changed-file and full-repo Ruff/format checks, pre-commit, `git diff --check`, Node tests, dependency audits, and an in-process finder/verifier review followed by required Codex and Claude diff reviews.
9. Fix every verified finding, rerun affected and full gates, preserve raw reviewer evidence, synthesize `diff.md` and `REVIEW.md`, move this thread into `docs/threads/done/rl-framework/2026-07-11/1`, stage only the coherent task files (never the pre-existing untracked `.agents/` tree), commit directly to `main` with the zero-CVE audit in the message, push, and verify the remote result.
