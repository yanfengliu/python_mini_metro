I've verified the load-bearing memory figure directly: `FAST_RENDER_PROFILE = RenderProfile("fast", 192, 108)` → `observation_shape = (3, 108, 192)` uint8 (`protocol.py:85,91,258`), and the action space is `multidiscrete` (`protocol.py:332`). So 8 frames × 3 = 24 channels; 8 envs × 128 steps × 24 × 108 × 192 = 509,607,936 B = **exactly 486.0 MiB**, and 16 frames = 972 MiB. The plan's item 3 rationale is arithmetically correct against the real resolution.

I now have everything I need. Here is my review.

---

## Verdict: APPROVED

The revised plan closes all five prior gaps, and every technical claim I could statically check holds against the live code. Residual items below are non‑blocking (one process note + minor confirmations); none makes the plan insufficient.

### The five prior gaps — each resolved, grounded in live code

**1 — argparse fresh‑vs‑resume (was MEDIUM) → RESOLVED.** Live code confirms the defect: `train_rl.py:96` uses concrete `default=DEFAULT_FRAME_STACK`, there is no `--algorithm` flag at all, and the resume gates hard‑`raise` at `train_rl.py:196-197` (algorithm) and `:198-202` (frame_stack). Plan item 2 introduces `None` sentinels for both `--algorithm` and `--frame-stack` (fresh → new defaults, resume → inherit unless conflicting), and item 1 adds red tests for *both* "legacy four‑frame PPO resume inheritance" and "explicit resume mismatch rejection." That is exactly the fix requested, and both the happy path and the reject path are covered.

**2 — sb3‑contrib provenance (was LOW/MED) → RESOLVED.** Confirmed both tuples currently omit it (`provenance.py:15-24` `DEFAULT_PACKAGE_NAMES`, `:25-33` `COMPATIBILITY_PACKAGE_NAMES`) and that drift only iterates the compatibility tuple (`:200,213-219`). Item 6 names both exact sites plus a runtime‑drift regression. Hyphenated `sb3-contrib` matches the existing `pygame-ce`/`stable-baselines3` convention, so `metadata.version` lookups stay consistent.

**3 — legacy PPO drift‑override (was LOW) → RESOLVED (plan‑level).** Confirmed the unconditional gates at `evaluate_rl.py:163` and `train_rl.py:196`, that `TRAINING_SOURCE_PATHS` hashes both RL requirements files (`training.py:26-40`) → adding the dep shifts the training fingerprint, and that both `--allow-training-drift` / `--allow-runtime-drift` flags already exist (`train_rl.py:118-127`, `evaluate_rl.py:70-79`). Item 2 now states legacy artifacts stay evaluable via dispatch and require *both* flags once fingerprints differ — correct, because adding `sb3-contrib` to `COMPATIBILITY_PACKAGE_NAMES` makes old manifests (which recorded no such version) trigger a runtime difference too. Fail‑closed behavior is accurately described.

**4 — LSTM topology + manifest (was LOW) → RESOLVED.** Item 4 disambiguates to *separate* actor/critic LSTMs via `CnnLstmPolicy` with `lstm_hidden_size=256, n_lstm_layers=1, shared_lstm=false, enable_critic_lstm=true`, all recorded in the manifest. `CnnLstmPolicy` is the correct sb3‑contrib RecurrentPPO policy for a Box image space (not `MultiInput`), and that field set is sb3‑contrib's standard separate‑critic config. The manifest needs no schema migration: `hyperparameters` is open, arbitrary frozen JSON (`manifest.py:160`) and `algorithm` is a free‑form validated string (`manifest.py:129,133`) — so a `recurrent_ppo` value and the new keys are recordable and fingerprinted, and legacy `ppo` manifests remain parseable.

**5 — evaluator boundary‑mask test (was LOW) → RESOLVED (plan‑level).** Confirmed `evaluate_vector_policy` discards state (`evaluation.py:50`) and the fake lacks the params (`test_rl_evaluation.py:16`). Item 1 requires red tests for "recurrent hidden‑state propagation during custom evaluation" and "exact episode‑boundary memory masks that prevent cross‑game leakage" — i.e. threading state *and* resetting `episode_start` on `done`, which is precisely what refutes cross‑episode leakage. A single unified evaluator works for both algorithms because SB3's base `predict(obs, state, episode_start, deterministic)` signature is uniform (state stays `None` for feed‑forward PPO).

### Residual notes (advisory, non‑blocking)

- **Process — plan review rests on a single reviewer.** The primary Opus run hit a "Fable 5 limit" (`raw/plan-opus.md`), Codex got 401 Unauthorized (`raw/plan-codex.stdout.log`), and `raw/plan-opus-fallback-2.stderr.log` is empty — only `plan-opus-fallback.md` produced findings. This re‑review is a second independent pass, but per AGENTS.md this is high‑risk work (provenance, dependency files, ARCHITECTURE.md, CLI surfaces, CI). The required **Codex + Claude diff review** at item 8/9 must actually succeed at implementation time rather than fall back again — that is now the real convergence gate.
- **CI registration (LOW).** The real `RecurrentPPO` lifecycle test (item 1) can only execute in the Windows `rl-smoke` job, which runs an *explicit* module list (`.github/workflows/test.yml:87`); the ubuntu `build` job installs only `requirements-locked.txt` (no torch/sb3‑contrib, `test.yml:53`). Land that test inside an already‑listed module (`test_rl_training.py`/`test_rl_evaluation.py`) or append the new module to line 87 — otherwise it silently won't run. Covered by item 8's "update CI and RL smoke coverage," just make it concrete. (Bonus: flipping the default means the existing rl‑smoke fresh/resume/eval steps will now exercise the recurrent path end‑to‑end.)
- **Dependency guard (LOW, optional).** `require_rl_dependencies()` (`training.py:60-67`) builds the single actionable "install requirements‑rl" message but doesn't list `sb3_contrib`; since recurrent is the new default, adding it there keeps the friendly error complete. The recurrent constructor's own import will fail loudly regardless, so this is polish.
- **Dispatch default‑raise (LOW).** Because `algorithm` accepts any non‑empty string, the new dispatch must raise on unknown values to stay fail‑closed. Implied by "algorithm‑aware dispatch," worth an explicit branch.

### Validation caveat
As with the prior reviewer, the Bash sandbox is down this session (`EPERM … session-env`), so I verified statically only — I could not import `sb3_contrib`, run `pip check`/`pip-audit`, or execute the suite. The item‑1 "real `RecurrentPPO` learn/save/load/predict on the pixel `MultiDiscrete` env" test remains the necessary dynamic proof and must pass where the regenerated lock is installed. The memory arithmetic (486/972 MiB), the recorded lock command (`requirements-rl-locked.txt:1-2` matches item 6 verbatim), and the `stable-baselines3==2.9.0` pairing (`requirements-rl.txt:3`) all check out.
