You are a senior code reviewer. Flag bugs, security issues, and performance concerns in the live rendering plus player-equivalent RL framework diff. Do NOT modify files or propose patches. Only return findings, explanations, and suggestions in plain text. Be concise but preserve impact and file/line evidence. Only point out a real and important issue; if there is none, say APPROVED.

Task intent: finish the existing uncommitted rendering prerequisite and ship a player-equivalent Gymnasium pixel environment plus a spawn-safe PPO training/evaluation stack. The policy sees only rendered pixels and low-level player input. Structured MiniMetroEnv remains compatible. Saved runs bind protocol/task/content/trainer/runtime provenance and authenticated artifacts; resume/evaluation fail closed.

Review the entire live worktree, including untracked files. Focus especially on src/rl/, scripts/train_rl.py, scripts/evaluate_rl.py, src/game_clock.py, src/game_session.py, src/rendering/, src/simulation_context.py, .github/workflows/test.yml, dependency locks, and test/test_rl_*.py. Also inspect shared game changes where they feed pixels or deterministic state.

Important prior findings already fixed and needing refutation: mixed boolean actions; missing terminal/event/cursor/fidelity/future-content tests; duplicated protocol defaults; SB3 load overriding requested seeds; repeated training evaluations consuming different seed suites; manifest/index/model verify-then-reopen races; evaluation output overwriting authenticated files; missing Shapely/shortuuid compatibility; stale evaluation provenance after a long run; missing lockfile fingerprint inputs; and absent resume CI coverage.

Validation already performed on Python 3.13.10 with Gymnasium 1.3.0, SB3 2.9.0, TensorBoard 2.21.0, Torch 2.13.0+cpu: 305/305 Python tests; 62/62 focused RL tests; full Ruff and format; both pip-audits report zero CVEs; npm audit zero vulnerabilities; pinned civ-engine 2.2.0 Node suite 41/41; recursive pass no-fix-candidate with exact 8/8 replay; fresh and resumed spawned PPO lifecycle with bound digests and seeds 10042/10077; tampered model and protected output rejection; 232 decisions/s and stable memory in the pixel probe. The pre-commit wrapper's private environment bootstrap stalled twice, but its exact YAML/whitespace/EOF hook implementations and Ruff hooks were run directly and pass.

Verify each claim in the plan/diff against the live codebase — grep for the symbols, function signatures, column names, and file paths it references; do not approve based on prompt text alone. Flag process regressions, stale documentation, and missing validation as well as code defects.

Begin your review with the literal token "===BEGIN-REVIEW===" on its own line and end with "===END-REVIEW===" on its own line. Do not emit those markers anywhere else in your output.
