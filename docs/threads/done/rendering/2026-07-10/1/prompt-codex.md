You are a senior code reviewer. Review the current uncommitted smooth-rendering diff in C:\Users\38909\Documents\github\python_mini_metro. Flag real bugs, regressions, API hazards, performance concerns, test gaps, process regressions, stale documentation, and missing validation. Do not modify files or propose patches. Only return findings, explanations, and suggestions in plain text. Be concise but retain file/line evidence. If there is no important issue, say so.

Intent: keep Python 3.13 and pygame-ce while separating logical route geometry from immutable visual lanes; add fixed 17/17/16 ms updates, render-only metro interpolation, bounded antialiased route caching, pure rendering, prepared hitboxes before input, lazy headless-safe bundled fonts, warm background, symmetric lanes, and outlined line-colored metros. Gameplay timing/topology must remain unchanged. The legacy MiniMetroEnv contract and recursive checkpoint/replay behavior must remain unchanged. Rendering must be deterministic and must not mutate gameplay or render-facing model state. The window must process input before updates, use one pacing authority, retain one renderer/cache, and reuse resize targets.

Focus on src/main.py, src/game_clock.py, src/game_session.py, src/mediator.py, src/rendering/, src/entity/path.py, draw methods under src/entity/ and src/geometry/, UI layout/drawing, config, all new/changed tests, ARCHITECTURE.md, GAME_RULES.md, README.md, and PROGRESS.md. Validation reported before review: 226 Python tests pass; repeated real-surface frames have identical RGBA bytes and unchanged canonical/render-state snapshots; rendered-vs-never-rendered trajectories match; one-entry route cache reuses its surface/layout; 300 warmed 1920x1080 frames took 0.4051 seconds.

Verify each claim in the plan/diff against the live codebase — grep for the symbols, function signatures, column names, and file paths it references; do not approve based on prompt text alone. Inspect git status and git diff yourself. Preserve existing game behavior unless explicitly described above.

Begin your review with the literal token "===BEGIN-REVIEW===" on its own line and end with "===END-REVIEW===" on its own line. Do not emit those markers anywhere else in your output.
